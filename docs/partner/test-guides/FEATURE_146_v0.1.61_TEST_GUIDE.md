# Partner 插件库（P1–P5 与首次连接）— 人工测试指导

## 功能概述

功能：F146 Partner Plugin Library Extension。规划版本：v0.1.61（fork）；当前宿主仍为 0.1.45，SDK 仍为 0.7.95。测试日期：2026-08-31；测试人员：待填写。

本指南覆盖独立插件包、专家、专家编辑与可选单个 Skill，以及飞书 CLI 连接器。飞书真实账号读写需单独记录；受控 fixture 通过不能代替第三方端到端验收。

## 测试环境

- 使用本仓库最新构建的 **Electron 桌面应用**，不以浏览器 5174 预览替代。
- 先执行 `npm run build:smoke` 和 `npm run build:partner-extension`；独立包位于 `out/extensions/kodax.partner-library-0.5.1.space-extension`。
- 使用专门的测试项目和隔离 profile；不要卸载或改动其他 profile 中的真实扩展，不删除共享 Skill。
- 安装和专家编辑不需要账号。实际模型回复测试只使用已授权的测试模型和无敏感信息的任务；离线 mock 回复不作为角色执行证据，实际 SDK profile 的自动化验证见开发计划。
- Skill 正向测试使用现有技能库中一项已确认适用于 Partner 的技能，记录其确切名称。不要把示例名称当成已经安装的技能。
- 分别记录开发态与本机打包态、窗口宽度、明暗主题。其他操作系统未测试时填“未验证”。

## 测试用例

### TC-001：独立包安装、启用和撤下

优先级：高。类型：正向测试。前置条件：测试 profile 未安装该包。

1. 打开“设置 → 扩展”，点击安装，选择上述 `.space-extension` 文件。
2. 检查新安装包未自动启用；启用后切换到 Partner，点击“文件”下面的“插件”。
3. 检查目录包含“专家 / 连接器”，专家列表来自包；0.5.1 的连接器展示名为“飞书”，未验证用户账号时不能显示为已连接。
4. 禁用包，再重新启用；然后卸载并重新安装、启用。

预期效果：

- [ ] 只有启用时显示 Partner 插件入口；禁用和卸载关闭相应页面，核心聊天与设置安装入口仍可用。
- [ ] 安装不连接账号、不安装 Skill、不发送消息。重装可以恢复入口。

实际结果：待填写。是否通过：[ ] Pass / [ ] Fail。

### TC-002：选择角色、保存会话与版本隔离

优先级：高。类型：正向测试。前置条件：包已启用；使用测试项目和已授权模型。

1. 在 Partner 输入一段未发送草稿，打开插件库，选择“写作导师”。
2. 返回对话，确认草稿原样保留且输入框显示专家。查看右侧专家详情，再发送两轮简短写作任务。
3. 新建另一会话，确认没有自动继承专家；返回旧会话，关闭并重启 Electron，检查原绑定。
4. 在一轮进行时切换或移除专家；下一轮再次发送。若使用 after-turn 排队消息，在其执行前完成专家切换。

预期效果：

- [ ] 选择不自动发送、不覆盖草稿，角色持续作用于该会话，两个会话互不串角色。
- [ ] 当前轮保持原角色；后续新轮读取已经保存的选择。重启恢复原版本；显式 fork 才继承绑定。

实际结果：待填写。是否通过：[ ] Pass / [ ] Fail。

### TC-003：创建、编辑和软删除自己的专家

优先级：高。类型：正向测试。前置条件：包已启用，已经存在一个绑定预置专家的测试会话。

1. 编辑一个预置专家，修改名称和角色提示词，保存。
2. 检查得到独立用户副本、原预置未被修改；选择用户副本并创建会话。
3. 再次编辑这个副本；检查 revision 增加，已有会话仍保留旧提示词。明确重新选择后才应用新版本。
4. 删除这个用户专家，确认对话框说明历史保留；检查原绑定显示不可用。
5. 创建另一个用户专家，卸载并重装同一插件包，检查该自定义专家仍在，已软删除专家没有复活。

预期效果：

- [ ] 保存不会自动选择或替换会话角色；冲突编辑有明确提示，不用旧表单覆盖新版本。
- [ ] 删除不清除会话历史；卸载不删除自定义专家数据、共享 Skill 或原项目文件。

实际结果：待填写。是否通过：[ ] Pass / [ ] Fail。

### TC-004：可选 Skill、失败提示与明确降级

优先级：高。类型：负向测试。前置条件：准备一个兼容现有 Skill 的专家，以及一个填了不存在 Skill 名称的专家。

1. 使用兼容专家连续发送两轮；本轮从原 Skill 入口显式选择另一技能，检查该轮以用户明确选择为准。
2. 使用缺失 Skill 的专家并尝试发送，确认不会伪装成技能已执行，不会自动安装或找替代。
3. 明确选择“仅用提示词”，再次使用；检查偏好在会话中保存、Skill 配置本身没有被删除。
4. 对普通未配置 Skill 的专家使用原有 Skill 入口；切回 Coder 检查原入口和原规则。

预期效果：

- [ ] 缺失、fork 或被既有 hook 规则阻止的配置明确拒绝，不调用外部模型假装成功。动态上下文命令仍禁止；当前 SDK 若返回禁用占位后继续，则保留该原有行为，不把它记成动态命令成功执行。
- [ ] 只有用户明确选择后才跳过专家默认 Skill；原独立 Skill 调用仍可使用，一轮不叠加两个显式 Skill。

实际结果：待填写。是否通过：[ ] Pass / [ ] Fail。

### TC-005：表单和存储边界

优先级：中。类型：边界测试。前置条件：打开创建专家表单。

1. 尝试空名称、全空白提示词；再分别填写 80 字名称、280 字说明、8000 字提示词。
2. 尝试超过上述限制、超过 4 条示例任务或单条超过 512 字；试填 `../skill` 作为 Skill 名称。
3. 保存带中文、换行和 emoji 的合法内容，再打开编辑确认完整保留。
4. 在隔离自动化夹具中验证 128 条用户记录（含软删除记录）和 1MiB 文件上限，不用真实数据手工填满。

预期效果：

- [ ] 合法边界可保存；越界、非法身份和 Skill 名称被拒绝，既有文件保持可读，没有静默截断或覆盖。

实际结果：待填写。是否通过：[ ] Pass / [ ] Fail。

### TC-006：页面和键盘操作

优先级：中。类型：UI 测试。前置条件：包已启用，分别使用宽、窄窗口和明暗主题。

1. 检查 Partner 输入框已没有原八个常驻模板按钮；保留原 Skill、权限、交付格式、模型和项目选择能力。
2. 在插件库用 Tab 切换分类、专家卡片、创建/编辑按钮及表单；保存、取消和错误提示可见。
3. 打开专家详情，查看角色、来源、revision、示例任务和可选 Skill；检查右侧详情仍可关闭再打开。

预期效果：

- [ ] 控件不溢出、长提示词可滚动、焦点可见；选择角色后能继续输入，错误不会变成空白页面。
- [ ] Coder 不显示 Partner 插件导航；现有资料 / 待审核 / 成果卡片保持可用。

实际结果：待填写。是否通过：[ ] Pass / [ ] Fail。

### TC-007：快速切换与并发编辑

优先级：中。类型：性能测试。前置条件：至少两个测试项目和两段 Partner 会话。

1. 连续切换会话、打开/关闭插件页面 20 次，同时保留未发送草稿。
2. 在保存或选择未完成时切换项目，再返回；同时从两份旧表单编辑同一用户专家。
3. 记录首次加载与保存耗时，观察是否卡死、出现重复请求或持续增长的后台刷新。
4. 尚未发送的草稿已选专家时，分别通过侧栏、文件菜单、快捷键、命令面板和项目行新建对话；检查专家清空而正文保留。用自动化夹具延迟旧创建 ACK，确认旧会话不能重新激活。

预期效果：

- [ ] 旧请求不会改变新项目的专家或吞掉草稿；并发同 revision 修改至多一份成功，另一份明确冲突。
- [ ] 操作中有反馈、关闭后不会持续调用；记录实际耗时，不把未测量结果写成性能保证。

实际结果：待填写。是否通过：[ ] Pass / [ ] Fail。

### TC-008：不可信扩展边界

优先级：高。类型：安全测试。前置条件：仅用隔离 profile 和仓库恶意包自动化夹具。

1. 尝试安装包含越界路径、符号链接、可执行入口、超大压缩数据、错误 hash 或不兼容 API 的包。
2. 在专家名称/说明填入 `<img src=x onerror=alert(1)>`，查看列表和详情。
3. 运行 IPC/frame 来源负向测试：伪造 source/token、子 frame 发宿主 IPC、重复请求、禁用后的新请求。
4. 检查插件页面只得到公开定义和受限结果，没有 Node、任意文件、账号密钥或任意 invoke。

预期效果：

- [ ] 不可信包/调用被拒绝，已有有效包和主应用仍可用；HTML 显示为文本，不执行脚本。
- [ ] 完整性 hash 只证明内容一致，界面不把它当作发布者身份认证。

实际结果：待填写。是否通过：[ ] Pass / [ ] Fail。

### TC-009：无包与打包态兼容

优先级：高。类型：兼容性测试。前置条件：分别准备开发态和本机打包的 `.app`，使用隔离 profile。

1. 未安装任何插件时构建、启动并使用默认 Partner / Coder。
2. 在两种运行方式下重复 TC-001、TC-002 的绑定恢复和 TC-003 的重装保留检查。
3. 打开修改前的旧会话，确认缺少 expert 字段时仍正常；仅在用户明确选择专家后增加绑定。

预期效果：

- [ ] 主程序不依赖插件源码/包存在；打包态从安装目录加载独立 UI，主 renderer CSP 不因插件被放宽。
- [ ] 不需要升级 SDK；原会话和原 Skill 行为不变。未执行的平台明确记录未验证。

实际结果：待填写。是否通过：[ ] Pass / [ ] Fail。

## 测试总结

| 用例数 | 人工通过 | 人工失败 | 未执行/阻塞 |
| ------ | -------- | -------- | ----------- |
| 24     | 待填写   | 待填写   | 待填写      |

自动化与桌面观察记录见 [开发计划](../features/v0.1.61-partner-plugin-library.md)；双轴复核见 [P1–P3 评审记录](../features/F146-plugin-library-review.md)和 [P4/P5 评审记录](../features/F146-p4-p5-review.md)。以上复选框是验收清单，不代表已执行通过。

### 2026-08-31 已执行的桌面子步骤

环境：本机 macOS arm64 打包版 0.1.45、独立包 0.3.0、隔离 profile、深色主题，使用 embedded 配置和离线 Mock。未改正式账号 / 项目；下面只记录已观察的子步骤，不把部分覆盖计为整条用例通过。

| 用例            | 已观察结果                                                                                      | 尚未覆盖的主要部分                                  |
| --------------- | ----------------------------------------------------------------------------------------------- | --------------------------------------------------- |
| TC-001          | 原生选择器安装、安装默认禁用、启用显示插件、禁用撤下入口、卸载后重装 / 启用均成功；连接器计数零 | 其他运行方式 / 平台                                 |
| TC-002          | 选择保留草稿且不自动发送；标签打开详情；离线两轮保持专家；新对话清空角色，返回旧会话恢复        | 真实模型、应用重启、运行中切换的人工复核            |
| TC-003          | 创建用户专家、编辑到 r2；卸载 / 重装后该专家及会话仍在，旧绑定恢复可用                          | 编辑预置副本、软删除、并发版本冲突的人工复核        |
| TC-004          | 不存在的 Skill 在首次发送时被拒绝；明确关闭默认 Skill 后发送成功，快照持久化 `useSkill: false`  | 真实兼容 Skill 正向运行及本轮显式覆盖               |
| TC-006 / TC-007 | 原八按钮已退出输入框；原 Skill / 权限 / 格式 / 模型入口保留；侧栏新对话清空专家但保留正文       | 明暗 / 窄屏、所有键盘路径、性能测量                 |
| TC-009          | 无扩展打包态启动、安装独立 UI、卸载 / 重装保留配置通过；主 ASAR 不内置插件源码 / 归档           | 开发态完整循环、真实模型重启 / 旧历史兼容的人工复核 |

其余表单 / 安全 / 并发边界已有自动回归，但未逐项通过桌面操作重演。实际 SDK profile、多轮、隔离、恢复 / fork 由本地 RecordingProvider 测试验证，不用 Mock 回复声称角色质量或第三方服务已验证。

## P4/P5 飞书连接器追加用例

### 本轮已观察的桌面子步骤（2026-08-31）

macOS arm64 最新打包版 0.1.45、独立扩展 0.4.0、同一隔离 profile、离线 Mock。构建和 packaged smoke 均通过。插件库保留九个预置及先前的隔离测试专家，新增“连接器 1 → 飞书文档”；“设置连接与会话范围”打开宿主右侧详情，真实显示“未找到飞书 CLI，请先安装官方 lark-cli”、固定版本安装说明、空账号选择及全局禁止写入提示。没有运行安装命令、授权账号或启用写入；TC-010 的缺少 CLI 分支已观察，其他需要真实 CLI / 账号的步骤仍未执行。

首次桌面自动检查有画面 / AX 更新滞后。重新启动隔离 QA 进程并使用 `--disable-renderer-backgrounding --disable-backgrounding-occluded-windows` 后完成上述导航观察；这些仅是测试进程参数，不是生产代码修改。源码完整 Shell 的导航复测正常，尚未确定默认启动时该现象的原因，P6 需在用户实际桌面环境复核。不要据此将整条端到端用例标为通过。

真实账号前置条件：用户按 TC-018 完成官方授权，或使用升级前已经连接并保留的专用测试账号；准备一个不含敏感数据的 docx 文档及新建文档用文件夹。不要把账号密码、授权 URL 或 token 写入测试记录。以下均为待执行清单，不代表真实飞书已通过。

### TC-010：连接、选择范围和读取

优先级：高。类型：正向测试。前置条件：上述测试账号有 docx 读取权限，独立包 0.5.0 已启用。

1. Partner → 插件 → 连接器 → 飞书，打开可信连接弹窗。
2. 完成首次连接或使用已有的已连接测试账号；确认尚未自动给当前会话授权。点击“去试试”。
3. 从输入框连接器菜单打开文档范围，添加一个指定 docx URL，选择“读取”，保存到当前会话。
4. 发送无敏感信息的读取请求，或在详情中点击读取；查看右侧资料卡及正文详情。

预期：仅所选文档可读，保留真实 URL、读取时间和版本；新会话不会继承；账号凭据不出现在详情、消息或日志。实际结果：待填写；是否通过：[ ] Pass / [ ] Fail。

### TC-011：新建 / 追加必须审核

优先级：高。类型：正向测试。前置条件：账号具备相应写入 scope；用户显式允许全局连接器审核写入，并给测试会话授权新建文件夹 / 可追加文档。

1. 生成一个新建提案，检查飞书尚未出现文档；核对“待审核”里的文件夹、操作、标题和完整正文。
2. 先取消确认，验证没有写入；再次审核并确认一次，检查成功回执对应真实文档。
3. 针对另一个已授权测试 docx 生成追加提案；审核后确认原内容保留、新文本追加。
4. 重复点击同一提案，检查远端没有第二次新建 / 追加。

预期：只有完整确认回执才进入成果；拒绝 / 取消不写；默认不开全局写入；不提供覆盖、删除和附件上传。实际结果：待填写；是否通过：[ ] Pass / [ ] Fail。

### TC-012：撤销、冲突与未知结果

优先级：高。类型：负向测试。前置条件：使用可安全中断的测试文档与网络。

1. 提案生成后，移除标签、断开账号或禁用包，再尝试提交。
2. 生成追加提案后，在飞书更改文档，再提交旧提案。
3. 提交过程中中断网络或结束测试应用，再启动查看状态；先在飞书核对结果，勿直接重试。

预期：失效授权不能派发，版本变化显示冲突；中断可能显示 unknown，部分结果显示 partial，不出现成功回执或自动重试。实际结果：待填写；是否通过：[ ] Pass / [ ] Fail。

### TC-013：范围及正文边界

优先级：高。类型：边界测试。前置条件：专用测试会话。

1. 尝试非飞书网址、Wiki 地址、重复 docx、33 个文档以及空正文。
2. 在正文输入 `<image src="file:///tmp/private">` 等 XML / Markdown 样式文本；只在测试文件夹审核新建。
3. 测试 128 KiB UTF-8 正文上下边界，包含中文和 emoji。

预期：不支持的范围 / 超限内容明确拒绝；文本仅按纯文本写入，不访问本地文件或自动上传媒体。实际结果：待填写；是否通过：[ ] Pass / [ ] Fail。

### TC-014：切换上下文与窄面板

优先级：中。类型：UI 测试。前置条件：两个测试项目 / 会话，明暗主题。

1. 验证 / 读取尚未返回时切换项目或会话；重新打开连接器详情。
2. 在窄面板中检查标签、范围表单、完整提案正文和状态提示；用键盘访问控件。
3. 在新对话中同时选择专家与连接器，保留草稿；触发其他入口的新对话。

预期：不残留旧账号范围、写入草稿或永久 loading；专家与连接器一起拒绝过期创建响应，原正文草稿不被覆盖。实际结果：待填写；是否通过：[ ] Pass / [ ] Fail。

### TC-015：慢请求和重复审批

优先级：中。类型：性能测试。前置条件：可控制延迟的测试环境。

1. 慢速读取期间继续输入普通对话草稿，切换卡片。
2. 触发两次同一提案审批，并在一项提交运行时停用包。

预期：界面仍可操作，请求有时限；远端仅一次写入；停用等待已派发任务结算，并且立即拒绝新操作，不声称能撤回已提交请求。实际结果：待填写；是否通过：[ ] Pass / [ ] Fail。

### TC-016：账号隔离与最小权限

优先级：高。类型：安全测试。前置条件：两个用户明确配置的测试 profile，两段 Partner 会话及一个 Coder 会话。

1. 两个 Partner 各选择不同文档，请求读取另一会话 URL。
2. 切到 Coder，检查不会获得这些会话连接器工具。
3. 验证中的旧请求尚未完成时点击断开；检查旧请求完成后不会重新连接。
4. 全局禁止连接器写入或切到计划模式，检查旧提案不能提交。

预期：账号 / 会话 / 范围严格隔离；插件 frame 不能取得账号凭据或调用 apply；原 Skill 系统行为不变。实际结果：待填写；是否通过：[ ] Pass / [ ] Fail。

### TC-017：内置连接组件、重启与平台

优先级：高。类型：兼容性测试。前置条件：使用一次性安装包副本，分别准备正常内置归档、归档缺失 / 损坏、没有全局 CLI、存在不兼容全局 CLI，以及未安装飞书桌面客户端的环境。

1. 在没有全局 CLI、没有飞书桌面客户端的打包环境打开飞书连接弹窗并点击一次“连接”；检查没有“安装并继续”、CLI 下载或终端配置提示，本地准备完成后自动打开飞书官方一次性页面。
2. 在存在不兼容全局 CLI 的环境重复，确认仍使用 Space 内置固定版本；插件归档中不得出现 executable，且不会修改 PATH、全局 CLI 或共享 Skill。
3. 保持有效内置归档，删除或损坏 Space 私有托管可执行文件后重新启动，或让下一次命令出现未 dispatch 的启动失败；确认已有连接会从本地归档静默修复并重试一次，不联网、不要求重新授权。随后在一次性安装包副本中移除或篡改内置归档并以新进程首次使用，确认不会打开无效通用网页或显示已连接，只提示更新或重新安装 KodaX Space。
4. 在正常内置版本完成绑定，重启应用并显式 fork 会话，检查范围恢复 / 继承。
5. 在 macOS 开发态和打包态测试；其他平台逐项实测并记录，未测不可勾选。

预期：正常连接不要求用户取得 CLI，飞书客户端不是前置条件；私有托管副本异常且有效内置归档仍在时本地静默修复，内置归档异常才进入 Space 更新 / 重装路径；会话授权持久化；独立包移除不影响 Coder / Skill 和主聊天。实际结果：待填写；是否通过：[ ] Pass / [ ] Fail。

## 首次连接与简约界面追加用例

这些用例区分受控夹具、打包内置组件、真实授权及远端读写。执行网页登录/授权的步骤由用户本人完成；不要把测试夹具的账号或二维码当成真实服务。原“安装并继续”路线已被替代，当前正常流程不得再以显式 CLI 下载作为前置步骤。

### TC-018：从一次点击到官方授权完成

优先级：高。类型：正向测试。前置条件：当前 Partner 包已启用，KodaX Space 安装包包含本平台固定校验的飞书组件，专用测试账号允许联网，保存有未发送草稿；系统可不安装全局 CLI 和飞书桌面客户端。

1. 打开插件 → 连接器 → 飞书；仅查看弹窗，不点击连接，确认没有解包、授权或打开网页副作用。
2. 只点击一次“连接”；确认没有 CLI、安装、下载、npm 或 profile 决策，宿主完成本地静默校验 / 解包后自动打开飞书官方配置页。
3. 在官方网页完成创建 / 使用已有应用、扫码和文档及多维表格权限授权，返回 Space 等待校验；有效页面可从弹窗重新打开。
4. 看到已连接后检查卡片状态，再点击“去试试”。

预期：

- [ ] 没有全局 CLI 或飞书桌面客户端也能由一次点击进入官方网页；正常流程没有“安装并继续”或组件网络下载，不改全局 CLI / Skill，也不要求在 Space 填写密码或密钥。
- [ ] 官方授权后经真实用户身份及权限校验才显示已连接；连接本身不自动读取文档、发消息或覆盖草稿。
- [ ] 去试试只启用本对话，已有文档范围保留；新绑定为空范围，访问文档仍需用户授权。

实际结果：待填写。是否通过：[ ] Pass / [ ] Fail。

### TC-019：取消、拒绝、过期与重试

优先级：高。类型：负向测试。前置条件：专用账号，允许中止的授权流程。

1. 开始连接，在等待官方页面时关闭或取消；观察取消等待反馈。
2. 让原授权响应迟到，检查不会在取消成功后新增已连接账号。
3. 重新开始，测试网页拒绝、缺少所需权限、链接过期以及重新打开有效授权页。

预期：取消与失败有实际状态，重试不覆盖 default 或其它应用的 CLI 配置；过期链接不再打开。若网页已经完成授权，Space 不声称撤销了远端授权；已先提交的连接如实显示完成。

实际结果：待填写。是否通过：[ ] Pass / [ ] Fail。

### TC-020：重复点击与跨项目边界

优先级：高。类型：边界测试。前置条件：两个 Partner 项目、延迟可控的测试环境。

1. 快速重复点击连接，或打开同一连接器两次。
2. 在 start 返回前切换项目/会话，然后让旧结果返回。
3. 关闭所有会话连接器，再重新打开输入框菜单。

预期：同一活动连接任务去重；旧任务不能选中新的会话/项目；全部关闭时仍能管理/重新开启连接器，空范围不得当成全库授权。

实际结果：待填写。是否通过：[ ] Pass / [ ] Fail。

### TC-021：简约布局、焦点与会话开关

优先级：中。类型：UI 测试。前置条件：明暗主题、宽窗口及最小支持窗口，已有测试账号连接。

1. 检查卡片、居中弹窗、右侧详情沿用 Space 的字号、配色和圆角；专家页和原 Skill 入口不变。
2. 用 Tab/Shift+Tab 在弹窗内移动，Esc 关闭/取消后检查焦点返回；校验错误不被底部按钮遮挡。
3. 在只有飞书已连接、企业微信 / 钉钉 / 腾讯会议尚未连接时打开“＋”，无需点击而将鼠标移到“连接器”：右侧同时出现连接器面板，飞书排在最上方并显示本会话开关，其余三个显示“连接”。连接器超过八个时只显示排序后的前八个。
4. 用键盘关闭飞书的本会话开关，再次检查飞书行仍保留且开关为关闭；回插件库检查账号仍为已连接。点击未连接服务的“连接”以及底部“更多”，均进入插件库连接器页，不直接授权、不自动发送。
5. 检查 Partner 输入框只保留一个“＋”入口，不再常驻独立技能 / 连接器图标；打开后依次可见“添加文件或照片、添加文件夹、斜杠命令、连接器、技能、专家”。文件与文件夹沿用既有选择和附件管线；斜杠 / 连接器 / 技能通过 Partner 专用右侧悬停面板展示，技能发现与安装包仍保留，连接器不调用 Coder 的 MCP 发现。
6. 切到 Coder，确认其“＋”菜单没有专家，悬停连接器不展开；点击后仍按原逻辑发现项目 MCP，顺序和返回交互均未改变。

预期：根菜单与右侧面板可同时操作，无截断或横向溢出；已连接服务优先、未连接服务可发现且最多八个；开关只影响本会话，关闭后账号连接不消失；“连接 / 更多”只导航插件页；技能入口、共享 Skill 与独立扩展包均保留。Coder 内容和交互不受 Partner 菜单改动影响。

实际结果：待填写。是否通过：[ ] Pass / [ ] Fail。

### TC-022：本地准备与授权等待不阻塞界面

优先级：中。类型：性能测试。前置条件：受控慢速本地归档 / CLI 夹具，不使用真实账号测试无限输出。

1. 延迟内置归档读取、校验、解包、版本探针和 CLI 响应；在后台等待时验证页面仍响应，尝试取消。
2. 在夹具中触发超时或过量输出，检查任务收束及残留进程。
3. 长时间停留连接器列表，检查没有持续重新授权或高频 auth status 调用。
4. 篡改或移除内置归档，确认任务安全结束并提示更新或重装 Space；不得启动组件网络下载、显示下载进度或回退到 PATH。
5. 本地准备失败时确认没有要求用户去浏览器完成操作；准备 / 网页授权 / 校验 / 失败 / 取消的辅助说明各自对应阶段。授权阶段保留“重新打开授权页”。
6. 在夹具中延迟本地准备和官方 CLI 的应用配置，确认仍能进入账号授权；链接到期时间不得因进度推送无限延长。

预期：IPC 快速返回短任务，本地准备 / 授权有时限，取消清理进程和本次 staging；列表使用本地状态，不循环扫描凭据，正常产品路径不下载 CLI。

实际结果：待填写。是否通过：[ ] Pass / [ ] Fail。

### TC-023：内置来源、凭据与停用安全

优先级：高。类型：安全测试。前置条件：隔离夹具和可中断测试账号；禁止复制真实授权 URL 到报告。

1. 在发布准备夹具中拒绝非官方下载域名 / 重定向和错误 SHA-256；在运行时直接提供错误摘要、符号链接或越界的本地内置归档，确认拒绝且不联网。
2. 检查扩展 frame 只得到目录和 connectedIds；伪造 job owner / 任意 URL / command 被拒绝。
3. 授权未完成时停用/卸载插件或退出应用；恢复后确认旧任务不重放，旧响应不复活连接。
4. 点击解绑并阅读确认，检查共享 CLI 配置、Skill、远端文档均未删除。

预期：凭据/短期授权链接/原始 CLI 输出不进入普通状态、模型或日志；取消、撤权和落盘顺序经过验证；写入仍需范围和逐项审批。

实际结果：待填写。是否通过：[ ] Pass / [ ] Fail。

### TC-024：无全局 Node/npm 与升级兼容

优先级：高。类型：兼容性测试。前置条件：隔离桌面 profile、0.4.0 用户专家 / 旧会话，系统路径没有 Node/npm/lark-cli，可用一次性打包副本验证资源损坏。

1. 更新到包含宿主内置飞书组件的当前 KodaX Space 版本并启用当前 Partner 包，检查旧专家与原会话仍可用。
2. 检查正式 Space 安装包包含当前平台固定校验的 CLI 归档与许可证；点击连接只进行本地静默解包，不依赖 npm shim、不在连接路径下载组件，Partner 插件归档仍拒绝 executable。
3. 模拟内置归档缺失、摘要错误、解包失败和版本不兼容，确认只提示更新或重装 Space，不回退到 PATH、运行时 `latest` 或飞书桌面客户端。
4. 若未来启用独立后台组件更新夹具，在官方页面打开后提供一个已审核新版本：当前任务继续固定旧版本，新版本只完成校验 / 暂存，并从下一任务开始使用；授权过程中不得热切换。
5. 分别测试开发态与打包态，记录实际 OS/架构；不要将 macOS 结果当作 Windows/Linux 已通过。

预期：组件由 Space 管理且不覆盖全局工具，当前任务不热切换，旧专家 / Skill / 会话保持兼容；明确报告无法验证的平台和真实账号环节。

实际结果：待填写。是否通过：[ ] Pass / [ ] Fail。

### 首次连接补充的执行记录（2026-08-31，历史方案）

> 以下记录属于已被宿主内置组件决策取代的显式下载 / 安装方案，仅保留作追溯，不得据此勾选 TC-017、TC-018 或 TC-024 的当前版本。

- 宿主仍为 0.1.45 / SDK 0.7.95；独立包 0.5.0 已在同一隔离 QA profile 中安装并启用。最新 macOS arm64 本地打包及 packaged smoke 通过，启动日志确认 renderer-ready 和窗口显示。
- 当前 Mac 锁屏，Computer Use 无法取得新版画面或操作控件。TC-018–024 的真实桌面、键盘、明暗/窄屏验收仍未执行，不提供虚构截图或勾选整条通过。
- 官方 CLI 在线安装器下载尝试在 180 秒测试截止时退出并清理 staging，慢网络下的在线自动安装未通过。随后完整下载官方归档并核对固定 SHA-256，以文件替代 transport 测试真实校验、提取、私有安装和 `--version`，确认返回 `lark-cli version 1.0.92`、exit 0。仅在独立临时目录执行，未配置/登录账号。
- 后端 59 项受控回归与最终弹窗 8 项组件回归通过，包括重复发起、取消/落盘竞态、缺 CLI 关闭、迟到响应、既有会话加载前零写入和恢复后保留范围/草稿；不能替代真实 Feishu 权限及文档验证。
- Standards 独立发现 0；Spec 发现的 1 项 P2 已修复并独立复验，未解决 0。完整证据及限制见 [首次连接记录](../features/f146-feishu-onboarding.md#5-实施证据)。
- 扫码、创建应用、授予权限及任何远端文档操作均未执行；测试进程使用离线 Mock，未消耗真实模型服务或变更用户账号。

### 首次下载失败修复后的复测（2026-08-31，历史方案）

> 以下在线下载和“安装并继续”相关结果已不再是正常连接路径的验收依据；当前验收以打包内置归档、本地静默准备和单次连接为准。

- TC-018 / TC-022 子步骤：真实官方网络下载、校验、原生私有安装成功（约 10 分 17 秒）；新版 Electron 点击连接自动打开飞书官方配置页。用户自行操作后，宿主飞书连接记录为已连接。未由代理提交网页权限。
- 阶段文案、慢下载 / 无进展 / 总时限 / 取消 / 官方跳转由新增回归覆盖；最终完整套件 3513 通过、4 条件跳过、0 失败。macOS arm64 打包与原生依赖 smoke 通过。
- 仅记录这些实际子步骤，不勾选整条用例通过；未进行远端文档读写、真实模型执行或 Windows / Linux 实机验证。完整说明见首次连接记录第 6 节。

### TC-025：飞书长条卡片与官方品牌展示（0.5.1）

优先级：中。类型：UI / 边界 / 负向 / 性能测试。前置条件：0.5.1 已启用；未连接状态使用独立测试 profile 或组件夹具，不为验证样式解绑真实账号。

1. 打开“插件 → 连接器”，检查左侧官方蓝绿色飞鸟、中间“飞书”及说明、右侧圆形操作按钮为同一横排。
2. 分别观察未连接与已连接状态：未连接为加号，已连接为箭头及名称旁绿色状态。通过辅助功能检查仍能识别“连接 / 管理连接”及连接状态。
3. 用 Tab 和 Enter 打开可信连接弹窗，检查标题和图标一致；关闭后返回原卡片，不发起新的授权或文档请求。
4. 在宽窗口和最小支持窗口查看；用组件夹具检查 360px 与 460px 可用宽度。图标与操作不挤压、文字不横向溢出，说明最多两行。
5. 以明暗主题分别检查焦点和状态；在离线夹具里确认图标可加载、重复打开不请求第三方图片服务器。打开失败时仍显示现有可理解的错误。

预期：长条布局保持 Space 风格，图标清晰、透明且不变形；状态和按钮可访问，专家页不变。图片随包加载，不引入网络依赖。

实际结果：待填写。是否通过：[ ] Pass / [ ] Fail。

### TC-026：品牌更新不改变现有连接（0.5.0 → 0.5.1）

优先级：高。类型：正向 / 安全 / 兼容性测试。前置条件：隔离测试 profile 的 0.5.0 已有连接与会话文档范围，当前没有进行中的读写或授权。

1. 记录当前连接 revision、会话允许的文档与文件夹范围（报告不包含凭据）；直接安装 0.5.1，不先卸载或解绑。
2. 新安装版本默认停用，明确重新启用；打开连接器列表，确认“飞书”仍为已连接。
3. 查看原会话菜单和文档范围，检查原范围与草稿未改变；标题来自新目录，旧历史快照不因更名而改写。
4. 打开并关闭管理弹窗；确认不会仅因查看页面而触发安装、重新授权、读取或写入。

预期：内部扩展 / 连接器 ID 不变，既有账号与会话授权不被清空或扩大；宿主 0.1.45 与 SDK 0.7.95 不升级。未测试平台单独标记，不把 macOS 结果外推。

实际结果：待填写。是否通过：[ ] Pass / [ ] Fail。

### 品牌卡片补充的执行记录（2026-09-01）

- TC-025：macOS arm64 打包 Electron 已核对横向卡片、官方图标、“飞书 / 已连接”、管理弹窗打开 / 关闭以及返回库。明暗对比度、窄 frame、键盘、无额外请求由组件夹具覆盖；不将它们记为全部原生窗口操作均通过。
- TC-026：同一隔离 profile 升级并启用 0.5.1，现有连接记录文件逐字节未变，真实桌面继续显示已连接；未再次授权或断开。当前测试项目没有已保存会话，因此真实会话文档范围步骤未执行，范围 / 草稿仍由原受控回归覆盖。
- 完整自动回归 3516 通过、4 条件跳过，类型检查、lint、生产构建与打包 smoke 通过。未执行真实文档读写、真实模型调用或其他操作系统验收。

## 首批多连接器追加验收（插件库 0.6.0，2026-09-01）

本批只有企业微信、钉钉、腾讯会议的官方授权和指定资源只读；六家其他提供方及新远端写操作尚未实施。主机为 macOS Apple Silicon，Space 0.1.45 / SDK 0.7.95 不变。真实授权由测试者在官方网页自行完成，不把 token、授权挑战、账号或文档内容贴入测试报告。

### TC-027：四品牌卡片与可信弹窗

优先级：中。类型：UI 测试。前置条件：0.6.0 已启用，保留原飞书连接。

1. Partner → 插件 → 连接器，应依次显示飞书、企业微信、钉钉、腾讯会议四张横向卡片；分别打开并关闭后三家弹窗。
2. 检查图标、名称及各自说明一致：机器人文档、企业 CLI Access、6–15 位腾讯会议号。检查无无关飞书 profile 设置和写入选项。
3. 在明暗主题、最小窗口和键盘 Tab/Enter 下检查；关闭弹窗后回到原卡片。仅看卡片不下载组件、不打开授权网页。

预期：图标离线可见、长条样式与 Space 一致、文本与焦点可读，无占位连接器。实际结果：待填写；[ ] Pass / [ ] Fail。

### TC-028：安装、官方授权与指定读取

优先级：高。类型：正向测试。前置条件：测试者拥有对应企业资格/机器人或腾讯会议账号，有权访问一个非敏感测试资源，允许下载固定官方 CLI。

1. 点击“+ / 连接”，确认私有组件安装提示，再由测试者明确点击“安装并继续”。必须跳到该服务官方网页，由测试者完成授权，不手工粘贴 token。
2. 观察验证后才显示已连接，点击“去试试”；原输入框草稿不被覆盖，空资源范围不得读全部资料。
3. 在会话连接器菜单打开“资源范围”，明确加入一个引用并保存：企业微信为无 query 的 `https://doc.weixin.qq.com/doc/<id>` 或 `wecom://document/<id>`；钉钉为无 query 的 `https://alidocs.dingtalk.com/i/nodes/<id>` 或 `dingtalk://document/<id>`（ALIDOC/adoc）；腾讯会议为 `tmeet://meeting-code/<6–15 位会议号>`。
4. 点击读取指定资源，检查“资料”卡片的快照标题、来源、内容和读取时间。腾讯会议仅返回基本信息，不显示录制内容、纪要、密码或参会人员名单。

预期：完成在线身份验证、所选资源真实读取且不发生远端写入；官方拒绝权限则明确失败。三家分开填写，不因一家通过而勾选其他两家。实际结果：待填写；[ ] Pass / [ ] Fail。

### TC-029：失败、取消与未授予权限

优先级：高。类型：负向测试。前置条件：隔离账号和测试资源，不撤销生产账号。

1. 在安装/授权中取消；网页晚完成时也不得把取消的任务选入会话。
2. 拒绝官方授权，或使用无权查看的资源；钉钉无 CLI Access/PAT 时不得自行批量扩权或跟随非白名单链接。
3. 模拟离线、组件损坏、返回格式改变；重试只能再次验证，不能猜测账号或显示假成功。

预期：固定安全错误，保留可关闭/重试入口，不泄露 CLI 原始 stdout/stderr。实际结果：待填写；[ ] Pass / [ ] Fail。

### TC-030：范围与大小边界

优先级：高。类型：边界测试。前置条件：夹具或获准的测试资源。

1. 空范围、错服务 URL、带 query/fragment、越界路径、超长 ID 应不能保存为有效授权。
2. 读取正文超过 128 KiB UTF-8 或标题超过 280 字符的资料；不得保存无提示的截断快照或暴露原始校验异常。
3. 同时验证中文多字节正文，不能只按字符数判断大小。会议记录不得把九位会议号自动当作已解析的实际 meeting ID。

预期：明确范围/大小错误，原资料卡片不被失败结果覆盖。实际结果：待填写；[ ] Pass / [ ] Fail。

### TC-031：慢网、持续任务与取消响应

优先级：中。类型：性能测试。前置条件：隔离网络/受控慢响应夹具。

1. 放慢组件下载；持续有数据时不会误报短超时，超过受控总时限必须结束。
2. 保持授权网页不确认至到期，再试取消、关闭和重新发起；UI 不永久 loading。
3. 停留连接器卡片页面，不得高频执行身份探针或反复打开浏览器。

预期：正常可交互、限时退出、无重复连接进程。实际结果：待填写；[ ] Pass / [ ] Fail。

### TC-032：关闭开关、跨账号与篡改组件

优先级：高。类型：安全测试。前置条件：仅使用隔离 fixture/测试账号，篡改测试不得针对真实安装。

1. 对照组合测试验证：异步读取前关闭本会话开关/移除资料/停用扩展，应在真实业务 spawn 前拒绝；已发出的请求最多完成网络操作，不能再向当前会话交付结果。
2. 不同账号、不同提供方和 Coder 均不能借用该范围；混合飞书会话也不能让新只读连接器发起飞书写入提案。
3. 用夹具替换固定组件为伪报版本的文件，必须在执行任何命令前拒绝；恶意 tar 目录、link、重复条目、摘要错误也必须拒绝。

预期：凭据/挑战只留可信宿主，插件 frame、模型、日志不接收它们；连接状态不会扩大范围。实际结果：待填写；[ ] Pass / [ ] Fail。

### TC-033：0.5.1 升级兼容及平台边界

优先级：高。类型：兼容性测试。前置条件：原飞书连接保留，没有在途读写或授权。

1. 更新独立包到 0.6.0 后启用，不先卸载或解绑飞书。确认飞书仍显示已连接，原会话范围与草稿未清空/扩大，共享 Skill 和专家不变。
2. 新三家只读连接不可显示文件夹新建、追加或全局写策略；Feishu 原有受审操作仍单独保留。
3. 不支持的平台明确提示不可用；本批 macOS arm64 验收不能作为 Windows/Linux/Intel 支持证明。

预期：原账号记录兼容，升级不重新授权，Space/SDK 版本不改变。实际结果：待填写；[ ] Pass / [ ] Fail。

### 首批验证记录

- 全量回归 3591 通过、4 条件跳过、0 失败；TypeScript、lint、renderer/main、0.6.0 独立归档与 macOS arm64 打包 smoke 通过。
- 真实 Electron 在原隔离 profile 显示 Space 0.1.45、插件 0.6.0、四张横向官方图标卡片、飞书已连接、原 10 个专家。升级前后飞书连接记录与用户专家文件摘要不变。
- TC-027 卡片部分、TC-033 升级状态部分已观察通过，其余检查不因此自动勾选。新弹窗布局/按钮与会话范围的自动测试通过；真实窗口点击“连接”被 Computer Use 风险规则阻止，未换手段绕过，因此三个新弹窗的实际点击及官方授权仍待用户继续。
- 真实官方账号授权、指定资源读取、企业资格、线上返回 schema 及其他操作系统未代用户验收，TC-028 等相应用例保持待填写。当前启动沿用测试 profile 的 `KODAX_FORCE_MOCK=1`，卡片与宿主连接器是真实实现，但不代表真实模型回答验证。
- [双轴评审与修复记录](../features/F146-multi-connectors-review.md)。未推送、发布或改动共享 Skill / SDK。

## 首个正式平台专家追加验收（插件库 0.8.0）

本批新增“飞书办公套件专家”。它由持续专家 prompt、内置 `feishu-office-suite` Skill、两级能力引导和既有飞书连接器组成；只有“新建多维表格”增加直接远端创建，文档新建 / 追加仍沿用待审核链。人工验收只能使用有权限的非敏感测试目录，不在报告中记录账号、token、CLI 原始输出或授权链接。

### TC-034：平台专家绑定与两级能力引导

优先级：高。类型：正向 / 交互 / 持久化测试。前置条件：0.8.0 已安装并启用，打开 Partner 会话。

1. 在“插件 → 专家 → 平台专家”找到“飞书办公套件专家”，引用后返回原主对话；专家标签应持续位于输入区，详情应显示正式 Skill，而不是只显示一段人设文案。
2. 输入框上方先显示“飞书文档 / 多维表格”，进入“多维表格”后显示“新建多维表格”，并可返回上一级。
3. 先输入一段已有草稿，再点击“新建多维表格”；模板只能插入光标位置且仍可编辑，不得自动发送、连接账号、读取资料或创建资源。
4. 发送一轮普通问题、切换项目 / 会话并返回、重启应用，核对专家只在原会话持续生效；移除后引导消失，其他会话不继承。

预期：专家在主对话中持续工作，两级入口只是引导；专家、Skill 与会话绑定隔离，点击本身零副作用。实际结果：待填写；[ ] Pass / [ ] Fail。

### TC-035：飞书账号、Base 权限与会话目录范围

优先级：高。类型：授权 / 负向 / 兼容性测试。前置条件：可使用隔离飞书测试账号；保留一条升级前连接用于兼容验证。

1. 旧连接未包含 Base scopes 时，目录输入应禁用并明确要求重新连接；仅引用专家不能获得 Base 工具。
2. 由用户在官方页面完成重新授权后，在“文档与多维表格范围”选择一个测试文件夹并保存；切换会话后范围不得跟随。
3. 空目录、其他连接的目录、带 query / fragment、非 `drive/folder` URL、已撤销连接、停用插件和 Plan 模式均应在业务命令 dispatch 前拒绝。
4. 保存范围只更新当前会话绑定，不发送输入框草稿，不删除或扩大原文档范围；旧记录应兼容为 `createBase=false` 和空 Base 任务列表。

预期：专家和 Skill 不替代账号身份、OAuth scope 或精确目录范围；权限不足时提供可恢复路径而不伪装成功。实际结果：待填写；[ ] Pass / [ ] Fail。

### TC-036：创建真实飞书多维表格

优先级：高。类型：真实服务正向测试。前置条件：TC-035 已通过；测试目录为空或其内容可核对；允许创建一个可删除的测试 Base。

1. 发送完整请求，明确 Base 名称、首表名称以及 1–20 个受支持字段（首字段为文本；其余可用文本、数字、静态选项、日期时间、复选框）。信息完整时不应再要求一次形式化确认。
2. 执行期间右侧自动打开一个以 Base 名称命名的结构化任务页，依次显示准备 / 提交状态；同一任务的状态更新不得重复开标签、抢焦点或复活用户已关闭的标签。
3. 成功后同一标签页切换到宿主验证的真实飞书 URL；核对 Base 名称、首表、字段以及空默认表已由官方组合命令处理。若飞书禁止嵌入或登录态不可用，使用保留的“系统浏览器打开”入口核对。
4. 确认任务只提交一次；不得出现 shell 拼接、模型伪造 URL、通用删除命令或第二次自动重试。

预期：新 Base 仅创建于授权目录，结构与请求一致，成功状态和链接来自严格宿主回执。实际结果：待填写；[ ] Pass / [ ] Fail。

### TC-037：失败、未知结果、历史记录与并发状态

优先级：高。类型：失败恢复 / 竞态 / 安全测试。前置条件：受控 CLI 夹具；真实账号仅用于获准的核对步骤。

1. 分别触发创建前校验失败、spawn 后超时、partial、unknown 和畸形回执；只有完整成功回执可显示资源页，其他状态留在结构化任务页并提示到飞书核对。
2. 对 unknown / partial 点击刷新、切换会话或重启；宿主不得自动重试创建。恢复后历史成功任务也不得自动打开右侧栏。
3. 连续发送普通连接器刷新、同任务 preparing / submitting / final 事件、两个不同任务事件，以及“事件先到、记录后到”；每个真实新任务最多自动打开一次，事件不得丢失。
4. 任务进行时移除会话范围或停用插件；dispatch 前必须拒绝，已 dispatch 的结果只能结算为可证明状态或 unknown，不得宣称撤回远端请求。

预期：失败不转成功、未知不重试、历史不重放，实时状态与任务 ID 保持同一宿主因果链。实际结果：待填写；[ ] Pass / [ ] Fail。

### TC-038：右侧隔离、窄窗布局与原能力回归

优先级：中。类型：UI / 安全 / 回归测试。前置条件：成功回执夹具以及 600、900、1440px 窗口尺寸。

1. 检查飞书资源 iframe 仍为 `allow-forms allow-scripts`，没有 `allow-same-origin`、Node、IPC、顶层导航或 referrer 泄露；保留系统浏览器入口，不承诺所有飞书页面都能嵌入编辑。
2. 600px 时先隐藏左栏并均衡主对话 / 详情；900px 时主对话至少 420px；1440px 时可保留左栏且自定义右栏不侵占 420px 主区。
3. 重新验证飞书文档读取、文档新建 / 追加待审核、Coder 工具、其他只读连接器和普通提示词专家；它们不得获得 Base 直写或被新专家改写语义。

预期：右侧外页按既有安全基线隔离，窄窗仍可对话，新增 Base 能力不回归文档审核链和其他 Surface。实际结果：待填写；[ ] Pass / [ ] Fail。

### P7 自动验证记录（2026-09-01）

- `NODE_OPTIONS=--no-experimental-webstorage npm test` 整体退出 0：release 54/54、IPC schema 338/338，desktop 全套 0 失败；A → B → A 并发任务信号、历史任务不重放、两级引导只插草稿、Base 严格回执与热撤权均包含在自动回归中。
- `npm run typecheck`、`npm run lint`、Prettier、`npm run build:smoke`、内置 Skill 快照检查与双轴复审通过，当前开放 P0–P3 为 0。
- 上述结果不自动勾选 TC-034–TC-038。真实账号授权、官方页面登录 / iframe 表现和 TC-036 的远端 Base 创建仍须用户在测试目录完成；本轮没有替用户连接、写入或删除任何飞书资源，也没有发布应用。

## P8 Partner 通用原生资源直达交付验收

P8 只改变 Partner 的通用任务、通知、右侧打开和网页会话。测试使用受控 provider fixture 交付已验证的 HTTPS URL；不要求也不验收飞书、钉钉、腾讯文档的真实创建、连接器配置或专家 prompt。下列 TC-039–TC-045 是此前飞书首适配草案，保留为后续 provider adapter 的测试素材，不阻塞当前通用能力完成。

### TC-039：飞书文档直接创建、登录与展示

优先级：高。类型：正向测试。前置条件：0.9.0 已启用；Partner 会话仅选择一个具备 create/read 权限的飞书测试账号；允许创建一份可删除测试文档。

1. 退出右侧网页中的飞书登录，但保留连接器授权；发送“帮我创建一份飞书文档，标题为 P8 测试，正文为今天的测试记录”。
2. 检查过程不出现本地文件、成果中转或待审核新建卡；信息完整时不再要求确认或文件夹链接。
3. 等待聊天返回真实标题/链接和右侧标签；若右侧出现飞书官方登录页，由测试者完成网页登录。
4. 登录后在同一右侧标签核对文档标题和正文；在飞书个人空间确认只有一份对应文档。

预期效果：

- [ ] 连接器创建和右侧网页登录相互独立；官方登录后原文档在同一右侧页面展示。
- [ ] 成功只来自创建回执与同账号回读，链接、标题、正文一致，且没有待审核/本地交付步骤。

实际结果：待填写。是否通过：[ ] Pass / [ ] Fail。

### TC-040：缺少连接、权限、多账号和未知结果

优先级：高。类型：负向测试。前置条件：准备未连接、缺 create/read scope、两个已选账号及可注入 unknown/partial 的受控环境。

1. 分别在未选账号、账号断开、权限缺失、扩展停用和 Plan 模式下发送同一新建请求。
2. 同时选择两个可创建账号，不做宿主账号选择；再只保留一个账号重试。
3. 注入派发后超时、回读不一致和不完整回执；刷新、切换会话并重启。

预期效果：

- [ ] 前述不可用状态在派发前停止并给出连接、重授权、选择账号、启用扩展或退出 Plan 的准确动作，不静默改成本地文档。
- [ ] unknown/partial 不声称成功、不打开 URL、不自动重试；多个账号时模型不能按列表或最近使用自行选择。

实际结果：待填写。是否通过：[ ] Pass / [ ] Fail。

### TC-041：目录、文本和重复调用边界

优先级：高。类型：边界测试。前置条件：账号同时具有个人空间和一个测试文件夹授权；准备中文、Unicode 组合字符、CRLF 与接近 128 KiB 的正文。

1. 不指定目录创建一次，再显式指定已授权目录创建一次；检查各自实际位置。
2. 输入格式合法但不在本轮授权中的文件夹 URL，再测试带 query/fragment、超长标题、空正文及超限正文。
3. 在同一 admitted turn 重放完全相同的工具调用，再以新用户轮次提交相同内容。

预期效果：

- [ ] 省略目录只进入个人空间；显式目录必须精确匹配当前 scope，非法或越权目录 0 dispatch 且不回退个人空间。
- [ ] 文本按 NFC/换行规则稳定回读；同轮相同调用只创建一次，新轮次才允许明确再创建。

实际结果：待填写。是否通过：[ ] Pass / [ ] Fail。

### TC-042：右侧原生网页体验与平台通用性

优先级：高。类型：UI 测试。前置条件：飞书真实成功链接；钉钉与腾讯文档使用经 schema 验证的成功任务夹具；明暗主题和 600/900/1440px 窗口。

1. 检查成功任务自动打开一个以真实标题命名的右侧网页标签；手动关闭后，历史 hydration 不得重新打开。
2. 在右栏完成登录、站内跳转、后退、前进、刷新及“系统浏览器打开”；检查加载态和地址栏同步。
3. 用钉钉、腾讯文档成功任务夹具触发同一 opener；检查资源键、标签和容器不含飞书专用分支。
4. 在三种窗口宽度及明暗主题下检查右栏、主对话和标签可读性。

预期效果：

- [ ] 平台真实网页在右侧受控容器中显示，官方登录和站内跳转不再受 iframe X-Frame-Options/CSP 限制。
- [ ] 三个平台走同一 task/signal/opener/web container，历史和重复信号不会抢焦点或开重复标签。

实际结果：待填写。是否通过：[ ] Pass / [ ] Fail。

### TC-043：慢网、大正文与交互响应

优先级：中。类型：性能测试。前置条件：受控慢网或延迟夹具；合法的大正文；可观察任务记录与 UI loading。

1. 分别延迟创建回执、私有回读和右侧页面加载，期间继续滚动对话、切换标签和输入草稿。
2. 让创建在提交前中断和提交后中断各一次；重启应用观察恢复时间与状态。
3. 连续产生多个不同任务 signal，检查按 revision 排空而不丢失后续任务。

预期效果：

- [ ] UI 不冻结、不永久 loading；任务状态最终收敛，提交后不确定状态不会因超时触发第二次创建。
- [ ] signal 有序处理，每份已验证的新资源最多自动打开一次。

实际结果：待填写。是否通过：[ ] Pass / [ ] Fail。

### TC-044：网页隔离、授权边界与 Coder 不变

优先级：高。类型：安全测试。前置条件：隔离 profile；可使用测试页面尝试 file/javascript URL、下载、权限请求和 window.open；打开一个 Coder 会话作对照。

1. 检查右侧 guest 使用专属持久化 partition，且 nodeIntegration/preload 关闭、contextIsolation/sandbox/webSecurity 打开。
2. 从地址栏或页面尝试 `file:`、`javascript:`、带账号密码 URL、下载、摄像头/通知权限及非 HTTP(S) 弹窗导航。
3. 在创建门前撤销会话范围、断开账号或改变 connection revision；确认远端 0 dispatch。
4. 在 Coder 中发送相同自然语言请求，检查不存在 Partner connector 直创工具、自动右侧平台页或 Partner 系统提示词行为。

预期效果：

- [ ] 仅安全 HTTP(S) 导航进入隔离网页会话；下载和站点权限被拒绝，网页不能访问 Node、preload 或宿主 IPC。
- [ ] 连接器 OAuth、右侧网页登录和会话 scope 三者不互相冒充；Coder 的工具、运行逻辑和界面保持原样。

实际结果：待填写。是否通过：[ ] Pass / [ ] Fail。

### TC-045：0.8.1/records v1 升级和旧审核记录兼容

优先级：高。类型：兼容性测试。前置条件：备份含 0.8.1 扩展、records v1、旧 pending create/append proposal、Base task 和会话绑定的隔离 profile。

1. 原位升级宿主 v0.1.61-p.1 与插件 0.9.0；启用后检查原账号、会话范围、专家快照和历史记录。
2. 结算升级前已经存在的 create/append proposal；再从新 Partner run 请求新建和追加文档。
3. 禁用 0.9.0 或换回不声明 capability 的 0.8.1，检查新直创停止但历史资源仍可查看。

预期效果：

- [ ] records 原子迁移到 v2 且原记录不丢失；旧 create 可继续结算，新 run 的新建走直创、append 仍走审核。
- [ ] capability 按 manifest 握手，不按版本字符串猜测；共享 `feishu-office-suite` Skill 内容与 SDK 0.7.95 不变。

实际结果：待填写。是否通过：[ ] Pass / [ ] Fail。

### P8 通用能力自动验证记录（2026-09-02）

- 定向 schema/main/renderer/service 测试 89 项与独立插件构建测试 3 项通过；覆盖 provider-neutral URL、capability 握手、直创私有回读、0/1/多账号、目录防越权、幂等、崩溃恢复、跨来源资源键和隔离网页 attachment policy。
- 完整 `npm test` 共 3768 项通过、0 失败、4 项按平台条件跳过；`npm run typecheck`、`npm run lint`、`npm run build:smoke`、`git diff --check` 和双轴 review 通过。全库 `format:check` 报告 325 个既有基线文件，本次触及文件的定向 Prettier 检查通过。
- provider-neutral 公共 seam 已增加任意未来 provider fixture，证明新增平台无需修改 Partner 中央 provider 枚举或 URL 白名单；HTTP、带凭据 URL与本地协议继续拒绝。TC-039–TC-045 转为后续平台适配测试，不是本轮通用能力的未完成项。
