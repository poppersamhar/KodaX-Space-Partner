# F146 — Partner 插件库开发计划

> 状态：InProgress；P1–P7 已进入实现 / 验收，用户于 2026-09-02 确认追加 P8“Partner 平台原生文档直达交付（飞书首适配）”。
> 创建：2026-08-31；更新：2026-09-02
> 所属：KodaX-Space-Partner fork；规划版本：v0.1.61，沿用已有未发布功能规划位置。
> 当前开发基线：Space 0.1.45 / KodaX 0.7.95 / Partner library 0.8.1；P8 当前集成构建为 fork-only `v0.1.61-p.1` + Partner library 0.9.1，同步提供通用 `partnerNativeDocumentDeliveryV1`（飞书首适配），不升级 SDK，也不提前发布 canonical v0.1.61。
> 登记：[FEATURE_LIST](../FEATURE_LIST.md#fork-feature-details)；版本设计：[FEATURE_146](v0.1.61.md#feature_146-partner-plugin-library-extension)。

## 1. 需求概述

### 已确认的产品边界

- Partner 左侧“新对话 / 文件”下面增加“插件”，库内只有“专家”和“连接器”两个分类。
- 专家是角色名称、用途说明、场景提示词和创建者可选配置的 Skill 的产品化组合，不是新的 Agent 执行引擎。
- Skill 仍是 Coder / Partner 共用的能力。保留现有技能库、入口、发现、调用和权限逻辑；不增加技能分类、安装器、自动匹配、推荐、替代或“Skill 引用解析”宿主服务。
- 专家没有配置 Skill 时，只增加专家提示词，不额外处理 Skill；这不关闭 Space 原有的独立 Skill 能力。
- 一段 Partner 会话最多一个专家，可选择多个连接器。专家对后续每轮对话生效，不是只给第一条草稿填入模板。
- 连接器通过每个平台实际支持的官方 CLI、MCP 或宿主适配接入第三方系统；账号连接、当前会话选择、资源范围和写入授权是不同状态，不能相互冒充。
- 输入框展示当前专家和连接器。资料、待审核、成果和本地 Artifact 只在各自语义适用时出现；低风险远端新建以聊天中的真实链接和右侧原生网页为主，不强迫用户经过这些中间栏目。
- 插件库整体必须位于可独立安装、禁用和卸载的 Space Extension 包，不能仅成为带开关的内置页面。

### 本轮计划采用的最小范围

- 第一版允许专家配置零或一个明确的现有 Skill。当前显式调用会拒绝同条消息中的多个 Skill；不为本功能改写该共享规则。多 Skill 组合不作为本期已解决能力。
- 第一包由本项目提供，携带前端资源和专家 / 连接器声明；不开放插件市场、任意主进程代码、安装脚本或 SDK filesystem hooks。
- 先贯通一个纯提示词专家，再迁移现有八个场景预设、添加专家编辑和可选 Skill 配置，随后接入连接器读写。
- 远端第一期支持能力已确认的文档读取、低风险新建和受审修改。新建个人空间文档或在本轮已授权目录新建文档可以直接执行；修改既有文档仍需差异审核。删除文档、覆盖、改变共享权限、群发和后台自动同步不在本期范围。
- 用户明确指定远端平台时不得静默生成本地文件或 Artifact；连接器未启用、未授权或失效时应给出准确的启用 / 授权恢复动作，并保持用户指定的交付目标。
- 不改 Coder 布局、运行时所有权、共享 Skill 机制、模型选择和现有权限底线。

## 2. 影响范围

### 当前代码证据与缺口

| 位置                                                                                                                                                                                                                  | 已有能力                                                        | 本功能需要补齐                                                            |
| --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------- | ------------------------------------------------------------------------- |
| [partnerSceneTemplates.ts](../../apps/desktop/renderer/src/features/partner/partnerSceneTemplates.ts) / [BottomBar.tsx](../../apps/desktop/renderer/src/shell/BottomBar.tsx)                                          | 八个场景预设只生成可编辑草稿                                    | 将角色配置迁入 Extension，原文本保留为示例任务；不覆盖用户已编辑草稿      |
| [partner-profile.ts](../../apps/desktop/electron/kodax/partner-profile.ts) / [real-session.ts](../../apps/desktop/electron/kodax/real-session.ts)                                                                     | 每轮创建固定 Partner profile；已有单项显式 Skill 调用和安全检查 | 叠加已绑定专家，复用既有执行准备，不改变共享技能语义                      |
| [session.ts](../../packages/space-ipc-schema/src/channels/session.ts) / [session-runtime-store.ts](../../apps/desktop/electron/kodax/session-runtime-store.ts) / [host.ts](../../apps/desktop/electron/kodax/host.ts) | 会话创建、原子 sidecar 存储、恢复和列表                         | 专家版本 / 快照及连接器选择的会话绑定；覆盖创建、恢复、切换、fork 和删除  |
| [LeftSidebar.tsx](../../apps/desktop/renderer/src/shell/LeftSidebar.tsx) / [Shell.tsx](../../apps/desktop/renderer/src/shell/Shell.tsx)                                                                               | 固定导航、布局和会话工作区                                      | 按扩展归属挂载入口和库页面，保留原对话 / 草稿                             |
| [SettingsModal.tsx](../../apps/desktop/renderer/src/features/settings/SettingsModal.tsx) / [McpPanel.tsx](../../apps/desktop/renderer/src/shell/popouts/McpPanel.tsx)                                                 | 设置页；MCP 面板能安装 MCPB 服务包                              | 新增真正的 Space Extension 管理入口；MCPB 不能冒充 UI 扩展安装器          |
| [sdk-extensions.ts](../../apps/desktop/electron/kodax/sdk-extensions.ts) / [partner-tools.ts](../../apps/desktop/electron/kodax/partner-tools.ts)                                                                     | 项目级 MCP 装载、Partner 工具过滤                               | 按当前会话选择和授权限制连接器调用，不能只过滤前端标签                    |
| [PartnerContextRail.tsx](../../apps/desktop/renderer/src/features/partner/PartnerContextRail.tsx) / [partner-file-proposal-store.ts](../../apps/desktop/electron/kodax/partner-file-proposal-store.ts)                | 三张卡片；待审核写入本地项目文件                                | 新增远端资料、远端修改提案和回执类型，复用展示外壳，不把 URL 当成本地路径 |
| [vite.config.ts](../../apps/desktop/vite.config.ts) / [build-main.mjs](../../scripts/build-main.mjs) / [electron-builder.yml](../../electron-builder.yml)                                                             | 静态 renderer / main / preload 构建                             | 独立扩展 UI 产物、归档、安装加载和无扩展包启动验证                        |

### 预计新增区域

- Extension 独立源码 / 构建产物目录，包含 manifest、ui、experts、connectors；具体目录名在 spec 固定。
- Space 最小扩展注册表、归档校验、隔离视图加载、受限消息桥和 typed IPC。
- Partner 专家会话配置适配及连接器选择 / 远端提案适配。
- 与源文件相邻的单测、Electron 集成 / E2E 测试及独立扩展归档夹具。

本轮不移动或改写现有 Skill 文件、注册目录和调用实现，不触碰当前未提交的项目切换及图标修改。

## 3. 技术方案

### A. 可独立投放的包与最小宿主

- 包内包含版本化 manifest、预编译 UI 静态资源、专家提示词、连接器声明和普通资源，不含 main / preload 入口、安装脚本或 Skill 安装内容。
- UI 在隔离的浏览器 frame 内运行，通过有限、版本化的消息桥请求宿主；不与可信主 renderer 同源加载任意模块，不获得 Node、完整 IPC、账号 token 或任意文件权限。
- 扩展资源限制在经验证的包根目录，校验大小、入口、路径穿越、符号链接、资源完整性和兼容版本。完整性 hash 不能被描述为发布者身份认证。
- 安装只校验并登记，不自动连接第三方服务。启用后才挂载导航 / 页面 / 声明；所有注册和请求都带扩展归属并由主进程校验。
- 新增宿主自有“设置 → 扩展”管理入口。即使插件库卸载，该入口仍能重新安装包。
- 独立前端加载只是 UI 宿主，不新增 Agent 执行引擎，不开启 SDK 任意代码扩展总开关。
- 声明可组合已支持的连接器工具；新原生适配能力不能仅靠 manifest 伪装成已经支持，需另行增加受审适配或使用实际具备该能力的 MCP 服务。

### B. 专家、草稿与会话

- 选择专家先绑定当前 Partner 草稿，不覆盖草稿、不自动发送、不隐式新开或清空已有会话。
- 首次发送时，主进程按已启用包中的专家 ID / 版本解析配置并持久化；前端不直接提交权威系统提示词。
- 每轮执行读取会话配置，组合“Partner 基础 profile + 专家配置”，保留基础验证与权限。读取历史、重启和上下文压缩后仍能恢复该绑定。
- 专家更改 / 移除在下一轮生效并可见；运行中的一轮固定其配置。新建其他会话不自动继承；显式 fork 按原会话继承配置。
- “下一轮”按实际开始执行划分：after-turn 排队消息在新一轮开始时读取已成功保存的专家；仍由 SDK 在当前轮消化的 interrupt 消息保持当前角色。保存尚未完成时不能提前让新配置生效。
- 修改专家定义产生新版本，不悄悄修改旧会话角色。旧会话缺少扩展或专家时显示不可用状态，不静默使用已经卸载的提示词。
- 专家编辑只管理名称、说明、提示词、示例任务及可选的一个明确 Skill 配置。预置专家编辑为用户副本，不修改共享 Skill 或已安装包的只读资源。
- 配置 Skill 后沿用既有调用准备和拒绝规则；缺失、项目不可用或权限不支持时明确提示，不搜索替代、不自动安装。用户仍可独立使用原 Skill 入口。
- 现有 SDK 的动态上下文在 Partner 中禁止执行命令；它可能将正文替换成“host 禁用”占位后继续模型回合。本插件保留此语义，不新增整轮拒绝规则，也不能把占位当成动态命令已执行。hook/fork 等继续各自现有拒绝路径。
- 专家默认配置与用户本轮显式 Skill 选择冲突时，以用户显式选择为准，该轮不叠加第二个 Skill；下一轮恢复专家默认配置。缺失 Skill 的降级需用户明确选择仅按角色继续，不能显示为技能已成功执行。

### C. 连接器与远端文档

- 分开保存连接器声明、账号连接状态、会话绑定和文档范围。账号已连接不会给其他会话授权全部知识库。
- 从当前会话发起连接，完成账号授权和范围确认后添加输入框标签。移除标签仅解绑本会话；断开账号和卸载扩展是独立操作。
- Partner 输入框“＋”菜单的连接器悬停面板从已启用插件读取连接器声明，稳定地将已连接服务排在未连接服务之前，最多展示八个。已连接服务提供本会话开关；未连接服务的“连接”和底部“更多”进入插件库连接器页。关闭本会话开关不删除账号连接，也不等于全局断开。
- 凭据使用宿主保管的引用，不能进入插件 UI、提示词、普通配置、日志和成果；SDK 原有全局 / 项目 MCP 配置不能被插件安装隐式覆盖。
- 模型只能看见本会话允许的连接器工具；实际调用也必须校验同一权限，不以提示词、UI 状态或 MCP 工具自报只读作为授权。
- 读取的文档带来源、明确文档身份、获取时间和可用版本信息，显示在“资料”；文档内容是证据，不是角色指令或授权。
- 远端写入按风险路由：创建全新飞书文档可在完整请求下直接执行；追加或修改既有文档继续按“生成方案 → 待审核 → 用户确认 → 后端提交”执行；删除、覆盖、权限与批量操作不因提示词而获得能力。只有后端结构化回执确认成功才能显示已写入。
- 远端提案绑定账号 / 会话 / 扩展、目标文档、操作、具体内容 hash 和可用基础版本。P8 后它仅用于既有资源的追加 / 修改；历史新建提案只为存储迁移和审计保留，不再展示、自动打开或提交。确认只覆盖追加提案，执行时重验授权、包状态与目标，不允许模型代替用户确认。
- 直接创建任务必须在提交前持久化并绑定账号、会话、扩展、连接 revision、目标目录、规范化标题 / 正文和输入 hash；提交前后重验授权。`unknown` / `partial` 不宣称成功、不自动重试，避免重复文档。
- 重复提交、权限撤销、版本冲突、超时及结果不确定有明确状态；能力不支持时不承诺版本锁、自动回滚或盲目重试。
- 账号断开后立即禁止新的远端调用。已保存引用和结果只按现有访问 / 留存策略展示，不因界面开关隐式删除或重新授权。

### 与既有规划的关系

这是当前 fork 的明确新增 F146，不将 F096 的未来连接器规划或 F102 的通用 slot registry 标记为已经实现。F146 复用 F096 关于账号、范围、来源、撤销的约束；插件入口和受审远端写入采用本次用户确认的新产品边界。只补本库实际需要的挂载点，不重启完整通用插件平台规划。F137 / F139 的既有范围和实现状态不变。

## 4. 接口契约

以下是已确认的五组职责；各阶段通过契约测试逐步交付，不将后续未实现能力当成已有 API：

| 接口组         | 最小职责和边界                                                                                                              |
| -------------- | --------------------------------------------------------------------------------------------------------------------------- |
| 扩展生命周期   | 安装、列出、启用、禁用、卸载；manifest / 宿主协议兼容；所有能力按 extensionId 归属                                          |
| UI 挂载        | Partner 左侧入口、插件库视图、输入框标签和现有右侧详情；固定消息类型，不提供任意 invoke 或任意系统路径                      |
| 会话配置       | 获取 / 更新 / 订阅专家和连接器绑定；草稿转会话、配置版本、每轮快照、恢复 / fork / 删除；Coder 不接收 Partner 绑定           |
| 连接与调用     | 授权状态、凭据引用、连接 / 断开、会话选择和工具范围；本轮飞书使用官方 CLI 适配，既有 MCP transport 不变，主进程执行权限检查 |
| 文档执行与审核 | 资料引用、低风险新建任务、远端修改提案、人工批准 / 拒绝、执行与回执；直创成功打开真实网页，区分本地文件与远端资源           |

最低数据约束：

- ExpertDefinition：稳定 ID、extensionId、revision、名称、说明、prompt、可选 skillRef、示例任务，以及仅用于目录展示的 expertType / category / listingType。平台专家还可声明只读 capabilityGuide，以两级分组展示任务入口；每个入口只引用同包连接器并生成可编辑 prompt 草稿，不自动发送、不授予工具或写入权限。没有 skillRef 时不增加专家专用 Skill 操作；目录分类和引导均不增加权限或编排能力。
- SessionBinding：sessionId / surface / project identity、revision、可选 expertRef、显式 connectorBindings；不包含密钥；后端按实际账号授权和包状态收窄能力。
- RemoteProposal：operationId、extensionId、sessionId、connectionRef、resourceRef、append、不可变内容摘要 / hash、可用 baseVersion、审核与执行状态；兼容读取已有 create 提案。本增量不新增任意 update / overwrite 操作。
- PartnerNativeDocumentTask：稳定 taskId、provider / adapter、session / extension / connector / connection 归属、connectionRevision、提供方自有的可选授权目标、规范化 title / content、inputHash / scopeHash、`preparing | submitting | succeeded | failed | unknown | partial` 状态，以及仅在成功时出现且相互匹配的 resourceId、真实 title 和 canonicalUrl。飞书、钉钉、腾讯文档适配器共享任务、状态、通知和右侧打开契约；各提供方的创建参数、授权范围与回读校验仍由独立受控适配器负责。模型输入不接受任意 CLI 参数、凭据或未经授权的目标。
- Bridge 请求必须校验实际 frame / 包 / 会话归属、允许操作、有效长度和当前权限；不能信任消息正文自报的身份。返回值和日志统一脱敏。

## 5. 实现步骤

用户已确认按以下顺序开发。P1–P6 同时作为实现票号，后续票依赖前一票的接口；同一票的独立前后端适配可并行，验收不以界面文字替代实际运行。正式 seam 与本地验证记录见下。

| 阶段                     | 可交付闭环                                                                                                          | 进入下一阶段前的检查                                                                           |
| ------------------------ | ------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------- |
| P1：真实扩展包           | 单独构建一个最小包；设置中安装、启用后出现“插件”及真实扩展视图；禁用 / 卸载后撤下，宿主可继续使用                   | 无包时开发 / 打包 / 启动正常；不向扩展暴露主 frame IPC；停用阻止新请求；卸载后可重新安装       |
| P2：第一个专家           | 包内一个纯提示词专家；选择后输入框显示；连续多轮按该角色执行；重启恢复                                              | 检查实际传给 SDK 的 profile，而非只看 UI；两会话不串角色；原草稿、默认 Partner 和 Coder 正常   |
| P3：专家产品化           | 八个预设转为角色与示例任务；支持创建 / 编辑用户专家、零或一个 Skill 配置、下一轮切换 / 移除                         | 原八按钮退出常驻输入区；原 Skill 入口与机制不变；显式 Skill 优先；缺失配置和卸载状态不伪装成功 |
| P4：连接器读取           | 飞书官方 CLI 适配；账号验证、范围确认、会话标签、读取文档并在资料 / 详情查看                                        | 账号授权与会话授权分离；多账号 / 多会话隔离；解绑、撤权和失败真实展示；不扩大共享 MCP 配置     |
| P5：受审远端写入         | 对已支持连接器提出新建 / 修改方案，审核后提交，成果显示真实文档链接和回执                                           | 拒绝不写、只读不写、撤权不写；重复 / 冲突 / 不确定结果测试；不绕过 Partner gate                |
| P6：Electron 验收        | 在真实 Electron 开发态和打包态验证完整闭环及安装 / 卸载 / 重装                                                      | 全量自动检查、双轴 code review、人工检查和回归通过；真实第三方验证与测试服务验证分别记录       |
| P7：首个正式平台专家     | 发布“飞书办公套件专家”，持续引用后提供两级任务引导；贯通多维表格新建及右侧真实资源打开                              | 引导不自动发送；未连接 / 未授权不执行；仅创建新 Base，文档审核链与高风险操作边界不回归         |
| P8：平台原生文档直达交付 | Partner 对完整的新建文档请求直接调用受控平台适配器；成功返回真实链接并在右侧打开，不落本地 Artifact；飞书为首个实现 | Coder 不变；新建无待审核；修改仍审核；缺失连接只给恢复动作；unknown / partial 不重试或伪装成功 |

第三方接入的外部前置条件：本轮按用户选择使用飞书官方 CLI，固定并核实其读写 / 授权契约，再由用户完成测试账号授权。CLI 不是 MCP server，不伪装为 MCP 接入。当前未安装和授权真实 CLI，不能标记真实连接器已验证。测试夹具只用于自动化，不能在用户界面冒充已连接第三方服务。

### 实施契约与进度（2026-08-31）

- **P1 代码、自动检查与 macOS arm64 打包态生命周期通过。** `SpaceExtensionStore` 持有归档与启用注册；`.space-extension` ZIP 内含 `manifest.json`、独立 `ui/index.html`，安装/升级默认停用。主应用仅注册 `space.extensions.list/install/setEnabled/uninstall/view`，主进程校验 primary main frame。UI 使用 `app://space/__space-extension-frame` + `sandbox="allow-scripts"` 和独立无网络 CSP，不能使用继承主页面 CSP 的 srcDoc。包在 `extensions/partner-library` 单独构建，主应用构建无需包存在。
- **P2 代码与自动检查通过。** `SpaceExpertRefT={extensionId,expertId,revision}` 是前端可提交的选择；`PartnerExpertSnapshotT={extensionId,extensionVersion,expert}` 只能由宿主解析并持久化。使用 `space.extensions.catalog/resolveExpert` 与 `session.partnerExpert.get/set/changed`，创建接纳在 ACK 前落盘。每轮固定快照并检查当前扩展可用；旧快照保持旧角色，不能代替当前使用授权。临时会话拒绝持久专家绑定，不偷偷把临时会话转为持久会话。
- **P2 已运行的检查：** 前端草稿 / 会话状态、目录刷新竞态、创建 ACK 所有权及详情展示的 37 项定向回归通过；`RealKodaXSession → SDK → 本地 RecordingProvider` 已验证多轮、两会话、禁用 / 卸载拒绝和排队切换，不调用外部模型。创建 / 切换的持久化与恢复 / fork、真实 IPC 注册权限、临时会话与删除清理均有回归。阶段回归 129 项、Electron typecheck 和 SDK ESM 边界检查通过。
- **P3 代码、自动检查与核心桌面交互通过。** `space.extensions.expert.save` 接收内容 `values` 和成对可选的 `expertId/expectedRevision`，宿主生成用户身份、执行版本冲突检查；编辑预置专家创建副本。`space.extensions.expert.delete` 只软删除用户专家。用户记录与只读扩展包分开保存，卸载和升级保留记录；`user.` 身份前缀只能由宿主使用。引用与快照新增可选 `useSkill`，明确选择 `false` 才跳过专家配置的 Skill，不影响原独立 Skill 调用。原八个场景按钮迁入 0.3.0 包，与写作导师合计九个预置专家；输入框保留原 Skill、交付格式、权限与模型入口。
- **P1–P3 交付时的公共边界。** Frame 仅能通过当前视图的受限消息请求目录、选择、详情及专家保存/删除，extension、project、session 归属由宿主持有。Skill 仍为零或一个明确名称，未配置就不增加 Skill 动作。当时 P4/P5 尚未实施；本轮追加进度见下，不把代码完成等同于真实账号已接通。
- **异步边界已补回归。** 专家目录读取与编辑共用队列，避免 Windows 原子替换空窗；同一会话的选择校验和持久化按入站顺序执行，早发慢选择不能覆盖后发移除。文件菜单、快捷键、命令面板、项目行及侧栏的新对话统一清除专家草稿并拒绝旧创建 ACK，正文保留；普通会话导航不清草稿。
- **P1 已运行：** 安装/升级/并发/越界/符号链接/压缩炸弹/卸载保留数据的 Store 测试；真实构建归档再安装测试；UI 状态、上下文失效、CSP、导航和 IPC sender 测试；全量 typecheck、renderer production build。没有第三方服务调用。
- **桌面观察：** 独立 profile 下 `app://space` 可渲染无扩展的主应用。最新 macOS arm64 打包版已完成原生文件选择器安装 0.3.0 包 → 默认禁用 → 启用后显示插件库 → 禁用撤下入口并将旧绑定标为不可用 → 卸载 → 重装 / 启用；自定义专家、原会话和草稿保留，旧绑定恢复可用。安装不连接账号，界面为九个预置专家加一个隔离测试专家，连接器计数为零。
- **专家桌面观察：** 从库中选择写作导师后原草稿未改写、未自动发送；标签打开现有右侧详情，提示词、版本和示例任务可见。新建用户专家并编辑至 r2 成功；故意配置不存在的 `qa-missing-skill` 后首次发送明确拒绝，用户在详情中关闭默认 Skill 后才创建会话，持久化快照保留 `useSkill: false`。离线两轮对话保持绑定；新对话清空专家但保留正文，返回旧会话恢复专家。
- **验收边界：** 桌面测试使用隔离 profile、embedded 配置和 `KODAX_FORCE_MOCK=1`，没有调用真实模型、授权第三方账号或写远端文档。Mock 仅证明交互与 IPC；角色实际进入 SDK 的证据来自上述 RecordingProvider 回归。真实模型重启恢复、开发态完整循环、明暗 / 窄屏及其他平台仍待 P6；默认 Coder daemon 启动等待未据此宣称已修复，不修改用户配置或生产启动门禁。

### 自动与人工检查方式

- 每个实现切片先写失败测试，再做最小实现；测主进程注册 / 持久化 / 实际执行入口，不只测组件文字。
- 定期运行相关单测和 typecheck；交付前运行完整测试、lint、typecheck、build 及必要的 packaged smoke。新增核心逻辑覆盖率按仓库要求不低于 80%。
- 浏览验证使用用户要求的 Electron，不以浏览器静态预览代替桌面能力验收；真实连接器授权和写入测试须使用用户明确提供的测试目标。
- 不自动发布、推送、修改第三方生产文档或升级 SDK；人确认计划后才进入构建链。

### 最终自动检查记录（P1–P3）

- `NODE_OPTIONS=--no-experimental-webstorage npm test`：release 46/46；desktop 3018 通过、4 条件跳过、0 失败；IPC schema 321/321。合计 3385 通过、4 跳过。该 Node 25 运行参数关闭无 DOM 测试中的实验性 localStorage，避免其路径警告干扰日志断言；不关闭测试断言。
- `npm run typecheck`、`npm run lint`、`npm run build:smoke`、`git diff --check` 均通过；独立插件包构建成功。SDK 锁定 0.7.95，应用版本保持 0.1.45。
- `node scripts/pack.mjs --mac --arm64` 通过，生成本地未签名 `.app` / `.dmg` / `.zip`，未发布。packaged smoke 检查 ASAR 入口、精确 SDK 依赖、worker、SQLite 与原生依赖；额外检查主应用 ASAR 不包含 `extensions/partner-library` 源码或 `.space-extension` 归档，独立 UI 从已安装包加载。
- 新增扩展存储/专家核心：行覆盖率 98.41%、分支 96.47%；前端目录/消息桥/绑定/操作核心：行 98.35%、分支 84.32%。覆盖范围不等同于整个历史代码库。
- 完整回归发现原 `listConfiguredAccounts` 忽略内存后端，已补最小 guard 和虚拟账号回归；不读取、修改或导出真实凭据。测试机 node-pty 的本地 spawn-helper 补回执行位后 9/9 通过，无依赖版本变更。
- 双轴评审的 4 个 P2 均已修复并独立复核，当前开放 0。见 [独立评审记录](F146-plugin-library-review.md)。桌面、打包与真实第三方结果单独记录，不由单元测试代替。
- 人工验收步骤见 [F146 P1–P3 测试指南](../test-guides/FEATURE_146_v0.1.61_TEST_GUIDE.md)。

### P4/P5 实施与自动检查记录（2026-08-31）

- **P4 代码完成。** 独立包升级为 0.4.0，连接器页提供飞书文档。宿主使用固定版本 `lark-cli` 验证用户已有 profile；账号连接、会话选择与文档范围分离。只读取明确允许的 docx URL，资料卡和详情保存来源、正文与版本；绑定支持恢复 / fork / 移除。未安装、未登录和范围失效如实显示。
- **P5 代码完成。** 仅支持新建纯文本文档和向指定文档追加纯文本。Partner SDK 每轮只获得范围内 read / propose 工具，不提供 apply。可信宿主界面展示完整内容并按不可变 hash 审核；主进程重验账号、范围、扩展、策略与 plan 状态。成功、冲突、未知、部分成功分别持久化，未知 / 部分成功不自动重试。不提供覆盖、删除或上传，不声称飞书有原子 CAS 或可回滚已提交请求。
- **原能力不变。** 不新增执行引擎，不注册全局 SDK 工具，不修改共享 Skill 或原 MCP 配置；远端来源 / 提案 / 回执使用专用类型，不冒充本地文件路径。全局连接器写权限默认关闭，只有用户在可信界面明确确认才能改变。
- **输入框入口收敛。** Partner 保留单一“＋”入口和 Coder 已有的文件 / 文件夹附件管线，但使用独立的 Partner 菜单实现，避免改变 Coder 的内容和点击交互。根菜单展示附件、斜杠命令、连接器、技能和专家；悬停斜杠、连接器或技能时在右侧展示对应内容。连接器面板使用插件清单及受控账号、本会话开关，不调用 Coder 的项目级 MCP 发现；技能继续走原共享 Skill 发现与插入，专家入口暂时进入现有插件库专家页。
- 最终全量 `NODE_OPTIONS=--no-experimental-webstorage npm test`：release 46/46；desktop 3091 通过、4 条件跳过、0 失败；IPC schema 325/325，合计 **3462 通过、4 跳过**。一次前序运行的既有命令面板浏览器用例超时，单独 6/6 与最终全量均通过；未延长超时或删断言，未将原因未经验证地归为业务缺陷。
- `npm run typecheck`、`npm run lint`、`npm run build:smoke`、独立扩展构建通过；最终 IPC 修复后重新执行主进程构建、Electron typecheck 与相关 lint 通过。宿主仍 0.1.45，SDK 仍 0.7.95。
- 定向核心行 / 分支覆盖：CLI runner 98.83% / 88.64%，CLI adapter 97.01% / 88.62%，service 92.06% / 75.12%，store 100% / 85.19%，run adapter 96.43% / 87.88%。不是整个应用的覆盖率。
- 双轴独立评审完成并复核全部修复，当前开放项为 0。详见 [P4/P5 评审记录](F146-p4-p5-review.md)、[飞书契约与边界](f146-p4-p5-feishu.md)及 [P1–P5 人工测试指南](../test-guides/FEATURE_146_v0.1.61_TEST_GUIDE.md)。真实账号授权和远端读写仍归 P6，不由受控回归替代。
- `node scripts/pack.mjs --mac --arm64 --dir` 与全部 packaged smoke 通过：未签名本地 `.app`、ASAR / SDK / worker / SQLite / 原生依赖检查成功，未发布。新包已在隔离 profile 中启用 0.4.0 插件；桌面看到原预置 / 自定义专家保留、连接器 1、飞书配置右侧详情、真实“未找到飞书 CLI”提示、空账号选择和默认禁止写入。未点击允许写入、未登录或调用第三方。
- 桌面观察曾出现部分 AX / 画面更新滞后；源码完整 Shell 导航独立复测正常。仅重启隔离 QA 进程并加 `--disable-renderer-backgrounding --disable-backgrounding-occluded-windows` 后，实际插件 / 连接器 / 配置导航观察通过；未改生产启动参数，未把该现象归因于已证实的业务缺陷或声称修复。默认启动条件下的重绘稳定性继续列为 P6 人工复核项。

### 专家目录分层补充（2026-09-01）

- 独立插件包 0.7.0 使用 host API v2 声明新增目录元数据；新宿主仍读取 v1 包，而 v1 包不能冒充支持该分层。专家页在“专家”内部固定提供“岗位专家 / 任务专家 / 平台专家”三个一级选项，不再把“专家”和“专家团”拆成两套列表。
- 每个一级选项根据当前目录动态生成少量业务大类，并始终以“全部”开头、“专家团”结尾；当前没有业务大类时只显示“全部 / 专家团”。类型或分类切换只在 frame 内过滤，不新增宿主请求，也不改变当前会话绑定。
- 九个现有预置按岗位或任务分类，并因定义新增目录元数据统一升到 revision 2；旧的 revision 1 新选择必须刷新，已经绑定的旧会话仍保留不可变快照。平台专家暂为空，不用连接器卡片冒充平台专家。旧定义缺少目录元数据时兼容归入岗位专家；旧编辑器修改或复制已有定义时保留元数据，新编辑器可用 `category: null` 明确清空分类。
- `listingType=team` 目前只表示目录中的“专家团”陈列标签；一段会话仍只绑定一个不可变 ExpertDefinition 快照。本轮没有引入多 Agent 成员、调度、权限并集或 subagent 编排，界面和文档不得把该筛选描述成已实现的多 Agent 专家团。

### 首个正式平台专家补充（2026-09-01）

用户确认按以下边界直接开发“飞书办公套件专家”，归入平台专家，不再以连接器卡片或纯提示词专家冒充完整能力：

- 专家由持续角色 prompt、一个明确的飞书 Skill、能力引导和飞书连接器共同组成。专家绑定继续使用现有不可变会话快照并在后续每轮生效；Skill 继续走共享单项调用和显式冲突规则；连接器仍是账号、资源和执行权限边界，任何一层都不能替代另一层。
- 平台专家绑定后在输入框上方展示“任务领域 → 具体动作”两级引导。点击只把该动作的模板填入当前可编辑草稿，不覆盖已有正文、不自动发送、不自动调用工具。用户可返回上一级或移除专家；能力引导随专家绑定持续存在。
- v1 对用户展示飞书文档与多维表格两类真实可用入口，但新增执行闭环只扩展“新建多维表格”：用户发送创建意图后，宿主使用当前会话已启用、已验证身份的飞书连接器执行受控 CLI 命令，创建全新的 Base 与首表字段，并返回真实 URL。读取 / 新建 / 追加飞书文档继续沿用既有范围与审核链，不借本票改变语义。
- 新建 Base 仅允许显式 `as user` 身份、固定 profile、受限名称 / 表名 / 字段数量和字段类型、无 shell 拼接。未安装 CLI、未登录、账号不匹配、连接器未绑定、缺少 create 权限或扩展不可用时必须拒绝。删除、覆盖、权限调整、批量通知、审批和其他高风险写入不在本票范围。
- 右侧采用混合任务面板：发送前可显示本地结构化创建草案；创建成功后打开经宿主验证的飞书资源 URL，使用户能在飞书原生页面继续修改。外部页面不继承主 renderer IPC、Node 能力或账号 token；失败 / 结果不确定时不打开伪造资源，也不显示成功。
- 其他飞书业务域只有在连接器和执行闭环实际存在时才能成为可点击能力；未实现项不以“即将支持”的可执行样式出现。真实账号写入仍须用户在测试目标上验收，fixture 只证明协议与交互。

P7 按以下 tracer-bullet 票执行，每票以前一票的公共接缝为依赖：

| 票号 | 公共接缝                                    | 完成定义                                                                                                                        |
| ---- | ------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------- |
| P7.1 | ExpertDefinition / manifest capabilityGuide | host API v3 校验两级引导、同包连接器引用、旧 v1/v2 包与旧快照兼容；正式平台专家携带可解析的 prompt、Skill 和引导                |
| P7.2 | Partner composer capability guide           | 绑定后持续显示两级入口；选择动作只更新草稿；已有正文、发送、移除、会话切换和窄屏均有回归                                        |
| P7.3 | 飞书 Base create typed connector action     | 模型仅在会话授权后获得受控新建工具；CLI argv / 输入 / 身份 / scope 被重验，成功返回真实 Base URL，失败不伪装成功                |
| P7.4 | Partner 右侧任务草案与远端资源              | 新建前显示结构化草案；成功回执打开已验证飞书 URL；外部页隔离，历史文档资料 / 提案 / 成果区不回归                                |
| P7.5 | 验收与回退                                  | 定向 / 全量测试、typecheck、lint、build、双轴评审和 Electron 人工指南完成；真实第三方结果单独记录，禁用专家或连接器可停止新调用 |

### P7 实施与自动检查记录（2026-09-01）

- **P7.1–P7.4 代码完成。** 独立插件包升级到 0.8.0，并声明首个正式“飞书办公套件专家”；专家包含持续 prompt、内置 `feishu-office-suite` Skill、同包飞书连接器引用和两级 `capabilityGuide`。host API v3 负责校验新元数据，旧 v1/v2 包和既有绑定快照保持兼容。
- **对话交互完成。** 专家绑定后在原主对话持续生效；输入框上方按“飞书文档 / 多维表格 → 具体动作”展示引导。点击动作仅在光标处插入可编辑模板，不自动发送、连接账号或触发远端操作；切换会话、移除专家、已有草稿和窄窗布局均有回归测试。
- **Base 创建闭环完成。** 模型只在 Partner 会话、已启用连接、精确飞书文件夹范围和显式 `createBase` 权限同时成立时获得结构化新建工具。主进程在异步门禁后重验账号、scope、连接 revision 与扩展状态，再以固定 argv 调用官方 CLI；输入 schema 与运行时共用一份事实源。只有名称、首表和字段完整匹配的严格回执才记为成功，partial / unknown 不自动重试或伪装资源。
- **右侧任务与资源完成。** `preparing / submitting / succeeded / failed / unknown` 状态先持久化为宿主任务，再由单一 renderer Provider 分发。新任务只自动打开一次；同任务后续状态更新原标签，历史任务不在恢复时重放。并发 `A → B → A` 事件按 revision 消费，不会因 Map 覆盖顺序漏掉 B。成功后只打开宿主验证的飞书 URL；iframe 保持 `allow-forms allow-scripts`，并保留系统浏览器回退。
- **安全与兼容复核完成。** First-party Skill 路径固定在专属根目录并拒绝绝对路径、父路径和符号链接；飞书 scope、Base JSON Schema 和远端记录订阅均已收敛为单一事实源。文档新建 / 追加继续使用原审核链；未实现业务域、删除、覆盖、权限修改和通用 CLI 不向模型开放。双轴独立复审当前开放项为 0。
- 最终 `NODE_OPTIONS=--no-experimental-webstorage npm test` 整体退出 0：release 54/54、IPC schema 338/338，desktop 全套完成且 0 失败；`npm run typecheck`、`npm run lint`、Prettier、`npm run build:smoke` 和 Skill 快照检查通过。测试仅使用受控 CLI / 浏览器夹具，没有登录或写入真实飞书。
- **P7.5 仍保留真实服务人工项。** TC-036 必须由用户在获准的非敏感测试目录创建一个可删除 Base，并核对目录、名称、首表、字段与官方页面行为；在该步骤完成前不宣称真实飞书端到端验收通过，也未发布安装包。

### P8：Partner 通用平台原生资源直达交付（2026-09-02）

> **范围修正（用户确认，2026-09-02）：** 当前 P8 只交付 Partner 通用能力：可扩展的 providerId、经适配器验证的 HTTPS canonical URL、宿主任务信号、只打开一次的右侧投影、隔离且持久化的网页登录会话，以及 Partner-only 归属。Partner 核心不枚举飞书、钉钉、腾讯文档，也不校验平台私有 URL 结构。具体平台的连接器、创建/回读适配器、账号与 scope 逻辑、manifest action 和专家 prompt 均移到后续增量。本节后续保留的飞书首适配内容是后续草案/既有实验实现，不属于当前通用能力的完成判据；本范围修正优先于旧文字。

#### 决策与版本

- 本增量继续归属 F146，不创建重复 Feature。当前开发 checkout 暂为应用 `0.1.45` 与 KodaX `0.7.95`；P8.5 的独立首发目标固定为 fork-only prerelease `v0.1.61-p.1`，不占用 canonical v0.1.61。正式 v0.1.61 仍需等待同版本 F137/F139 等全部 exit gate，SDK 不升级。
- 宿主以 `partnerNativeDocumentDeliveryV1` 表示通用“已验证任务 → canonical URL → 右侧原生网页”能力；它不等于任何平台的写权限。当前 Partner library 不请求该 capability，待某个平台适配器完成后再由对应插件 manifest 显式声明。
- P8 只修改 Partner profile、Partner 远端资源宿主契约和 Partner 右侧网页工作区。任何 Coder 专属系统提示词、运行时、界面和业务逻辑均不修改；具体平台专家、连接器声明和共用 Skill 不属于本轮目标。

#### 后续平台适配草案（本轮不实施）

#### 目标流程与风险矩阵

用户已经在当前 Partner 会话启用飞书连接器，并说“帮我创建一份飞书文档”时，平台选择已经完成。Partner 应根据现有材料和用户给出的自由度生成标题与正文，直接创建到已连接账号的个人空间；只有用户明确指定了本轮已授权目录时才使用该目录。成功后聊天返回真实标题和链接，右侧打开该 canonical URL。右侧浏览器没有飞书网页 cookie 时允许显示飞书官方登录页；网页登录状态与连接器 OAuth 状态是两套状态，不能互相冒充。

| 用户意图 / 状态                                    | Partner 行为                                                                          |
| -------------------------------------------------- | ------------------------------------------------------------------------------------- |
| 读取本轮已授权的既有文档                           | 直接读取并引用来源，不产生本地副本作为交付目标。                                      |
| 新建文档；标题 / 正文已给出或用户允许“自由发挥”    | 直接调用 typed create 工具；无额外形式化确认，不产生 Pending-review 或本地 Artifact。 |
| 新建文档并明确指定本轮已授权文件夹                 | 直接在该目录创建；目录不在当前 scope 时停止并给出授权动作，不改去个人空间或本地文件。 |
| 用户明确说“先起草 / 先给我看 / 暂时不要创建”       | 只在聊天中给草稿；不调用远端写工具，也不把草稿冒充已创建文档。                        |
| 向既有文档追加或修改内容                           | 继续生成不可变提案、展示完整差异并由可信宿主确认后提交。                              |
| 删除、覆盖、移动、共享 / 权限、批量通知或审批      | 当前版本不执行；不得因系统提示词或专家 Skill 获得新权限。                             |
| 连接器未启用、未连接、授权失效或缺少显式目录 scope | 给出准确的启用、连接、重新授权或目录授权动作；保持飞书目标，不静默生成本地文件。      |
| 当前 run 有多个可创建账号且无宿主选择记录          | 显示可信账号选择器；选择落宿主记录前 0 dispatch，不接受模型猜测或最近账号。           |
| 返回 `unknown` / `partial` 或派发后连接中断        | 不宣称成功、不自动重试、不打开伪造页面；提示用户到飞书核对，避免重复创建。            |

#### 技术接缝

1. **Partner 基础 profile。** `partner-profile.ts` 先识别交付目标，指定平台的专用连接器优先于通用 delivery / Artifact；验证契约检查 `destination-fidelity`、`connector-result-truthfulness` 和 `no-silent-fallback`。该定向修改及回归测试已经完成，但单独不能产生直创能力。
2. **Typed task。** 在 `partner-connector` schema 增加通用 `PartnerNativeDocumentTask`，并把 task 摘要纳入 `PartnerRemoteRecords`；飞书首适配另有仅含可选已授权 folderUrl、规范化 title 和有界 content 的 provider input。connectionId、选择凭证和 invocation identity 均由宿主解析 / 注入。task 同时承载命令状态和验证结果，不生成要求 proposalId 的 legacy receipt；只有经 schema 校验并由对应 provider adapter read-back 成功的 `succeeded` 才能携带匹配的 resourceId、title 与 canonicalUrl。
3. **账号 eligibility。** 宿主投影非敏感 `PartnerDocumentCreateEligibility`：状态、枚举 reason、recoveryAction，以及需要选择时来自 live bindings 的 connectionId / accountLabel / connectionRevision 候选。0 个 eligible binding 时工具隐藏但 run context / UI 能区分未连接、扩展禁用、需重授权、缺 create/read scope 和宿主升级；1 个由宿主解析；多个时按安全 accountLabel 生成可信账号选择器，用户选择写入绑定 session、connectionId、connectionRevision 的 host-owned 记录后才可执行。自然语言、模型 tool args、列表顺序和最近使用都不能生成选择证据，binding 变化会使选择失效。
4. **持久化与一次派发。** store 从严格 v1 原子前迁移到 v2，加入 documentTasks、唯一 invocation key 与单调 record revision，保留旧 proposals / receipts / Base tasks。`PartnerConnectorService` 复用 Base 的 `preparing → submitting → terminal`、dispatch-owner、connection revision、scope hash 和提交前重验模式，调用现有受控 `FeishuCli.create`。每个 admitted 顶层输入在 runtime 构建前取得稳定 `turnExecutionId`：pre-admission retry / reconnect 沿用，interrupt 留在当前 ID，after-turn 下次 admission 才换新 ID。账号和目录解析后以 session、turn、resolved connection + revision + scopeHash、canonical target 和 normalized title/content hashes 组成 key；相同调用返回同一 task，CLI 至多派发一次，不同账号 / 内容 / 新 admitted turn 不碰撞。遗留 `preparing` 收敛为 `failed/not_dispatched`，死亡 owner 的 `submitting` 收敛为 `unknown`，均不重试。
5. **严格后置校验与目标证据。** `FeishuWriteResult` 没有 title，因此 task 内部 postcondition reader 只能对本次严格 create receipt 返回且 ID 匹配的 exact URL 使用同一账号和 readonly scope read-back；它不需要 URL 预先在 `binding.documents`，不创建 RemoteSource / Materials，也不扩张模型后续读取权限。reader 识别官方 Markdown 导出的 `<title>…</title>` 包装或 H1 标题，解码基础实体；title/body 均 NFC、CRLF→LF、去行尾空白，正文对比忽略导出器插入的纯空行但保持所有非空行内容和顺序。派发前错误=`failed`；派发后无可信 ID/URL=`unknown`；有可信身份但 read-back/后置条件不完整或不匹配=`partial`；全部匹配=`succeeded`。官方 CLI 暂无 parent metadata，因此目录证据明确来自已持久化 canonical target、live scope 重验和固定 `--parent-position my_library` / exact `--parent-token` argv；自动测试核对 argv，真实账号验收核对页面位置，不冒充远端 parent read-back。
6. **Run-scoped 工具与握手。** 首个适配增加 `partner_feishu_document_create`，仅在 Partner、非 Plan、`hostApiVersion >= 4`、宿主和扩展都声明通用 `partnerNativeDocumentDeliveryV1`、飞书 action enabled，且账号 eligibility 已解析时出现。列出后发生撤权 / revision 变化，在派发前返回结构化可恢复失败。工具只由 `createPartnerConnectorRunRuntime` 临时物化且不写 SDK 全局注册表，service 再断言 Partner surface。后续钉钉、腾讯文档用各自 provider tool/action，不改通用 delivery capability。本次最小发布组合是 v0.1.61-p.1 / library 0.9.1；未来版本仍按 capability 而非精确 SemVer 判定。
7. **兼容但不可执行的旧提案记录。** `partner_connector_propose`、proposal mutation IPC 和 `service.propose` input 全部 append-only；持久化 record/output 继续识别 `create | append`，避免旧数据库升级失败。历史 create 提案不进入待审核列表、不触发 opener，apply 会收敛为 rejected 且 0 dispatch；升级后不存在新造或执行 legacy create proposal 的 seam。
8. **Partner 专家路由。** 仅更新飞书平台专家 prompt、starter task 和 capabilityGuide：把“新建文档”路由到直创工具，把“追加 / 修改”保留在审核链。共用的 `feishu-office-suite` Skill 不作任何修改。用户说“随便写一份 / 你决定”时可推断标题和内容；专家 revision 随定义变化递增，旧会话继续保留不可变快照。
9. **直接打开平台原生资源。** `usePartnerRemoteRecords` 消费宿主 / store 所有的单调 revision + documentTaskId signal；provider-neutral direct-task opener 只挂载在 `PartnerWorkspace`，对 hydration 后首次发生的 `succeeded` 转换生成 open intent。它不判断飞书、钉钉或腾讯文档的登录态，也不渲染本地文档，而是把经过对应适配器验证的 HTTPS canonical URL 交给同一个 Partner 右侧浏览器；未登录时自然显示该平台官方登录页。旧 create-proposal opener 及其投影已删除；历史、失败、unknown、partial 不打开。
10. **简化可见流程。** 直创任务保留宿主侧最小审计记录，并在成功后登记为“任务产物”；正常路径不创建 RemoteProposal / RemoteReceipt、本地 Delivery / Artifact，也不进入 Pending review。任务信号会直接打开右侧平台网页；“任务产物”卡片预览与其展开后的完整列表读取同一份 `documentTasks`，点击后用相同 provider/resource 键复用右侧网页标签。既有资源的审核后追加回执也归入该完整列表；内部 taskId、hash、CLI 命令和凭据不展示给普通用户。

#### 用户批准的 tracer-bullet 开发票（范围修正版）

| 票号  | 状态      | Title                        | Blocked by   | What it delivers                                                                                                                           |
| ----- | --------- | ---------------------------- | ------------ | ------------------------------------------------------------------------------------------------------------------------------------------ |
| P8-T1 | Completed | Partner 原生资源交付契约扩展 | 无           | 通用 capability 与 store v2/provider-neutral document-task 契约；providerId 可扩展，Partner 核心不维护平台枚举。                           |
| P8-T2 | Completed | 通用右侧交付与网页登录       | P8-T1        | 经适配器验证的无凭据 HTTPS URL 经 records signal 到同一个 Partner browser target；官方登录与资源展示共用隔离的持久化网页会话。             |
| P8-T3 | Completed | Partner-only 归属与去重      | P8-T1、P8-T2 | opener 只挂载 PartnerWorkspace；历史不重放、非成功状态不打开，同一 providerId/resourceId 只保留一个右侧资源标签。                          |
| P8-T4 | Completed | 通用安全、兼容与回归         | P8-T1–T3     | 拒绝 HTTP、凭据 URL、本地协议和不安全 webview attachment；旧 records 可读；现有及未来 provider fixture 均通过同一公共 seam，Coder 不接入。 |
| P8-T5 | Deferred  | 平台连接器与专家适配         | P8-T1–T4     | 飞书、钉钉、腾讯文档等平台的创建/回读、账号/scope、manifest action 与专家 prompt 由用户后续补充，不属于本次 Partner 通用能力交付。         |
| P8-T6 | Completed | 删除旧新建提案并闭环右侧产物 | P8-T1–T4     | 新建文档只能走 direct task；真实飞书导出回读可验证成功；成功任务进入任务产物，预览与完整列表均复用右侧网页标签；历史 create 提案不可见且不可提交。 |

用户在 2026-09-02 明确要求开始开发，视为批准上述粒度和阻塞边。推进前沿从 P8-T1 开始；每票在已确认的 Partner conversation / run-scoped connector seam 上执行独立 red → green，前票未绿不推进被阻塞票。

### P9 — Partner 统一任务详情与回执优先交付（2026-09-03）

#### 已确认问题与范围

- 真实飞书验收已经证明最近两次文档均完成创建；失败来自 Partner 宿主把飞书 Markdown 导出的表格规范化和字符转义当成正文丢失，从而把可信创建回执误判为 `partial`。旧契约随后禁止 `partial` 保存 URL，导致聊天没有链接、“任务产物”为 0、右侧浏览器无法打开。
- WorkBuddy 与 Codex 的共同交互基线是“聊天承担任务控制与摘要，右侧承担统一查看”；任务状态与可查看资源必须正交，不能让内容校验告警吞掉已经存在的 URL 或文件。
- 本轮只改 Partner 的连接器宿主契约、任务卡片和右侧详情工作区。`@kodax-ai/kodax`、通用 session/runtime、Coder 提示词、Coder 界面和 Coder 工作流均不是修改目标。

#### 替代 P8 严格回读门的产品契约

1. 飞书官方 CLI 返回经适配器校验的 `documentId + credential-free HTTPS URL + revision` 时，Partner 即确认“远端资源已创建”，保存为 `succeeded` 任务并把真实链接交给聊天与右侧工作区。
2. 内容回读是独立的质量信号：完全匹配记为 `verified`；平台规范化、回读失败或内容无法确认记为 `unverified` 并附非阻塞提示。它不得改写资源创建状态、隐藏 URL 或触发自动重试。
3. 派发前失败仍为 `failed`；派发后没有可信资源身份仍为 `unknown`。历史 `partial` 记录继续可读但不猜测已丢失的 URL；新建文档路径不再产生新的“有链接但被隐藏”的 `partial`。
4. Partner 右侧仅保留按资源类型打开的详情标签：网页、输入文件、输出文件 / Artifact、Skill、专家、连接器和任务协作。卡片栏固定为“任务资料 / 任务协作 / 任务产物”，点击卡片项打开对应详情。
5. 删除右侧永久的“待审核 / 成果”目的地与确认汇总入口。确需确认的既有资源修改仍由宿主安全策略控制，但确认属于当前操作，不再是所有产物都要经过的栏目；全新文档直创不经过确认。

#### 用户批准的 tracer-bullet 开发票

| 票号  | 状态       | Title                              | Blocked by | What it delivers                                                                                                              |
| ----- | ---------- | ---------------------------------- | ---------- | ----------------------------------------------------------------------------------------------------------------------------- |
| P9-T1 | Completed | 真实往返与状态正交的失败测试       | 无         | 用真实 GFM 表格 / 转义差异固定“资源已创建但内容被平台规范化”的回归；保留无可信 URL 的失败与一次派发边界。                       |
| P9-T2 | Completed | 回执优先的远端产物契约             | P9-T1      | schema 与 Partner connector service 分开保存创建状态和内容校验；可信 URL 始终进入聊天、任务产物和自动打开链。                  |
| P9-T3 | Completed | Codex 式 Partner 统一详情工作区    | P9-T2      | 三张任务卡直接路由到网页、文件、Artifact、Skill、专家等详情；移除永久 Pending-review / Results 目的地和面向办公用户的终端入口。 |
| P9-T4 | Completed | Partner-only 集成验证与范围门禁    | P9-T1–T3   | 验证创建→链接→产物卡→右侧网页登录闭环、历史兼容与无重复打开，并扫描确认没有新增 Coder / KodaX Runtime 改动。                  |

上述 P9 契约取代 P8 第 242、247、250、254、273 行中“只有严格回读匹配才暴露 URL、`partial` 一律不打开”的旧结论；P8 的一次派发、授权重验、安全 URL、隔离网页登录和历史不重放约束继续有效。

#### P9 自动实现与验证记录（2026-09-03）

- 新建在线文档在适配器返回可信 `resourceId + canonicalUrl + revision` 后先原子保存并立即返回；内容回读改为后台、非阻塞的独立质量信号。平台规范化、回读暂不可用或进程退出均不再隐藏已经存在的链接，也不触发重复创建。
- Markdown 校验使用保留结构空白的快速路径和 GFM 语义树比较，覆盖表格分隔符、字符转义以及代码围栏内空行等真实差异；无可信 URL 的派发后不确定状态仍保持 `unknown`。
- Partner 固定卡片栏为“任务资料 / 任务协作 / 任务产物”；统一详情工作区承载网页、输入文件、输出文件 / Artifact、Skill、专家、连接器与任务协作。新在线资源信号直接打开同一持久化隔离网页会话，旧的永久 Pending-review / Results 导航和 Partner 终端入口已移除。
- disconnect、forget、deactivate 使用固定点 drain 等待在途任务及其派生校验，避免停用时迟到校验继续发布；相关并发回归已加入。
- 自动门禁通过：`npm test` 3,766 passed / 4 skipped / 0 failed，`npm run typecheck`、`npm run lint`、`npm run build:renderer` 与 `git diff --check` 均通过。两路只读独立审查未发现开放的 P1/P2。真实账号下的新指令交互仍由用户在已连接环境中做最终视觉验收，不由 fixture 代替。

#### P8 通用能力验收标准

- [x] providerId 是有界开放标识符；加入未来平台 fixture 时无需修改 Partner 中央 provider 枚举或平台 URL 白名单。
- [x] Partner 只接受适配器标记为 `succeeded` 且包含 resourceId、真实标题和无凭据 HTTPS canonical URL 的任务；失败、unknown、partial 和历史 hydration 均不自动打开。
- [x] 新成功信号只打开一次；providerId/resourceId 使用无歧义资源键，不同平台或资源不会碰撞，同一资源后续更新复用右侧标签。
- [x] 右侧使用 Partner 独立持久化 web session；禁用 Node/preload，拒绝不受信 partition、HTTP、带用户凭据 URL、本地协议、下载和站点权限。
- [x] 通用远端成功路径直接投影到右侧原生网页，不要求本地 Artifact、Results 中转或 Pending-review 卡片。具体写操作是否需要审核由平台适配器按风险决定。
- [x] opener 只挂载 `PartnerWorkspace`；Coder 不消费任务信号，不修改 Coder 提示词或工作流。
- [x] 当前平台插件不声明直达 capability，飞书等现有专家保持原审核语义；平台连接器、创建/回读与专家路由留待后续适配。

#### P8 通用能力自动实现记录（2026-09-02）

- P8-T1–T4 已完成。通用 task/signal/opener 接受语法受限但开放的 providerId，以及经对应适配器验证的无凭据 HTTPS canonical URL；新增平台不需要修改 Partner 中央 schema。平台私有 URL 与 resourceId 的一致性由适配器负责，Partner 只消费已验证成功结果。
- Partner 右侧远程页由无凭据 iframe 升级为独立持久化网页会话：无 Node/preload 权限、禁止非 HTTP(S) 导航、下载和站点权限，网页登录与连接器授权隔离。它能承载拒绝 iframe 嵌入的平台官方登录页；登录后仍在同一 Partner 右栏显示文档。该容器是 provider-neutral，不包含飞书域名白名单。
- 通用能力已通过既有平台和任意未来 provider fixture、URL 安全、历史不重放、单次打开、Partner ownership 与 webview attachment 测试。真实平台账号、API 创建和专家路由属于后续 provider adapter 验收，不再阻塞 P8 通用能力；F146 因其余插件库工作仍保持 InProgress。

#### 非目标与回退

本增量不增加任何平台的读取、创建、删除、覆盖、移动、分享或权限能力，也不把现有连接器自动提升为可写。平台适配器必须另行提供授权、风险和结果验证契约后才能声明通用 capability。通用层回退时可关闭 task opener，但不得改变既有连接器审核链和历史记录。store v2 只承诺前向迁移与新宿主继续读取，不承诺降级到只认识 v1 的旧二进制。

## 6. 验收标准

### 多连接器扩展的前置核实（2026-09-01）

用户要求新增企业微信、钉钉、网易邮箱、QQ 邮箱、腾讯会议、腾讯文档、百度网盘、金山文档及 Notion。[路线矩阵与批次](f146-multi-connectors.md) 已获用户确认；官方 CLI、MCP、邮箱授权码与自有服务端不能混为同一种登录方式。首批企业微信、钉钉、腾讯会议仅做官方授权与指定资源只读，独立包 0.6.0；其余六家依已确认路线后续实施，金山文档需外部应用 / 服务端前提。不更改既有飞书连接 / 共享 Skill。以下早期验收记录保留为历史阶段结果；飞书授权与 0.5.1 横向卡片见首次连接文档第 6–7 节。

### 首次连接与简约界面补充（2026-08-31）

用户参考 WorkBuddy 截图确认直接实施：本轮增加官方 CLI 安装确认 / 首次授权向导、简约连接器卡片和居中弹窗、输入框聚合会话开关；保留 Space 风格和独立 Extension 边界。接口、接缝、实现票及真实服务验证限制见 [首次连接设计](f146-feishu-onboarding.md)。这替代此前必须手工预配置 CLI 的产品前置条件，不扩大文档授权或改变 Skill。

代码与独立包 0.5.0 已实现；独立双轴评审修复 1 个 Try 加载竞态，当前开放 0。macOS arm64 打包与真实 CLI 归档校验 / 提取 / 版本执行通过，在线下载受慢网络限制；新版 Electron 已启动，但最终 Computer Use 视觉验收因 Mac 锁屏未执行。真实服务授权和完整 P6 仍待用户参与，不因此标记 F146 完成。

### 原功能验收清单

- [ ] Partner 显示一个“插件”入口，库内恰为“专家 / 连接器”；Coder 不新增此入口，原 Skill 入口和调用不变。
- [ ] 插件 UI 代码及目录来自独立安装包；卸载不是隐藏已静态编译进主 renderer 的页面；无包时 Space 可构建 / 启动 / 正常聊天。
- [ ] 纯提示词专家不触发额外 Skill 搜索、推荐或安装；配置的 Skill 复用旧执行边界，不能绕过单项 / dynamic-context / 工具权限限制。
- [ ] 专家页固定显示岗位 / 任务 / 平台三个一级选项；各自大类由当前目录生成，并以全部 / 专家团收口。平台无条目或专家团无条目时显示真实空状态，不伪造专家或多 Agent 能力。
- [ ] 专家标签、后端绑定和每轮 SDK profile 一致；创建 ACK 前持久化，失败可恢复；重启 / fork / 切换 / 删除覆盖测试。
- [ ] 两个 Partner 会话的专家和连接器不串；Coder 运行配置和技能行为不受影响；已有会话无新字段仍可用。
- [ ] 输入框不再固定展示八个模板按钮；专家 / 连接器选择不覆盖草稿、不自动发送；点击标签用现有右侧看详情。
- [ ] 连接成功、会话启用和写入权限分开；只读、断开、过期、错误有真实状态；移除标签不删除账号配置。
- [x] 远端资料、需要审核的既有资源改动和成功任务进入适用的现有表面；低风险文档直创登记到任务产物并自动打开右侧网页，不经过待审核，本地文件提案流程保持可用。
- [ ] 需要审核的修改在未确认、授权撤销、目标变化、扩展禁用或提案内容变化时不能提交；直创任务同样在派发前重验授权。失败或未知结果不显示成功、不盲目重试。
- [ ] token / secret 不出现在插件 UI、模型输入、普通持久化记录、日志、资料或成果。
- [ ] 安装 / 禁用 / 卸载 / 升级失败不破坏主应用；兼容版本、包路径安全、frame 来源和调用归属有负向测试。
- [ ] 禁用立即阻止新调用；已在途的远端操作先结算 / 对账再完成卸载，不声称能撤回已提交请求。历史会话、成果、审核、自定义专家配置及共享 Skill 保留；旧绑定显示不可用。
- [ ] 重新安装后能恢复可用入口，账号若需重新授权则明确提示；扩展升级不静默改变旧会话专家配置。
- [ ] 真实 Electron 下完成专家多轮、重启、连接器读写审核及卸载 / 重装验收；mock 和真实第三方的测试结论分别记录。

### 发布与回退

先发布满足接口版本的宿主，再独立投放扩展包；当前集成交付同步 fork-only v0.1.61-p.1 与 0.9.1 library，canonical v0.1.61 必须等 F137/F139 等整版门禁。按扩展或连接器禁用即可停止新能力，核心聊天、Skill、既有本地文件 / 审核 / 成果仍可用。保留历史记录只表示保留记录，不代表旧新建提案仍可展示或提交，也不绕过账号撤权、留存或内容访问限制。
