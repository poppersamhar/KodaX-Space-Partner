# KodaX Space v0.1.61 — Native Document Skill Suite and Semantic UI Polish

> Status: Planned
>
> Design owner: KodaX Space
>
> Runtime baseline: public KodaX Skill plugin registration already consumed by F135; no KodaX
> SDK fork
>
> Design method: the independent baseline in this document was completed before the comparative
> capability audit. It derives from Space F109/F114/F135, public file-format behavior, audited
> redistributable libraries, and KodaX Space product requirements. Anthropic prompts, code,
> examples, assets, wording, and document structure are not source material.

## Version outcome

`v0.1.61` turns Space's baseline Office/PDF byte writers into four dependable, Chinese-first
builtin workflows:

- `docx`: create, inspect, revise, and verify professional Word documents;
- `pdf`: inspect, extract, assemble, transform, fill, and verify PDFs;
- `xlsx`: create, inspect, revise, recalculate, and verify spreadsheet workbooks;
- `pptx`: create, inspect, revise, merge, render, and format-verify native editable presentations.

The release is not a promise to reproduce every feature of Microsoft Office, Adobe Acrobat, or any
third-party Skill. It provides one truthful path from user intent to a durable file, with bounded
input handling, deterministic mechanics, render-based verification, provenance, and explicit
limitations.

Alongside the document suite, F139 gives the existing KodaX Space shell a bounded semantic-motion
pass. Dense Task Dock lists and collapsible sections gain clearer scroll boundaries, stable active
markers, restrained item transitions, and state-specific emphasis without adding another permanent
background effect or moving interactive hit targets.

## Product boundary

- Skills orchestrate work; they do not become a second document engine.
- Space owns file access, delivery registration, checkpoints, policy, audit, rendering, and
  validation receipts.
- Coder and Partner share the same format semantics. Partner never gains unrestricted shell access;
  it uses typed Space document operations within F114 output roots.
- Existing `create_office_artifact` remains the fast structured path. The new suite may extend or
  replace its internals, but does not create a competing artifact store.
- Existing File Viewer remains the review surface. A Skill may request a render or validation
  receipt, but does not create a new permanent document editor. File Viewer only displays
  validation outputs; it is not the renderer and cannot produce a `visual: passed` result.
- F051 remains the later mixed-source Partner Document Work Pack for evidence-grounded extraction,
  comparison, synthesis, and repeatable business workflows. F137 supplies reusable format
  primitives; it does not pull the connected Partner pack into this release.
- F137 owns the reusable native PPTX format primitives: inspect, template-aware create/edit,
  merge/split/reorder, package repair, render checks, and format-level validation. F129 later reuses
  these primitives to add the durable Presentation Project/Studio, high-fidelity authoring loop,
  target-Office compatibility matrix, and verified-editability label; it must not create a second
  competing format service.
- `frontend-slides` remains the HTML-presentation workflow. `huashu-design` remains the visual
  direction and high-fidelity HTML design workflow. The new `pptx` Skill owns native file
  mechanics and format verification; F129 owns projectized high-fidelity authoring and target-Office
  verification.
- No Skill downloads packages or executables during a run. Optional capabilities that need
  LibreOffice, OCR, or another external runtime are advertised as unavailable when the runtime is
  absent.
- F139 is an independent renderer-only enhancement. It neither depends on document-job delivery nor
  changes Runtime, Session, permission, persistence, IPC, or artifact ownership.

## Independent source policy

The implementation may use:

- ECMA-376 / ISO/IEC 29500 concepts for OOXML packages and relationships;
- the PDF specification and documented behavior of selected redistributable PDF libraries;
- public documentation for audited libraries selected by Space;
- KodaX Space's own F109 writer, F114 delivery/checkpoint contract, preview workers, fixtures, and
  user requirements;
- independently authored test documents and intentionally malformed fixtures.

The implementation must not use third-party proprietary Skill text, scripts, examples, assets,
test data, section ordering, or distinctive checklists. A later comparative audit may add a
format-agnostic capability requirement such as “detect formula errors” or “warn before invalidating
a signature”, but must not supply implementation expression.

## Shared execution model

Every Skill follows the same six-state job:

1. **Inspect** — identify type, size, encryption, macros/active content, relationships, page/sheet/
   slide counts, and available processing capabilities.
2. **Plan** — select a bounded operation and state whether the source will be preserved, copied, or
   replaced through an F114 checkpoint.
3. **Execute** — use deterministic typed operations for mechanical changes; use the model only for
   content judgment, synthesis, and layout decisions. Extracted document content remains
   untrusted evidence and cannot authorize tools, paths, network access, credentials, or a changed
   task objective.
4. **Validate** — run format-specific structural and semantic checks.
5. **Render and review** — render representative or complete output according to the format's
   acceptance policy and inspect for clipping, blank output, broken glyphs, or obvious layout
   regressions.
6. **Deliver** — register the file with F114, retain content hashes and validation receipts, expose
   warnings, and never claim unsupported fidelity.

Generated and edited files default to a new output path. In-place modification requires an
explicit user request, an allowed F114 workspace root, and a checkpoint. Input files are never
silently overwritten.

### v0.1.61 execution and evidence boundary

- Pure-JavaScript parsers run in the existing bounded Worker path with byte/page/sheet/object limits,
  V8 heap limits, a hard deadline, cancellation, and no partial result. Office-compatible
  renderers/recalculators and OCR adapters run as job-scoped child processes with staged inputs, a
  fresh private output/temp/profile root, a scrubbed environment, macros/link updates disabled,
  no shell interpolation, and whole-process-tree cleanup on timeout or cancellation.
- These controls provide fault containment and reduce accidental exposure; they are not described
  as an OS security sandbox and do not claim to contain a successful native parser/renderer
  exploit. OS-enforced filesystem, network, process, and native-resource isolation is deferred to
  F138 after the `v0.5.x` product line and is not a v0.1.61 capability gate.
- If a required parser, renderer, OCR, or recalculation adapter is absent, only that adapter-backed
  operation is unavailable. Functionality is not disabled merely because F138 has not shipped.
- Every extracted paragraph, cell, formula string, hidden value, comment, note, metadata field, OCR
  span, and embedded-object summary carries source location and an `untrusted-document-content`
  label. Model-facing content uses the existing F122/F124 evidence delimiter and prompt-injection
  policy.
- Job-scoped plaintext intermediates and profiles use least-privilege private directories. Success,
  failure, cancellation, crash recovery, and startup orphan cleanup all remove them. Passwords and
  other secrets never enter options, logs, receipts, persisted job state, or model-visible text.
- Delivery modes are explicit: `inspect-only`, `sanitized-copy`, or
  `authorized-fidelity-copy`. The last mode requires user confirmation and an active-content
  inventory with per-part hashes; it cannot receive the ordinary safe-output label.

## Shared package shape

Each builtin is expected to contain only the resources it needs:

```text
<skill>/
├── SKILL.md
├── LICENSE
├── agents/
│   └── openai.yaml
├── references/
│   ├── operations.md
│   ├── validation.md
│   └── limitations.md
└── scripts/
    └── <small deterministic helpers only>
```

`SKILL.md` stays below 500 lines and contains the routing decision, core workflow, safety boundary,
and links to one-level-deep references. Large file-format notes belong in `references/`. Repeated,
fragile mechanics belong in typed host operations or small deterministic scripts, not copied code
blocks inside the prompt.

## Shared host contract

The exact IPC/tool names may change during implementation, but the owner boundary must preserve
these typed concepts:

```ts
type DocumentKind = 'docx' | 'pdf' | 'xlsx' | 'pptx';
type DocumentInputKind = DocumentKind | 'dotx' | 'xlsm' | 'xltx' | 'csv' | 'tsv' | 'potx' | 'image';

interface DocumentInputRef {
  readonly stagedId: string;
  readonly role: 'primary' | 'template' | 'merge-part' | 'asset' | 'data-source';
  readonly ordinal: number;
  readonly sha256: string;
  readonly byteSize: number;
  readonly kind: DocumentInputKind;
  readonly sniffedMime: string;
}

interface EphemeralSecretRef {
  readonly secretId: string;
  readonly purpose: 'pdf-open-password' | 'pdf-owner-password';
  readonly expiresAt: string;
}

type DocumentOutputTarget =
  | { readonly kind: 'artifact-store' }
  | { readonly kind: 'partner-run-output' }
  | {
      readonly kind: 'workspace-mutation';
      readonly workspaceEntryId: string;
      readonly expectedSha256?: string;
      readonly overwrite: 'new-copy' | 'checkpointed-replace';
    };

type DocumentOperationRequest =
  | {
      readonly kind: 'docx';
      readonly operation:
        | 'inspect'
        | 'create'
        | 'template-create'
        | 'bounded-edit'
        | 'comment-edit'
        | 'redline-copy'
        | 'accept-redline-copy'
        | 'review-pdf-copy';
    }
  | {
      readonly kind: 'pdf';
      readonly operation:
        | 'inspect'
        | 'extract'
        | 'page-transform'
        | 'acroform-fill'
        | 'overlay-form-copy'
        | 'encrypt-copy'
        | 'decrypt-copy'
        | 'extract-images'
        | 'ocr-searchable-copy'
        | 'generate'
        | 'redact-verified';
    }
  | {
      readonly kind: 'xlsx';
      readonly operation:
        | 'inspect'
        | 'create'
        | 'template-create'
        | 'bounded-edit'
        | 'import-delimited'
        | 'export-delimited'
        | 'recalculate-copy';
    }
  | {
      readonly kind: 'pptx';
      readonly operation:
        | 'inspect'
        | 'create'
        | 'template-create'
        | 'bounded-edit'
        | 'merge'
        | 'split'
        | 'reorder'
        | 'render-verify';
    };

// Each branch above owns a closed, operation-specific payload schema. Generic options and arbitrary
// path fields are intentionally absent.

interface DocumentJobRequest {
  readonly idempotencyKey: string;
  readonly action: DocumentOperationRequest;
  readonly inputs: readonly DocumentInputRef[];
  readonly outputTarget: DocumentOutputTarget;
  readonly secretRefs?: readonly EphemeralSecretRef[];
  readonly sourceRefs?: readonly string[];
  readonly deadlineAt?: string;
}

interface DocumentOutputManifestEntry {
  readonly role: 'primary' | 'review-copy' | 'extracted-asset' | 'preview' | 'receipt';
  readonly deliveryId: string;
  readonly sha256: string;
  readonly byteSize: number;
  readonly sniffedMime: string;
  readonly validationReceiptId?: string;
}

interface DocumentCheck {
  readonly name: string;
  readonly status: 'passed' | 'warning' | 'failed' | 'not-run' | 'not-verifiable';
  readonly method:
    | 'structural'
    | 'expected-value'
    | 'source-grounding'
    | 'sampled'
    | 'model-reviewed'
    | 'user-confirmed';
  readonly checked: number;
  readonly total?: number;
  readonly locations?: readonly string[];
  readonly inheritedFromInput: boolean;
}

interface DocumentValidationReceipt {
  readonly jobId: string;
  readonly kind: DocumentKind;
  readonly structural: 'passed' | 'failed' | 'not-run';
  readonly visual: 'passed' | 'warning' | 'failed' | 'not-run';
  readonly renderer: string;
  readonly checks: readonly DocumentCheck[];
  readonly outputs: readonly DocumentOutputManifestEntry[];
  readonly baselineFindings: readonly string[];
  readonly introducedRegressions: readonly string[];
  readonly truncated: boolean;
  readonly uninspectedScope: readonly string[];
  readonly warnings: readonly string[];
  readonly unsupportedFeatures: readonly string[];
}
```

Requirements:

- `sessionId`, `runId`, `surface`, `projectRoot`, and `toolCallId` come from trusted tool execution
  context; model-authored arguments cannot grant authority. Files cross the boundary as staged IDs,
  not arbitrary paths or an authorizing `outputRoot`.
- Validate the order, role, hash, root, bytes, and sniffed type of every input. Template, merge,
  asset, and data-source paths cannot hide inside generic options.
- Sniff bytes; do not trust extensions.
- Apply ZIP entry, expanded-size, entry-count, compression-ratio, XML-size, and traversal limits to
  OOXML.
- Parse XML without external entities or network resolution.
- Treat macros, external relationships, embedded packages, OLE, PDF JavaScript, launch actions,
  and remote data links as active content. Report and preserve only when explicitly supported;
  never execute them.
- Stage all outputs as one job, recheck input CAS before publication, validate every required
  output, then atomically publish the manifest. Failure/cancellation registers no partial success;
  retries with the same idempotency key cannot duplicate deliveries.
- Keep raw document content out of logs and validation receipts.
- Record the selected renderer and skipped checks so “not run” cannot look like “passed”.
- A sampled or model-reviewed semantic check never means the whole document “passed”. Receipts
  expose method, coverage, inherited input defects, newly introduced regressions, truncation, and
  uninspected scope.

## Format routing summary

| User intent                                                     | Primary Skill     | Optional composition                                   |
| --------------------------------------------------------------- | ----------------- | ------------------------------------------------------ |
| Write or revise a Word report, contract, memo, or specification | `docx`            | `pdf` for a reviewed final export                      |
| Extract, merge, split, rotate, fill, redact, or generate a PDF  | `pdf`             | `docx` when an editable source is also requested       |
| Build, clean, analyze, or revise a spreadsheet deliverable      | `xlsx`            | none by default                                        |
| Produce or revise a native editable PowerPoint file             | `pptx`            | `huashu-design` for visual direction                   |
| Produce an animated HTML presentation                           | `frontend-slides` | `pdf` for static export                                |
| Explore a high-fidelity visual direction or HTML-first deck     | `huashu-design`   | `pptx` only when native editable delivery is requested |

Routing never invokes multiple Skills merely because a file extension is mentioned. The primary
input/output contract decides the owner.

## Comparative capability audit

> Audit performed after the independent baseline was frozen on 2026-07-26.
>
> Pre-audit document SHA-256:
> `C9BA9C9DBB15223AB08E9B8207CE35ABC45FEC1A81C7AA713447E53536BE54CD`.
>
> External comparison baseline: Anthropic `skills` commit
> `b29e7cf65e5cb78a5ac33d582270551bc74a14eb`, accessed 2026-07-26. Only this
> identifier is retained; proprietary source content is not stored.

### Audit method

- Compare only public, high-level capability coverage for Anthropic's `docx`, `pdf`, `xlsx`, and
  `pptx` products.
- Do not copy or translate prompt text, code, examples, assets, test data, headings, ordering, or
  implementation instructions.
- Convert an observation into a Space requirement only when an independent product/safety reason
  exists.
- Record rejected and deferred capabilities so “reference” cannot silently turn into scope copying.
- Implementation authors use this document, public format/library documentation, and independently
  created fixtures. Proprietary Skill files are not vendored or used as implementation templates.

### Decisions

| Format | Abstract capability question                                                                                | Baseline state                                                                              | Space decision                                                                                                                                                                                                       | Independent reason                                                                                                                         |
| ------ | ----------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------ |
| DOCX   | Can users review changes through comments and a redline instead of receiving only a replaced document?      | Existing changes were detected but not authored.                                            | **Adopt with a narrow gate.** Add comment inventory, bounded comment add/reply, and a text-level redline output mode after the core create/edit path is stable. Complex field/table/section redlines remain blocked. | Contracts, policies, specifications, and collaborative reports require inspectable review semantics; silent replacement is not sufficient. |
| DOCX   | Is page geometry explicit and is every generated page visually reviewed?                                    | Render review existed; page geometry was implicit in some flows.                            | **Strengthen.** Require explicit paper size, orientation, margins, section breaks, and a renderer receipt.                                                                                                           | Cross-platform pagination changes when geometry is left to library defaults.                                                               |
| PDF    | Can a user protect a PDF or open one with a supplied password without implying password cracking?           | Encryption was detected; mutation was out of scope.                                         | **Adopt.** Support user-supplied-password open, decrypt-to-copy, and encrypt-copy operations. Never recover or bypass an unknown password.                                                                           | Password-protected delivery and authorized access are ordinary PDF workflows with a clear consent boundary.                                |
| PDF    | Can embedded page images be extracted and can OCR create a searchable copy rather than only returning text? | OCR/text classification existed; image extraction and searchable overlay were not explicit. | **Adopt as optional adapters.** Extract bounded images with provenance; when OCR exists, create a new searchable PDF that preserves the original page image and records per-page OCR status.                         | Scanned archives need reusable images and searchable delivery, not only chat text.                                                         |
| XLSX   | Does “formula checks passed” distinguish evaluation success from a semantically correct formula?            | Error scanning and optional recalculation existed.                                          | **Strengthen.** Add sampled dependency/range assertions and expected-value fixtures; a zero-error recalculation alone is insufficient.                                                                               | A wrong but valid cell range produces a clean workbook with incorrect business results.                                                    |
| XLSX   | Are calculation-engine compatibility and cached values visible?                                             | Optional recalculation and stale-cache warnings existed.                                    | **Strengthen.** Record engine/version, unsupported function families, cached-value freshness, and whether save/recalculation may damage external-link semantics.                                                     | Excel-compatible engines differ; users need to know what was actually evaluated.                                                           |
| XLSX   | Should one universal Skill impose financial-model colors and conventions?                                   | Not present.                                                                                | **Reject from the general Skill.** A future finance profile may add such conventions when explicitly selected.                                                                                                       | Domain styling is not a file-format correctness requirement and would overwrite existing workbook conventions.                             |
| PPTX   | Are POTX templates, layout inventory, and placeholder cleanup first-class?                                  | Template-first PPTX existed; POTX and placeholder residue were not explicit.                | **Adopt.** Inspect `.potx`, preserve template semantics, produce a layout/slide inventory, and scan final content for unresolved placeholders.                                                                       | Native template reuse fails visibly when placeholders or incompatible layouts survive.                                                     |
| PPTX   | Can the verifier distinguish real overflow from renderer font substitution?                                 | Missing fonts and overflow were reported.                                                   | **Strengthen.** Record font substitutions and treat text-fit results as approximate when metrics differ; require layout slack or a compatible font.                                                                  | A preview rendered with substitute fonts can create both false failures and false confidence.                                              |
| PPTX   | Must comments be modified in v0.1.61?                                                                       | Comments were not a write capability.                                                       | **Defer modification; adopt read-only inventory.** Preserve comments and report their presence.                                                                                                                      | Editing modern PowerPoint comments safely is not required for the first native-delivery milestone and must not risk relationship loss.     |
| All    | Should product-specific library choices, command sequences, and formatting recipes be mirrored?             | Space chose its own typed host boundary.                                                    | **Reject.**                                                                                                                                                                                                          | Toolchain and prompt expression are implementation-specific; Space must remain portable, governed, and independently authored.             |

### Audit additions to the v0.1.61 gate

> Product decision recorded 2026-07-27: the cross-review recommendation for an OS-enforced
> document/native-tool sandbox is **deferred**, not adopted as a v0.1.61 gate. F138 owns that
> post-v0.5.x hardening. F137 ships complete core format workflows with bounded Workers/child
> processes and discloses the residual native-engine risk; none of those controls may be labeled an
> OS security sandbox.

- DOCX fixtures include plain edits, comment threads, simple text redlines, and complex-redline cases
  that must block instead of corrupting the package. `.dotx` template input and accepting a bounded
  redline into a clean copy preserve author/time provenance and prove unrelated content unchanged.
- PDF fixtures include known-password open/encrypt/decrypt, image extraction, and optional searchable
  OCR output; unknown-password and signature-invalidating cases remain blocked or warning-gated.
  Non-AcroForm overlays, read-only annotation inventory, and verified redaction are separate paths.
- XLSX receipts separate structural validity, formula evaluation, semantic sample checks, engine
  compatibility, and cached-value freshness. `.xltx` and CSV/TSV fidelity cover encoding,
  delimiter/quotes/newlines, leading zeros, dates, nulls, and formula injection.
- PPTX fixtures include `.potx`, unresolved placeholders, font substitution, and comments that must
  survive an unrelated edit.
- Cross-format fixtures distinguish inherited source defects from introduced regressions and apply
  resource budgets to PDF objects/pages/pixels, OOXML expansion, image decode, OCR, and external
  process time.
- These additions do not change the independent package shape, routing, ownership, or implementation
  source policy.

---

## FEATURE_137: Chinese-First Native DOCX/PDF/XLSX/PPTX Builtin Skills

### 1. 需求概述

用户需要四个可随 Space 安装包分发、无需另行安装、以中文为主要操作语言的文档 Skill。
它们必须覆盖常见的创建、读取、修改和验证工作，同时明确区分：

- 文本或数据理解，由模型完成；
- 文件结构变换、渲染和校验，由确定性工具完成；
- 文件写入、覆盖、回滚和交付，由 Space 的 F114 边界完成；
- 高级能力不可用时，返回可操作的降级说明，而不是假装完成。

四个 Skill 均为 KodaX Space 独立作品。名称使用通用文件扩展名只是为了准确触发，不表示
兼容或派生自任何第三方 Skill。

### 2. 影响范围

#### Space-owned changes

- 新增四个 builtin 目录、来源/许可证记录、文件完整性锁和包内 smoke 检查。
- 扩展 F109 的文档结构与 writer/transformer 能力，或引入一个内部
  `DocumentProcessingService` 作为四种格式的统一安全入口。
- 新增可取消的持久 Document Job、job 级 staging/commit、受限 Worker/独立子进程执行与孤儿
  临时目录清理。解析和 Office/OCR 工作不占用 Electron main；v0.1.61 不实现或宣称 OS
  安全沙箱。
- 复用 F114 delivery/checkpoint、F098 policy/audit 和 F131 File Viewer；使用独立、版本锁定
  的 render service 产生验证证据，File Viewer 只展示结果。
- 为生成/编辑任务增加结构、语义和视觉验证 receipt。
- 增加中英文触发、格式冲突和 Skill 组合路由测试。
- 为 Space 自有 builtin 增加 first-party authoring source 与可复现快照规则；不把
  `resources/builtin-skills/` 直接当作手工作者目录。

#### KodaX-owned boundary

- 只使用现有公开 Skill plugin 注册和 `registerTool`/daemon catalog 能力。
- 不修改 KodaX SDK，不把格式处理逻辑放入 Runtime。
- 如果 daemon 侧不能发现安装器拥有的 builtin，沿用 F135 已完成的 Space/daemon catalog
  合并，不创造新的 Skill 存储层。
- OCR、Office render/recalculation 等长任务由 `registerTool` 启动 Space-owned Document
  Job，并由 Space 提供查询、进度和取消；不把长进程阻塞在一次 daemon host-tool 调用中。
- 只有未来必须让同一次 host-tool 调用原生承载 cancellation/deadline/progress 时，才向
  KodaX 提出 SDK 增强；这不是 v0.1.61 的前置依赖。

#### Out of scope

- 完整桌面 Office 编辑器；
- 协同编辑、云端 Office API、Google Workspace API；
- VBA/宏的生成或修改；
- 数字签名创建、可信验证或签名后修改；
- PDF XFA、DRM 绕过、密码破解；
- `.doc`/`.ppt` 旧二进制格式直接编辑；未来只能通过受控 adapter 转换到新副本；
- 损坏 PDF 的自动 repair-to-copy、线性化或体积优化；可作为后续显式 adapter，不能在
  v0.1.61 静默重写；
- 任意 OLE/嵌入程序执行；
- 对任意复杂模板承诺像素级无损编辑；
- 自动安装 LibreOffice、OCR、字体、Python 或 npm 依赖；
- 用四个新 Skill 替换 `huashu-design` 或 `frontend-slides`。
- 在 F129 内另建一套与 F137 竞争的 PPTX format service；F129 必须复用 F137 primitive，
  只增加项目/Studio、高保真创作流程和目标 Office 兼容认证。
- OS 强制的文件系统/网络/进程/native-resource 沙箱；由 F138 在 `post-v0.5.x` 独立补强，
  不作为本版本功能门禁。

#### v0.1.61 GA scope and capability gates

- GA 公共底座：四格式有界 inspect、active-content/privacy inventory、不可信证据边界、
  受限 Worker/独立进程 Job、原子多产物交付和真实 validation receipt。
- GA 功能面覆盖 3.1-3.4 声明的四格式核心工作流，不以“每格式一个纵切”代替完整实现：
  DOCX 创建/模板/有界编辑/批注与简单红线，PDF 提取/页面操作/表单/口令/图片/OCR/生成/
  验证型脱敏，XLSX 创建/模板/清洗/公式/图表/CSV/TSV/重算，PPTX 模板感知创建/编辑/
  合并拆分/重排/备注/基础图表和格式验证。
- OCR、Office-compatible 重算和 Office render 是 adapter-dependent 功能：有合格 adapter、
  平台矩阵和功能/保真 fixture 就开放；缺少 adapter 时只对该操作给出真实降级，不把整套
  Skill 或其他功能隐藏。F138 不在任何功能的前置条件中。
- 复杂 field/table/section redline、XFA、VBA/宏修改、任意 OLE/SmartArt/复杂动画编辑和
  F129 的 Presentation Project/Studio、创意迭代、目标 Office `verified` 标签仍按明确边界
  延后，不属于“格式 Skill 功能完备”的虚假承诺。

### 3. 技术方案

#### 3.1 `docx` Skill

**触发边界**

- DOCX 是主要输入或主要交付物时触发：新建报告/合同/规范、修改现有 Word 文件、套用
  `.docx`/`.dotx` 模板、重排章节、插入表格/图片、生成目录、导出审核版。
- 仅需要聊天回答、纯 Markdown、PDF 操作或电子表格时不触发。

**核心能力**

- 读取段落、标题层级、列表、表格、页眉页脚、脚注/尾注、批注标记、超链接、图片和
  文档属性的有界结构摘要。
- 从结构化内容创建 DOCX；支持中英文字体回退、标题样式、编号/项目符号、表格、图片、
  分页、页眉页脚、页码、目录域、来源附录。
- 对现有文件执行有界修改：替换/插入内容、移动章节、调整样式、更新表格、替换图片；
  未修改的未知 OOXML parts 默认原样保留。
- 识别修订、批注、宏、嵌入对象和外部关系；支持有界批注读取/添加/回复和简单文本级
  红线。复杂 field/table/section redline、宏和嵌入对象无法安全保真时拒绝或输出副本并
  显式警告。
- 简单红线只覆盖明确的 paragraph/run/comment range 和作者/时间元数据；可选择“保留修订
  副本”或“接受修订后的干净副本”。干净副本保留修订来源证据并证明没有未跟踪修改；
  超出支持边界时只能交付独立视觉 diff，不冒充 OOXML Track Changes。
- 明确设置纸张尺寸、方向、页边距和 section geometry，不依赖 writer 默认值。
- 可选生成 PDF 审核副本，但 PDF 由 `pdf` Skill/共享渲染适配器验证。

**验证**

- OOXML 包、关系和必需 part 可打开；
- 文档结构计数与计划一致，关键文本存在；
- 所有页面完成渲染，无全空页、缺字占位、图片解码错误或明显越界；
- 模板编辑 fixture 验证未修改区域的 part/hash 或语义等价性；
- 既有缺陷与本次新增回归分别记录；新增回归会阻止交付，既有告警不会掩盖本次损坏；
- 声明目录/页码已更新时，必须由可用 Office-compatible renderer 实际更新，否则标记
  为“打开 Word 后更新域”。

**建议资源**

- `references/operations.md`：新建、模板编辑、结构修改、PDF 审核副本的选择。
- `references/validation.md`：OOXML、分页、字体、图片和保真检查。
- `references/limitations.md`：修订、宏、嵌入对象、复杂域和模板限制。
- 小型脚本仅负责可重复的 package inspection/diff；生成和写入走 typed host service。

#### 3.2 `pdf` Skill

**触发边界**

- PDF 是主要输入或交付物，或用户明确要求合并、拆分、旋转、裁剪、填写表单、加页码、
  添加水印、提取内容、OCR、生成或审阅 PDF 时触发。
- 只查看一个已能在 File Viewer 打开的 PDF 且不要求处理时，不额外启动工作流。

**核心能力**

- 检查页数、页面尺寸/方向、加密、表单、附件、书签、元数据、文本层、图片占比、签名
  字段、批注、page labels、named destinations、内部链接、tagged structure、
  JavaScript/launch actions 和损坏情况。批注至少提供页码、类型、位置和只读 inventory。
- 提取有位置依据的文本和表格；扫描件只在 OCR adapter 可用时 OCR，并逐页标记文本层、
  OCR 或失败来源。
- 合并、拆分、重排、旋转、裁剪、加页码、水印、书签和元数据；所有页码选择使用明确
  的一基用户页码并在内部规范化。每种页面操作显式声明 outlines、page labels、
  destinations、links、annotations、AcroForm、附件和 tagged structure 的保留/重映射/
  丢弃策略；合并表单检测同名字段冲突。
- 填写标准 AcroForm；不声称支持 XFA。对没有交互字段的扫描/平面表单，可在用户确认
  字段坐标后填写到新副本并全页渲染复核。扁平化必须由用户明确要求。
- 使用用户明确提供的口令打开 PDF，或创建新的加密/解密副本；不恢复、不猜测、不绕过
  未知口令。
- 有界提取嵌入图片并记录页码/object provenance；OCR adapter 可用时可创建保留原页面
  图像的可搜索 PDF 副本。
- 从受控文档结构生成带 CJK 字体、可搜索文本和来源附录的 PDF。
- 普通视觉遮盖与 `redact-verified` 是不同操作。后者必须完整重写文件并清除增量历史、
  未引用对象及相关 XObject、OCR 层、附件、批注、表单 appearance、书签/destination 和
  元数据，再复检 raw bytes、对象图、文本/OCR 层及渲染结果。图片区域无法证明内容级移除
  时，必须走明确的 destructive raster/redaction 路径或拒绝 `redaction-verified` 标签。
- 任何会使现有数字签名失效的变换必须在执行前警告。

**验证**

- PDF parser 可重新打开，页数、顺序、尺寸和预期操作一致；
- 所有输出页面渲染成功；
- 生成型 PDF 的关键文本可提取且 Unicode 正确；
- 表单写入后字段值/appearance 与坐标副本可复核；只有明确目标和受支持内容类型通过
  完整取证式检查后才出具 `redaction-verified`，普通遮盖不得声称“不可恢复”；
- OCR 输出保留逐页置信度/来源警告，不把 OCR 猜测当作原始文本。

**建议资源**

- `references/operations.md`：提取、页面变换、表单、OCR、生成和脱敏选择。
- `references/validation.md`：重开、渲染、文本复检、签名/active-content 检查。
- `references/limitations.md`：XFA、DRM、签名、复杂矢量重排和 OCR 限制。
- 小型脚本可处理 deterministic page operations；不得执行 PDF 内脚本或附件。

#### 3.3 `xlsx` Skill

**触发边界**

- XLSX/XLSM/XLTX/CSV/TSV 是主要输入或主要交付物，用户要求建表、清洗、计算、格式化、
  图表、公式、合并数据或修改工作簿时触发。`.xltx` 只作为模板输入，输出为新工作簿。
- 主要交付物是报告、代码、数据库或网页时不因出现一张表就触发。

**核心能力**

- 有界读取工作表、已用区域、单元格类型、公式、名称、表格、筛选、冻结窗格、合并
  单元格、条件格式、数据验证、图表和外部关系摘要。
- 新建工作簿时保持输入/公式/结果/说明分区，使用真正的日期、数字、百分比和货币类型，
  不用格式化字符串冒充数值。
- 修改现有工作簿时先识别其样式和约定；默认沿用现有字体、颜色、数字格式、公式模式和
  工作表命名。
- Inspection 标记超过 15 位的标识符、前导零、科学计数法风险、工作簿 1900/1904 日期
  系统、日期序列/时区/locale、空值与空字符串。默认保留原始类型、serial 和显示格式；
  无法无损判断时要求显式列类型。
- 公式必须写入公式而不是硬编码计算结果；跨工作表引用、绝对/相对地址和区域扩展必须
  经结构检查。
- 识别并阻断未授权的外部工作簿链接、DDE、远程数据连接和危险公式；CSV/TSV 导出防止
  公式注入。CSV/TSV 转换显式记录编码、BOM、分隔符、引号、嵌入换行、前导零、日期
  解析、null/empty 和 locale，不能以“能打开”代替 round-trip 保真。
- XLSM 只允许“宏原样保留但不执行”的路径；无法证明保留时拒绝写回。
- 需要已计算结果时，使用可用的 Office-compatible recalculation adapter；没有计算
  引擎时保留公式并明确说明缓存值可能未更新。重算只作用于副本，保存前后比较
  chart/pivot/query/table/name/shared/array formula 与外部关系 inventory；非目标结构被
  删除、降级或改写时阻止发布。
- validation receipt 记录计算引擎和版本、缓存值新鲜度、不支持的函数族，以及代表性
  公式的 dependency/range/value 复核；“没有错误值”不能单独证明公式语义正确。

**验证**

- 输出可重新打开，工作表、行列、类型、公式和名称符合计划；
- 公式错误扫描覆盖 `#REF!`、`#DIV/0!`、`#VALUE!`、`#NAME?`、`#N/A` 等错误值；
- 当重算 adapter 可用时，打开、重算、保存、再读取后零新增公式错误；
- 长 ID、前导零、数值极值、1900/1904 日期、时区、locale CSV fixture 的原值和显示语义
  round-trip 一致；
- 代表性工作表渲染并检查截断、隐藏列误用、极端列宽、不可读格式和空白打印区；
- 更新现有 fixture 时验证未目标化公式和工作表不发生意外变化；
- 图表或透视表无法安全修改时保留并警告，不静默删除。

**建议资源**

- `references/operations.md`：新建、清洗、公式、格式、图表和保真修改路径。
- `references/validation.md`：类型、公式、错误值、重算、外部链接和视觉检查。
- `references/limitations.md`：宏、数据连接、透视表、数组公式和计算引擎限制。
- deterministic helpers 负责工作簿检查、公式错误扫描和受控导出。

#### 3.4 `pptx` Skill

**触发边界**

- 原生 PPTX 是主要输入或主要交付物，用户要求创建、读取、模板套用、修改、合并、拆分、
  重排、替换内容、添加讲者备注或验证可编辑演示时触发。
- HTML 动画演示由 `frontend-slides` 负责；视觉方向探索由 `huashu-design` 负责。用户既
  要求高质量视觉又要求原生 PPTX 时，可先形成视觉规格，再由 `pptx` format service
  负责原生交付；F129 上线后可把该过程纳入持久项目和目标 Office 验证。

**核心能力**

- 读取页面尺寸、母版、版式、主题、占位符、文本、图片、表格、图表、备注、隐藏页、
  批注、超链接、媒体和外部关系的有界摘要。
- 接受 `.potx` 作为模板输入，生成 layout/slide inventory，并在交付前扫描未替换占位符。
- 新建原生可编辑 PPTX，优先使用用户模板；没有模板时使用少量 Space-owned 中性主题，
  不把 HTML 截图冒充可编辑幻灯片。
- 支持标题、正文、形状、图片、表格、基础图表、备注、页码和来源页；图片保持比例并
  记录裁剪策略。
- 修改现有 deck 时复用母版/版式/主题和 placeholder，避免把模板页扁平化成自由形状；
  合并/拆分/reorder 时修复关系 ID、备注、媒体和 slide references。
- 动画、SmartArt、复杂图表、嵌入媒体和 OLE 无法可靠编辑时保留原 part 并警告；不得
  静默丢失。

**验证**

- OOXML 结构、关系、slide/master/layout/theme 引用完整；
- 所有幻灯片均可渲染，页面数量和顺序正确；
- 检查元素越界、重叠风险、过小字号、文本溢出、低分辨率图片、缺失字体和对比度警告；
- renderer 使用替代字体时记录 font substitution，并把 text-fit 结果标为近似；需改用
  metric-compatible 字体或保留足够 layout slack 后才能通过；
- 模板 fixture 验证母版、版式、主题、placeholder 和未修改页保持；
- 讲者备注、来源页和可编辑对象在重新解析后存在；
- 通过结构、关系、原生对象和全页渲染检查后可声明“格式验证的可编辑 PPTX”；只有 F129
  的目标 Office 引擎、平台兼容矩阵和逐对象 receipt 才能升级为 `PowerPoint verified` 或
  对应目标 Office 的 verified 标签。

**建议资源**

- `references/operations.md`：新建、模板优先、内容替换、合并拆分和组合路由。
- `references/validation.md`：OOXML 关系、溢出、字体、图片和 render review。
- `references/limitations.md`：动画、SmartArt、复杂图表、媒体、OLE 和模板保真边界。
- deterministic helpers 负责 package inspection、关系修复、slide render manifest 和
  几何检查；视觉创意不硬编码在脚本中。

#### 3.5 依赖选择与打包原则

- 每个新增依赖必须允许商业复制、修改和再分发，并固定版本、许可证 hash 和来源。
- 优先使用当前包内已有的 `jszip`、`xlsx`、`pdfjs-dist`、`mammoth` 和 Space 预览代码，
  但不得因为“已有”就假设它适合写回或高保真编辑。
- 新 writer/transformer 通过代表性 fixture 证明保真后才能替换 F109 路径。
- 可选 LibreOffice/OCR adapter 必须是能力探测，不是安装时偷偷下载的依赖。
- Skill 自身和其脚本纳入 F135 的 path/size/SHA-256 精确锁、symlink/secret/link
  检查和 packaged smoke。
- F135 增加 first-party local-source 模式：作者源位于独立目录，构建以仓库 commit、
  source tree hash、许可证 hash 和生成规则产出只读 vendored snapshot；
  `resources/builtin-skills/` 仅为生成物与打包输入，不接受直接手工编辑。
- 四个 Skill 均使用 Space 自有许可证与来源声明；不包含第三方 proprietary Skill
  许可证文本或材料。

### 4. 接口契约

#### Skill metadata

四个 Skill 的 frontmatter 只保留 `name` 和 `description`。`description` 同时包含中文
触发语义和通用扩展名，正文不重复“何时触发”。

```yaml
name: docx
description: 处理以 DOCX 为主要输入或输出的专业文档创建、检查、修改和验证任务；当用户要求 Word 报告、合同、规范、模板编辑、表格图片或 DOCX/PDF 审核交付时使用。
```

其余三个分别以 PDF 页面/表单/提取、XLSX 工作簿/公式/数据、PPTX 原生可编辑演示为主要
触发语义。实际 frontmatter 在实现阶段通过 Skill validator 和中英文触发 fixture
定稿。

#### Operation result

所有格式操作返回一个机器可解析结果和一个简短用户摘要：

```ts
interface DocumentJobResult {
  readonly jobId: string;
  readonly status:
    | 'queued'
    | 'running'
    | 'completed'
    | 'completed-with-warnings'
    | 'blocked'
    | 'cancelled'
    | 'failed';
  readonly outputs: readonly DocumentOutputManifestEntry[];
  readonly progress?: {
    readonly phase: string;
    readonly completed: number;
    readonly total?: number;
  };
  readonly warnings: readonly string[];
  readonly nextActions: readonly string[];
}
```

- `completed` 要求所有确定性 postcondition 和适用的结构/视觉检查通过；抽样或模型审阅
  只按真实覆盖率呈现，不能升级为“全文语义正确”。
- 缺少 renderer、OCR 或 recalculation engine 时只能是 `completed-with-warnings`，除非
  该能力与本次任务无关。
- `blocked` 用于密码、宏/签名保真、路径权限或 unsupported active content 等可解释边界。
- `cancelled` 会终止整个隔离进程树并清理 staging；不会注册半套 outputs。
- 工具错误不回显任意文件正文、绝对敏感路径或未清洗的 parser stack。

#### Validation ownership

- Skill 决定需要哪些检查；
- `DocumentProcessingService` 执行确定性检查；
- F114 注册输出；
- 独立、版本锁定的 render service 产生视觉证据，File Viewer 只展示文件和预览；
- validation receipt 是结果旁的证据，不进入模型自报的自由文本真相。

### 5. 实现步骤

1. **冻结独立设计与来源记录**：评审本文件，记录公开规范/库候选与许可证，不读取任何
   proprietary Skill 表达作为实现输入。
2. **建立 fixture corpus**：独立创建中英双语、正常/畸形、宏/外链/签名、扫描件、模板、
   公式、图表、复杂关系、视觉溢出、提示注入、超时、崩溃和资源上限样本。
3. **先建可靠执行底座**：复用受限 Worker，增加独立子进程 adapter、私有 staging、输入/
   输出预算、整树取消、孤儿清理和无宏/无链接更新的隔离 profile；不等待 F138。
4. **定义共享服务与 Job**：实现 typed inputs/actions/targets、inspect/execute/validate/
   render/deliver、幂等 CAS、多输出 manifest、进度与取消，接入 F098/F114；F131 只展示。
5. **先做 read-only inspection**：四种格式先实现 bytes sniff、limits、active-content/
   privacy inventory、不可信 evidence delimiter、覆盖率和结构摘要。
6. **完成四格式功能面**：按 inspect/create/edit/transform/validate 顺序实现 3.1-3.4 的
   全部核心能力；adapter-dependent 路径用 capability probe 和真实降级处理，每步先写失败
   fixture，不以纵切样例代替 GA 功能。
7. **建立验证闭环**：结构重开、明确 postcondition、renderer receipt、基线缺陷/新增回归、
   截断范围和失败/警告降级；不可把抽样包装成全文件语义通过。
8. **编写中文 Skill**：使用 `skill-creator` 初始化四个作者源目录，保持 SKILL.md 精简，
   把格式细节放入一层 references。
9. **处理 Skill 重叠**：增加 `pptx`/F129/`frontend-slides`/`huashu-design` 路由测试；不新增
   `canvas-design` 或 `frontend-design` builtin，不创建第二套 PPTX writer/validator。
10. **纳入 F135 供应链**：实现 first-party local-source snapshot，自有来源条目、许可证
    hash、完整性锁、打包和干净 profile discovery。
11. **前向验证**：以真实用户任务运行四个 Skill，检查产物而不是只检查提示词；失败样本
    反推 Skill/host contract。
12. **能力缺口审计**：独立基线冻结后，仅把第三方产品观察转换成抽象能力问题；任何新增
    条目都要有公开规范/产品需求的独立理由，并在本文件的审计附录记录“采用/拒绝/延后”。
13. **发布门禁**：typecheck、lint、focused/full tests、build smoke、packaged skill byte
    integrity、四格式 artifact smoke，以及每个发行平台的 renderer/font/OCR capability
    matrix、结构/CJK/artifact smoke 全部通过；Windows portable 另做人测。

### 6. 验收标准

#### Cross-format

- 干净 profile 能发现且调用 `docx`、`pdf`、`xlsx`、`pptx`，无需复制到用户 Skill 目录。
- 用户/项目同名 Skill precedence 仍遵守 KodaX 规则；slash completion 不重复。
- 中英文自然语言触发正确；仅提到扩展名但主要交付物属于其他格式时不误触发。
- Partner 全流程不获得 raw shell/package install 权限。
- 每个输出都有 hash、delivery、producer、输入来源和 validation receipt。
- ZIP bomb、路径穿越、XXE、外部关系、公式注入、PDF active actions 和宏 fixture 均
  fail closed 或产生明确警告。
- ZIP/XML/PDF 解析上限、Worker heap、Office modal hang、timeout/cancel 和 adapter crash
  fixture 都会有界失败，并在取消后清理整个已启动进程树；本版本不声称阻止 OS 级逃逸、
  native exploit 或所有网络访问。
- 正文、隐藏内容、批注、备注、公式字符串、OCR 和跨格式提示注入 fixture 不能改变工具
  权限、输出目标、网络策略或用户任务目标。
- 并发修改、重复请求、Worker crash、磁盘写满、delivery 注册失败和重启恢复 fixture 不会
  发布半套产物、覆盖新输入或重复注册；成功/失败/取消后均无明文 job 临时物残留。
- `inspect-only`、`sanitized-copy`、`authorized-fidelity-copy` 的 active-content 状态明确；
  后者需确认且不能冒充普通安全输出。
- receipt 展示检查覆盖率、截断原因、未检查范围、既有缺陷和新增回归；抽样/模型复核不显示
  为全文确定性语义通过。
- 作者/公司属性、custom XML、历史批注/修订、隐藏页/表/行列、讲者备注、附件、EXIF、
  缩略图和外链进入统一隐私清单；标题/语言/表头/alt text/阅读顺序/tagged PDF 缺失至少
  产生 accessibility warning。
- Windows、macOS x64/arm64 和 Linux 的实际发包目标都有 capability matrix；缺少
  release-qualified renderer/font/OCR 时隐藏对应 mutation/verified capability。
- 不存在“未运行的验证显示为通过”或“仅生成字节即宣称专业交付”。

#### DOCX

- 创建和模板编辑 fixture 覆盖中英文字体、标题、列表、表格、图片、页眉页脚、分页和
  来源附录，并包含 `.dotx` 模板输入。
- 输出可被 parser 和 Office-compatible renderer 打开；全页渲染无空白/缺字/严重溢出。
- 简单文本红线和批注可重新解析；接受修订的干净副本保留作者/时间来源并证明无未跟踪
  修改；复杂红线被明确阻止。
- 未支持的宏、OLE 或复杂域不会被静默移除。

#### PDF

- 文本型、扫描型、混合型、表单型、加密和损坏 fixture 得到真实分类。
- merge/split/reorder/rotate/crop/page-number/watermark/form-fill 输出页数和顺序正确且
  全页可渲染；书签、page labels、destinations、内部链接、批注、表单、附件与 tagged
  structure 按声明的策略正确保留或重映射，同名字段冲突不会静默合并。
- 用户提供口令的 encrypt/decrypt fixture 可用；未知口令不会被猜测或绕过。
- 图片提取和可搜索 OCR fixture 保留页面/object provenance 与逐页 OCR 状态。
- 非交互式/扫描表单按确认坐标填写到新副本并通过全页复核。
- `redact-verified` fixture 经过完整重写，目标内容无法从 raw bytes、增量历史、对象图、
  文本/OCR 层、图片、附件、批注、表单、书签和元数据复检中恢复；未达到该门禁时只标记
  为普通遮盖。
- 数字签名或 XFA 限制在修改前可见。

#### XLSX

- 单元格类型、数字格式、日期、合并单元格、公式、名称、筛选、冻结窗格和基础图表
  fixture 可读取并按支持边界修改；`.xltx` 可作为模板输入。
- 可重算环境中零新增公式错误；不可重算环境中缓存值状态明确。
- 代表性公式通过 dependency/range/value 语义样本；receipt 记录计算引擎兼容性。
- 外链/DDE/危险公式和 CSV injection fixture 被阻断或安全转义。
- 长 ID、前导零、数值极值、1900/1904 日期、时区、空值/空字符串、编码和 locale CSV/
  TSV fixture 的原值与展示语义 round-trip 一致。
- 重算副本的非目标 chart/pivot/query/table/name/shared/array formula 与 external
  relationship inventory 无意外变化。
- XLSM 宏不是原样保留就是拒绝，绝不静默丢失。

#### PPTX

- 无模板创建、`.potx` template-first 创建/编辑、内容替换、合并/拆分/reorder fixture 均
  产生原生可编辑对象并保留未修改模板结构。
- slide/master/layout/theme/notes/media 关系完整，所有页可渲染。
- 越界、溢出、极小字号、低分辨率图片和缺失字体能产生验证结果。
- F137 receipt 明确标记 parser/format/render 验证范围，不冒充 `PowerPoint verified`；
  F129 后续复用同一 format service 增加目标 Office 兼容矩阵和逐对象 editability receipt。
- `pptx`、`frontend-slides`、`huashu-design` 在路由测试中各自拥有清晰主责，组合调用
  不复制整份内容或产生两个相互竞争的最终交付物。

#### Clean-room and licensing

- 发布树中不存在 Anthropic/OpenAI proprietary Skill 文件、摘录、翻译、脚本、素材或
  衍生测试数据。
- 每个依赖和资产都有允许再分发的许可证、固定版本/commit 和 hash。
- comparative audit 只包含抽象能力名称、Space 决策和独立理由；不得保存第三方原文。

## FEATURE_139: Semantic Motion Scroll Affordance and State Emphasis

### 1. 需求概述

KodaX Space 已经具备 Liquid Glass、Aurora、cursor spotlight、统一按钮 sheen、Reveal
入场动画和三档视觉质量控制，基础视觉语言完整。当前主要缺口不是更多装饰，而是密集工作
界面对以下事实表达不够明确：

- Task Dock 折叠区的标题行、箭头和相邻区块之间曾存在触发区域偏小或视觉间距大于实际
  hit target 的情况，用户难以判断整行是否可操作；
- 智能体、工作流、通知和引用文件等动态列表在新增、状态改变或发生滚动溢出时缺少边界
  反馈，容易被理解为静态内容；
- 当前选中区块、需要用户处理、运行中和失败状态主要依赖文字与局部颜色，快速扫视时
  层级不足；
- 已有玻璃、光晕与按钮 sheen 如果继续无差别叠加，会增加噪声和 GPU 成本，而不是提高
  可用性。

F139 以 React Bits 的交互模式作为行为级参考，独立实现适合桌面生产力工具的克制版本：

| 参考模式                                                                                                                         | Space 采用内容                                                        | Space 明确不采用                                       |
| -------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------- | ------------------------------------------------------ |
| [Animated List](https://reactbits.dev/components/animated-list)                                                                  | 新增/状态变化的短时淡入、滚动边缘提示，以及符合现有组件语义的键盘操作 | 对每次普通重渲染、流式 token 或历史回放重复播放动画    |
| [Gradual Blur](https://reactbits.dev/animations/gradual-blur)                                                                    | 8-16px 的非交互式滚动边缘渐隐                                         | 在滚动文字上堆叠多层实时 `backdrop-filter` blur        |
| [Line Sidebar](https://reactbits.dev/components/line-sidebar)                                                                    | 当前 Task Dock 区块的 2px 语义色 active rail 和最多 3px 的装饰性位移  | 编号导航、30px 跟随位移或任何会移动 hit target 的效果  |
| [Border Glow](https://reactbits.dev/components/border-glow)                                                                      | 等待输入、运行中、警告和失败等语义状态的低强度边框强调                | 普通卡片的持续彩虹 hover 或无状态依据的装饰 glow       |
| [Specular Button](https://reactbits.dev/components/specular-button) / [Count Up](https://reactbits.dev/text-animations/count-up) | 仅精修主操作高光和短时变化的真实指标                                  | 为所有按钮新增第二套 sheen，或让数字动画延迟真实值呈现 |

#### 目标

1. 所有可折叠标题行保持完整、连续且稳定的 32-36px hit target；箭头只是状态图标，不是
   唯一可点击区域。
2. 动态列表只在真实插入或用户可感知的状态变化时使用 120-180ms、最多 4px 的入场反馈。
3. 可滚动内容在顶部/底部存在更多内容时显示柔和边缘提示，并在到达边界后消失。
4. active rail 和 semantic glow 只表达真实 UI 状态；颜色沿用现有 accent、reasoning、
   warning、danger 和 success token。
5. 所有反馈服从现有视觉质量档位、`prefers-reduced-motion`、键盘访问和对比度要求。

#### 非目标

- 不引入全屏 WebGL 背景、Splash/Blob/Pixel Cursor、Tilted Card、Magnet、弹性缩放或
  其他会改变目标几何位置的效果；
- 不重做 KodaX Space 的信息架构、主题系统、玻璃材质或按钮基础样式；
- 不改变 Session、Runtime、Workflow、Agent、Notification、Artifact 或文件引用的状态
  所有权；
- 不增加 React Bits 运行时依赖，不复制其源码、CSS、示例或组件结构；外部页面仅作为
  行为级设计参考；
- 不把 P2 指标动效作为 0.1.37 发布阻塞项；P0/P1 的列表、滚动、选中和语义状态反馈才是
  GA 范围。

### 2. 影响范围

#### Renderer surfaces

- `apps/desktop/renderer/src/shell/RightSidebar.tsx`
  - 折叠标题整行 hit target；
  - Task Dock active rail；
  - Run、Workflow、Agent、Changes、Sources、Artifacts、Context 区块的列表入场与
    scroll-edge hint；
  - 引用文件容器的材质层级和边界提示。
- `apps/desktop/renderer/src/features/workflow/WorkflowPanel.tsx`
  - 工作流新增、阶段变化和选中状态的 bounded transition。
- `apps/desktop/renderer/src/shell/NotificationsSurface.tsx`
  - 新通知入场、等待处理/失败状态的 semantic emphasis。
- `apps/desktop/renderer/src/shell/ConversationStreamV2.tsx`
  - 仅在确有滚动溢出时提供边缘提示；流式 token 不进入 list-enter 动画。
- `apps/desktop/renderer/src/features/partner/PartnerRightSidebar.tsx`
  - 在复用同类列表/区块结构时使用相同语义，不建立 Partner 专用视觉分叉。
- `apps/desktop/renderer/src/components/Reveal.tsx`、
  `apps/desktop/renderer/src/components/Collapse.tsx` 和
  `apps/desktop/renderer/src/lib/motion.ts`
  - 扩展现有 motion gate 与可复用的短时 transition，不引入第二套动画策略。
- `apps/desktop/renderer/src/styles.css`
  - 增加 motion、scroll-edge、active rail 和 semantic glow token/utility；
  - 保留现有 button sheen、GlassAurora、spotlight 与 reduced-motion 规则。

#### Tests

- 扩展 `tests/e2e/right-sidebar-popouts.spec.ts`，覆盖完整标题行 hit target、折叠状态和
  active rail；
- 增加 focused renderer/unit tests，覆盖 scroll-edge 状态、stable-key 插入判定、
  semantic tone 映射和 motion quality gate；
- 增加键盘、`focus-visible`、reduced-motion、200% zoom 和窄 Task Dock 截图回归。

#### 不受影响

- Electron main、preload、IPC schema、SQLite、KodaX SDK/Runtime、Document Job 和
  delivery contract；
- Monaco、xterm、Windows title-bar controls、disabled controls 和已有显式
  `no-ix`/`no-button-sheen` opt-out；
- 任何业务状态机、权限判断、通知持久化或工作流调度逻辑。

### 3. 技术方案

#### 3.1 共享 motion token

在既有视觉质量 token 上定义一组窄用途变量，数值可在实现阶段微调，但不得突破以下
边界：

```css
:root {
  --motion-list-enter-duration: 160ms;
  --motion-list-enter-offset: 4px;
  --scroll-edge-hint-size: 12px;
  --section-active-rail-width: 2px;
  --semantic-glow-opacity: 0.28;
}
```

- 只动画 `opacity` 和合成层 `transform`；不动画 `height`、`width`、`top`、`left` 或
  hit target 几何尺寸。
- `q-minimal` 与 `prefers-reduced-motion: reduce` 退化为瞬时状态变化；balanced 使用
  默认数值；full 可提高装饰层细腻度，但不得扩大位移或持续时间。
- 装饰层一律 `pointer-events: none`，不能遮挡滚动条、文字选择、按钮或上下文菜单。

#### 3.2 动态列表反馈

- 复用/扩展 `Reveal` 的门控能力，新增列表 item 只在 stable key 第一次进入集合时播放
  `opacity: 0 -> 1` 和 `translateY(4px) -> 0`。
- 已存在 item 的状态变化允许一次不改变布局的 tone transition；普通父级重渲染、窗口
  resize、折叠后重新展开、虚拟化回收、历史恢复和流式 token 更新不得被误判为新 item。
- stagger 只用于同一批次真实插入，单项间隔不超过 24ms，总延迟不超过 120ms。
- 键盘行为沿用组件本身的 ARIA 模式：普通链接/按钮列表保持 Tab 顺序，只有真实
  `listbox`/menu 模式才使用方向键；不得为了模仿示例而劫持全局方向键。

#### 3.3 滚动边缘提示

- 滚动容器放置不可聚焦的 top/bottom sentinel，通过 `IntersectionObserver` 推导
  `canScrollBefore` 与 `canScrollAfter`，避免在每个 scroll frame 执行 React state 更新。
- 边缘提示优先使用 surface color 到 transparent 的渐变 overlay 或 mask；不能模糊实际
  文字，也不能覆盖 scrollbar hit area。
- hint 仅在 `scrollHeight > clientHeight` 且对应方向仍有内容时显示；到达边界、容器
  变为不可滚动、区块折叠或被卸载时立即隐藏。

#### 3.4 Active rail

- Task Dock 区块继续以完整标题行作为按钮，并保留 `aria-expanded` 和
  `aria-controls`。
- 当前通过 `requestTaskDockFocus`、键盘 focus 或用户点击明确定位的区块显示 2px active
  rail；仅打开但不是当前目标的区块不冒充 selected。
- rail 在区块之间切换时可以进行 120-160ms 的 opacity/translate transition；标题文字
  的视觉位移不超过 3px，布局盒和点击位置保持不变。

#### 3.5 Semantic glow

统一使用业务状态到视觉 tone 的封闭映射：

| 状态              | Tone      | 行为                                      |
| ----------------- | --------- | ----------------------------------------- |
| 正常/静止         | neutral   | 无 glow                                   |
| 正在运行/推理     | reasoning | 状态进入时轻扫一次，随后保持低亮边框      |
| 等待用户输入/授权 | warning   | 低频静态强调；不使用无限闪烁              |
| 成功完成          | success   | 最多一次短时确认，不永久发光              |
| 失败/阻塞         | danger    | 稳定边框与图标/文字共同表达，不能只靠颜色 |

- glow 不从 hover 猜测状态，必须由现有 view model/props 显式传入；
- 状态 intro 最长 320ms，不使用无限旋转渐变；
- `q-minimal`、reduced motion 和高频状态抖动时只切换静态 border color；
- 同一 viewport 同时出现的持续 glow 数量应有界，普通卡片不参与。

#### 3.6 主操作与指标精修

- 现有全局 button sheen 继续是唯一按钮交互底座。主操作可以提高 specular 对比度，但
  不能再包一层独立组件或改变尺寸。
- Count-up 仅用于上下文数量、变更数等短时变化且非安全关键的真实数值；持续时间不超过
  250ms，最终 DOM/accessible name 立即反映准确值，动画中的视觉插值不进入可访问名称。
- 权限、费用、token 上限、错误数量等决策关键值默认瞬时更新，不使用计数动画。

#### 3.7 性能与独立实现

- 不新增 React Bits、Framer Motion、GSAP 或 WebGL 依赖；使用现有 React、CSS、WAAPI
  和浏览器观察器能力。
- 不使用多层实时 blur、持续 pointermove React render 或全局每帧循环。现有
  `useSpotlight` 是唯一全局 cursor-following visual owner。
- 在开发/打包构建中记录 representative Task Dock 的 scripting、layout、paint 与 GPU
  基线；balanced 档空闲时不得因 F139 保持持续动画。
- 参考链接只记录抽象行为；实现、命名、测试和样式均从 Space 现有组件与 token 独立
  推导。

### 4. 接口契约

F139 不新增 IPC。Renderer 内部保持窄类型和显式语义：

```ts
type SemanticEmphasisTone = 'neutral' | 'reasoning' | 'warning' | 'success' | 'danger';

interface SemanticEmphasis {
  readonly tone: SemanticEmphasisTone;
  readonly active: boolean;
  readonly intro?: boolean;
}

interface ScrollEdgeState {
  readonly canScrollBefore: boolean;
  readonly canScrollAfter: boolean;
}

interface MotionListItem {
  readonly stableKey: string;
  readonly change: 'inserted' | 'status-changed' | 'stable';
}
```

DOM/CSS contract:

- `data-semantic-tone` 只能取上述封闭 tone，且必须与可见图标/文字状态一致；
- `data-scroll-before` / `data-scroll-after` 只控制非交互装饰层；
- `data-motion-change="inserted"` 仅存在一个动画周期，完成后回到 `stable`；
- 可折叠标题保持原生 `button`，具备 accessible name、`aria-expanded`、
  `aria-controls`、`focus-visible` 和完整行 hit target；
- active rail 不取代 DOM focus indicator；键盘 focus 始终拥有独立、对比充分的 outline；
- reduced motion 与 visual quality 是共享 policy，组件不能提供绕过它们的局部开关。

### 5. 实现步骤

1. **建立基线**：为 Right Sidebar、窄/宽 Task Dock、Workflow、Agent、Notifications、
   Context referenced files 和 Conversation overflow 采集 balanced/minimal 截图、键盘路径
   与空闲性能基线。
2. **固化 hit target**：统一折叠标题的 32-36px 完整行按钮，扩大 icon button 到既有桌面
   可用尺寸，保证相邻区块之间没有不可解释的 dead gap；补充 E2E hit-test。
3. **扩展 motion policy**：在 `motion.ts`/`styles.css` 中加入共享 token、stable-key
   insertion gate 和 reduced-motion/minimal fallback，避免各列表自行实现不同动画。
4. **落地 P0 列表反馈**：先覆盖 Agent、Workflow、Notifications 和 Context referenced
   files；验证折叠、重排、回放和流式更新不会重复入场。
5. **落地 P0 scroll-edge hint**：实现 sentinel 驱动的 top/bottom 状态，接入
   Conversation 和 Task Dock 中确有 overflow 的容器，确认 scrollbar/selection 不受影响。
6. **落地 P1 active rail**：将明确的 Task Dock focus request、点击与键盘定位映射到单一
   active section；打开状态与当前选中状态分离。
7. **落地 P1 semantic glow**：从现有 Agent/Workflow/Notification/Run view model 显式映射
   tone，覆盖 waiting/running/success/error，限制 intro 次数与同时发光数量。
8. **评估 P2 精修**：只在不复制全局 sheen 且指标语义真实时加入主操作 specular 与
   count-up；不满足条件则保持现状，不阻塞 GA。
9. **可访问性与本地化检查**：验证键盘、screen reader 名称、200% zoom、窄栏、中文/
   英文长文案、Windows 高对比和 reduced motion。
10. **回归与发布门禁**：运行 focused unit/E2E、renderer lint/typecheck、desktop build 和
    packaged smoke；比较 balanced 空闲性能及截图，确认没有新增持续动画或 hit target
    位移。

### 6. 验收标准

- Right Sidebar 每个可折叠标题的整行均可通过鼠标、触控板和键盘触发，连续 hit target
  高度不少于 32px；标题与箭头之间、相邻区块边界内不存在只有视觉 hover 但不可点击的
  dead zone。
- hover、focus、active、expanded 和 selected 语义可区分；active rail 不遮挡
  `focus-visible`，也不作为唯一状态表达。
- Agent、Workflow、Notifications 和引用文件的新 item 只入场一次；父级重渲染、折叠
  重开、resize、历史回放和流式 token 不会重复播放。
- list transition 在 balanced/full 下持续 120-180ms、位移不超过 4px；minimal 与
  reduced-motion 下瞬时完成，内容顺序和 hit target 从第一帧起正确。
- Conversation/Task Dock 的 top/bottom hint 只在对应方向确有更多内容时可见，到达边界
  后消失；hint 不模糊正文、不截获 pointer、不覆盖 scrollbar。
- Task Dock 同一时刻最多一个 active section；由 focus request 定位后 rail 与实际展开/
  滚动目标一致。
- running、waiting、success 和 error 使用封闭语义色映射；状态同时有文字或图标，不能
  只靠 glow 传达；普通卡片和无状态 hover 不显示 semantic glow。
- 页面空闲后不存在 F139 引入的持续 requestAnimationFrame、无限 CSS 动画或 pointermove
  React render；balanced 档 representative Task Dock 的 scripting/layout/paint 不出现
  可感知回归。
- 主操作仍只有一套 button sheen；如果启用 count-up，准确最终值和 accessible name
  立即可用，动画不用于权限、费用、上限或错误决策。
- 200% zoom、窄 Task Dock、中英文长文案、Windows 高对比、minimal/full/balanced 和
  `prefers-reduced-motion` 的自动/人工检查通过。
- 不新增 React Bits 或其他动画运行时依赖；产物中不存在复制的第三方组件源码、示例或
  CSS。
- focused tests、renderer lint/typecheck、desktop build、相关 E2E 与 packaged smoke
  全部通过，视觉基线明确记录 F139 的预期差异。

## FEATURE_146: Partner Plugin Library Extension

> Fork implementation: 2026-08-31 · Status: InProgress · Priority: High
> Canonical detailed plan: [Partner 插件库开发计划](v0.1.61-partner-plugin-library.md).
> Uses the existing unreleased v0.1.61 planning file. The current development checkout still
> reports Space 0.1.45 and KodaX 0.7.95; the first P8 build is the fork-only prerelease
> v0.1.61-p.1 with Partner library 0.9.0. Canonical v0.1.61 still waits for every F137/F139
> release gate. P8 does not change the SDK, those feature scopes, or mark upstream F096/F102
> implemented.

### 1. 需求概述

Partner 插件库作为可独立安装 / 禁用 / 卸载的 Space Extension，仅提供专家和连接器。
专家包装场景提示词与创建者可选配置的现有 Skill，Skill 仍由 Coder / Partner 共用，
不新增技能库、匹配、安装或调用机制。一会话一个持续专家、多个显式连接器。

#### P8 Problem Statement

当前 Partner 缺少与平台无关的“远端资源已完成 → 右侧原生网页”交付能力，具体连接器因此容易各自实现本地成果、待审核和页面打开逻辑。新增一个平台时还可能迫使 Partner 核心增加平台名称与 URL 特判。用户要求本轮只修改 Partner 通用能力；飞书、钉钉、腾讯文档等连接器和专家适配后续单独补充。

#### P8 Solution

Partner 增加 provider-neutral 的远端资源任务与打开协议。连接器适配器负责平台 API、账号/scope、写入风险和平台私有 URL/resourceId 校验；Partner 只接受适配器已经验证成功的 `{ providerId, resourceId, title, canonicalUrl }`，并在 hydration 之后把新成功信号恰好一次投影到右侧浏览器。canonicalUrl 必须是无凭据 HTTPS URL。右侧使用隔离且持久化的网页会话，因此平台官方登录页与登录后的真实资源可停留在同一标签；通用成功路径不要求生成本地 Artifact、进入 Pending review 或到 Results 二次查找。该能力只挂载 Partner，Coder 不接入。具体平台连接器、创建/回读 action 和专家 prompt 不在本轮范围。

### 2. 影响范围

新增受限扩展宿主和独立 UI 包；接入 Partner 导航 / 输入框、会话配置、MCP 会话范围、
远端提案及已有资料 / 待审核 / 成果卡片。完整代码证据和范围见详细计划第 2 节。

#### P8 Implementation Decisions — 影响范围

> 下列既有平台适配决策保留为后续 connector adapter 草案；凡涉及飞书 CLI、账号、scope、目录、read-back、manifest action 或专家 prompt 的条款，不属于本轮 Partner 通用能力验收。

- 扩展 Partner profile 的目标路由与验证契约；该步骤已经完成，范围仅限 Partner profile。
- 扩展共享 IPC schema 中的 Partner connector DTO、扩展宿主能力握手和连接器 store v2，但只有 Partner run adapter、飞书 service、Partner remote-record projection 与右侧浏览器消费新 action。
- 在现有飞书 CLI 受控适配、connection revision、scope hash、dispatch owner 和 task reconciliation 基础上增加 document-create task；不增加第二套连接器运行时或通用任务框架。
- 只在飞书平台专家、Partner profile 与 capability guide 中路由新建和既有资源修改。共享的 `feishu-office-suite` Skill 及其 lock/source 记录保持字节不变，不在 P8 中修改共享 Skill 的发现、安装、内容或执行机制。
- 历史 create proposal、receipt、Base task 和旧专家快照继续可读；新模型运行不再为低风险文档新建产生 create proposal。
- document-create task 本身同时是命令状态和经验证的结果记录，不另造要求 proposalId 的 legacy receipt。正常成功路径只产生该 task 与 browser target，不注册本地 Delivery / Artifact，也不要求用户进入 Pending review 或 Results；旧 receipt 仍只属于 proposal/apply 链。

### 3. 技术方案

隔离加载包内前端，宿主只提供版本化受限接口；不引入新 Agent 引擎或任意主进程插件。
每轮加载持久专家配置；连接器按账号、会话、文档范围授权。远端写入按风险路由：新建个人空间文档或在本轮已授权目录中新建文档可直接执行，既有文档修改仍先审后提交。
第一期专家允许零或一个明确 Skill 配置，复用现有单项执行规则，不修改共享 Skill 系统。
首个正式平台专家增加声明式两级 capabilityGuide；它只生成可编辑草稿，实际能力仍由同包连接器、账号、会话范围和宿主工具共同授权。
飞书 CLI 是 Space 宿主资源：正式安装包携带固定校验归档，连接时仅本地静默解包并进入官方网页；插件包不携带 executable，电脑是否安装飞书桌面客户端与连接器无关。

#### P8 Implementation Decisions — 执行模型

- 风险路由的最小单位是操作而不是平台：新建全新文档可直创；读取直接执行；既有文档追加 / 修改继续审核；删除、覆盖、移动、共享、权限和批量操作不支持。
- “随便写一份 / 你决定 / 自由发挥”授权 Partner 生成标题和正文并直接创建；只有 typed call 缺少无法安全推断的必要字段才追问。用户明确要求“先起草 / 暂不创建”时不得调用写工具。
- 可创建账号按当前 run 的 live binding、扩展状态、连接 revision 以及 `docx:document:create` 和 `docx:document:readonly` scope 计算。宿主把非敏感的 `PartnerDocumentCreateEligibility` 投影到可信 run context / UI，结构包含状态、枚举 reason、recoveryAction，以及多账号时来自 live bindings 的 `{ connectionId, accountLabel, connectionRevision }` 候选：0 个时不列出工具，并区分未连接、扩展禁用、需重授权、缺 create/read scope 或宿主需升级；1 个时可自动解析；多个时 action 保持不可执行，直到用户通过宿主账号选择器产生绑定 session、connectionId 和 connectionRevision 的选择记录。自然语言或模型参数不能伪造这份记录，binding / revision 改变后选择立即失效。
- task 状态固定为 `preparing → submitting → succeeded | failed | unknown | partial`。宿主在构建本轮 runtime 前生成不可由模型填写的 `turnExecutionId`；账号和目录解析完成后，再以 `(sessionId, turnExecutionId, resolvedConnectionId, connectionRevision, scopeHash, canonicalFolderTarget, normalizedTitleHash, normalizedContentHash)` 生成唯一 invocation key。同一轮相同调用返回同一 task，最多派发一次；同轮不同账号 / 目标 / 内容或新的明确用户轮次得到不同 key。恢复时，未进入 `submitting` 的遗留 `preparing` 可证明未派发并收敛为 `failed/not_dispatched`；死亡 owner 的 `submitting` 收敛为 `unknown`，两者都不自动重试。
- remote success 的唯一事实源是“严格创建回执 + 同账号 read-back”。现有 `FeishuWriteResult` 不含标题，因此 task 内部 postcondition reader 只可对本 task 严格 create receipt 返回且 documentId 一致的 exact canonical URL 执行验证读取，复用同一 OAuth identity 与 readonly scope；它不要求该新 URL 预先存在于 `binding.documents`，也不注册 RemoteSource / Materials 或扩张后续模型读取权限。read-back 的 title 取第一个非空 Markdown 行并去掉 H1 标记；title 与 body 都先 NFC、CRLF→LF、去行尾空白和单个结尾换行，body 再移除该 title 行后与规范化请求逐字比较，保留大小写和正文内部空格。派发前失败固定为 `failed`；派发后没有可信 documentId + URL 为 `unknown`；已有可信身份但 read-back 或任一后置条件不完整 / 不匹配为 `partial`；全部匹配才是 `succeeded`。非成功状态都不得重试或自动打开。固定输入 / read-back 样例是测试 oracle，不能调用生产 comparator 生成期望值。
- 当前官方 CLI create receipt / read-back 不返回 parent metadata，所以 P8 不宣称“远端再次读取并验证目录”。目录事实源是宿主在派发前记录的 canonical target、当前 binding/scope 重验，以及不可由模型改写的固定 CLI argv：个人空间必须是 `--parent-position my_library`，显式目录必须是已授权 folder token 的 `--parent-token`。自动验收逐字检查该 argv 与 task target，真实账号验收再核对飞书页面位置；未来若官方 CLI 提供 parent metadata，再把它升级为结构化后置条件。
- 连接器 OAuth 与右侧网页 cookie 独立：前者授权 API 创建，后者只影响用户能否查看原生页面。browser target 只接收宿主验证过的 URL，不获得 token、Node 或主 renderer IPC。
- 显式飞书目标不可用时，Partner给出启用 / 连接 / 重授权 / 目录授权动作并停止；不得静默降级成本地文件、个人空间中的不同目录或普通网页操作。
- Partner-only 归属落在三层：工具只由 `createPartnerConnectorRunRuntime` 在 Partner run 内临时物化且不进入 SDK 全局注册表；Partner connector service 的写入口再次 `assertContext(surface === 'partner')`；新 task opener 只挂载在 `PartnerWorkspace`。P8 不修改任何 Coder 专属提示词、运行时、界面、业务逻辑或测试文件。

### 4. 接口契约

仅五组：扩展生命周期、UI 挂载、会话配置、MCP 连接与调用、文档与审核。
归属、权限、凭据和写入提交由主进程校验；无独立 Skill 解析服务。
字段与错误边界见详细计划第 4 节；已实现阶段的 typed IPC 与持久化 seam 记录于第 5 节。

#### P8 Implementation Decisions — 接口契约

- 模型可见工具名为 `partner_feishu_document_create`。输入只有可选 folderUrl、title 和有界 content，不接受 connectionId、账号选择凭证、profile、token、invocation key、任意 CLI argv、任意 URL 或 shell 文本；宿主只从唯一 eligible binding 或当前可信选择记录解析账号。
- 首个飞书工具只在 Partner、非 Plan、宿主与扩展均协商通用 `partnerNativeDocumentDeliveryV1`，飞书 provider action 已启用，且当前 run 至少存在一个同时满足 create/read scope 的可用飞书绑定时列出。调用时再次校验 live binding、扩展启用状态、revision 和 action capability；列出后发生撤权 / revision 变化时在派发前返回结构化可恢复失败。显式 folderUrl 必须等于本轮授权的 create folder，省略时才使用个人空间。后续钉钉、腾讯文档由各自受控 provider action 接入同一交付能力。
- service 返回 provider-neutral `PartnerNativeDocumentTask`，它就是本次 command/result 的唯一持久记录，不生成 `PartnerRemoteReceipt`。只有 `succeeded` 可含 provider、resourceId、read-back 得到的真实 title 与 canonicalUrl；非成功状态不得带可自动打开的资源。`unknown` / `partial` 的用户文案明确要求人工核对且不重试。
- `PartnerRemoteRecords` 增加 documentTasks 摘要；store 每次相关状态转换递增宿主所有的持久化 revision，并发出至少包含 revision 与 documentTaskId 的 changed signal。renderer 在完成初始 hydration 后，只把新观察到的首次 `succeeded` 转换投影为 browser target；历史加载、刷新和后续终态更新不得重复打开。
- 新的 provider-neutral direct-task opener 与现有处理 legacy create proposal receipt 的 `PartnerDocumentAutoOpener` 并存。两者都以 provider + resourceId（由 canonical URL 复核）生成相同的稳定 resourceKey，而不是 taskId/proposalId，并交给 Partner detail workspace 的同键替换 reducer；即使 direct task 与 legacy receipt ID 不同，同一资源也只保留一个右侧标签。opener 不识别或模拟平台登录态，只打开受支持平台经适配器验证的 HTTPS URL；飞书、钉钉和腾讯文档在未登录时分别显示自己的官方登录页。direct task 永远不伪造 legacy receipt。
- `partner_connector_propose`、所有新的 proposal mutation IPC input 和 `service.propose` input 都改为 append-only。只有持久化 record/output 联合类型以及按既有 proposalId 执行的 list/get/apply/reject 继续接受历史 `create | append`；升级后不存在任何可新造 legacy create proposal 的 public/service/model mutation seam，也不能为了隐藏模型入口而破坏历史记录解析。
- store 从严格 v1 单向、原子迁移到 v2，新增 documentTasks、唯一 invocation key 与单调 record revision；v1 记录迁移后保持原值且补默认字段。回退方式是关闭 action 并继续读取历史，不承诺已写 v2 数据后的旧二进制降级。
- 首个最小发布组合是 fork prerelease v0.1.61-p.1 host 与 Partner library 0.9.0；canonical v0.1.61 必须等待 F137/F139 和该版本的所有 release gate。运行时不比较精确 SemVer，而以 `hostApiVersion >= 4`、0.9.0 manifest 的受限 `requiredHostCapabilities: ['partnerNativeDocumentDeliveryV1']`、宿主 `supportedExtensionCapabilities` 包含同值且具体 provider action 未禁用为准；host API v4 本身不授予能力。新宿主继续兼容旧 v1-v3 扩展但不为其推断直创；v3 validator 对 0.9.0 升级必须原子拒绝整个 v4 包，保留已安装 0.8.1、enabled 状态和 bindings，并提示升级 Space，不能展示无法执行的专家能力。未来兼容版本仍按 capability 判定。
- 连接器 action 可单独关闭以回退。关闭后新建请求应明确能力不可用或只给用户要求的草稿，不能恢复静默本地 Artifact；append 审核链和历史记录保持不变。

### 5. 实现步骤

按真实扩展包 → 单专家多轮 / 恢复 → 专家编辑与预设迁移 → 连接器读取 →
受审远端写入 → Electron 全链路验收推进。用户已批准顺序实施：P1–P3 已通过代码、自动检查与双轴评审，
macOS arm64 打包、扩展安装 / 启停 / 重装及专家核心交互也已通过隔离测试；真实模型重启和完整跨平台桌面链留待 P6。
P4/P5 已按用户指定采用飞书官方 CLI：限定会话文档范围、读取、受审新建 / 追加及回执；详细边界见 [飞书切片](f146-p4-p5-feishu.md)。真实账号授权与第三方读写验收仍留待 P6，不把 fixture 视为真实连接成功。
用户进一步确认开发 [首次连接向导与简约界面](f146-feishu-onboarding.md)：可取消授权任务、独立库卡片 / 居中弹窗与输入框会话开关。2026-09-01 又以“Space 安装包内置固定校验 CLI、连接按钮单次操作、本地静默解包后进入官方网页”的决策取代原显式下载 / 安装确认；CLI 随 Space 更新，未来后台更新只暂存并从下一任务启用。沿用 Space 风格，无 Figma 或新执行引擎。
2026-09-01 用户已确认[官方路线与分批计划](f146-multi-connectors.md)。首批实现企业微信、钉钉、腾讯会议的官方 CLI 授权与指定资源只读，独立插件库升级至 0.6.0；其余六家及新远端写操作尚未实施。三家真实账号授权与读取仍须用户验收，不将夹具或卡片展示视为真实连通。飞书后续实际授权与 0.5.1 升级证据以首次连接文档第 6–7 节为准，不将旧阶段的未验证描述视为当前结果。
同日用户进一步确认实施 P7“飞书办公套件专家”：持续引用、两级任务引导、一个明确飞书 Skill 和同包连接器组成首个正式平台专家；首个新增执行切片只贯通受控“新建多维表格”和右侧真实飞书资源，不放开删除、共享或任意写入。公共 seam、票序和回退边界见[详细计划的首个正式平台专家补充](v0.1.61-partner-plugin-library.md#首个正式平台专家补充2026-09-01)。
2026-09-02 用户确认并启动 P8“Partner 平台原生文档直达交付（飞书首适配）”：Partner 在用户明确要求新建平台文档且对应连接可用时直接执行，不再生成本地 Artifact 或待审核新建提案；创建后必须由 provider adapter read-back 才能返回真实标题和 canonical URL，并在右侧打开该平台网页。宿主任务、changed signal、opener、browser target 与去重必须 provider-neutral，使钉钉、腾讯文档可复用，而不是写死飞书；未登录时展示各平台官方登录页。多个账号先由宿主可信选择器取得用户选择。既有文档追加 / 修改仍审核，删除 / 覆盖 / 权限 / 批量操作不开放。用户随后再次明确 P8 只修改 Partner：不改 Coder 专属代码，也不改 Coder / Partner 共用的 `feishu-office-suite` Skill。首个宿主交付是 fork prerelease `v0.1.61-p.1`，不提前发布仍受 F137/F139 门禁的 canonical v0.1.61。详细风险矩阵、typed task、票序和版本决定见[详细计划 P8](v0.1.61-partner-plugin-library.md#p8-partner-平台原生文档直达交付增量飞书首适配2026-09-02)。

P8 的五张 tracer-bullet 票已获用户批准并写入详细计划。P8-T1–T4 已完成：通用契约、飞书首适配、可信账号/目录路由、幂等恢复与跨来源资源去重均已通过定向自动测试；右侧网页容器也已改为无 Node 权限且网页登录态独立持久化的 provider-neutral 容器，可承载平台官方登录页。P8-T5 的代码、完整自动回归、静态检查、生产构建和双轴 review 已完成，仅等待获授权真实飞书账号完成远端创建与网页登录验收。

### 6. 验收标准

独立包移除不破坏宿主；专家每轮及恢复一致；共享 Skill 文件保持不变；
连接器权限不跨会话；低风险新建文档按当前授权直接执行，既有文档修改未审不执行；结构化回执反映真实结果且凭据不泄漏。
平台专家的能力引导只更新可编辑草稿；Base 新建必须经过当前会话连接器与账号授权，成功后才可在隔离右栏打开真实 URL。
平台原生文档直创只能由 Partner 当前运行中可用的受控 provider 工具触发；成功返回并打开宿主验证的真实 URL。交付和右侧打开契约不得写死飞书，飞书首适配与后续钉钉、腾讯文档共享 provider-neutral task / signal / opener。缺失连接 / 授权时不静默改成本地文件，unknown / partial 不自动重试；实现改动只进入 Partner 专属路径和 Partner 连接器宿主契约。
飞书正常连接流程不得要求用户安装 CLI；私有托管副本缺失、损坏或版本不匹配时先从有效内置归档静默修复，只有内置归档缺失、损坏或不兼容才提示更新或重装 Space；不得用全局 CLI、插件 executable 或飞书桌面客户端伪装连接成功。
自动、人工、打包及回退清单以详细计划第 6 节为准，未经验证不得标记完成。

#### P8 User Stories

1. 作为已在当前 Partner 会话启用飞书的用户，我想直接说“创建一份飞书文档，内容你决定”，以便无需理解 Artifact、Pending review 或 Results 就拿到真实文档。
2. 作为指定了已授权飞书文件夹的用户，我想让文档只创建在该目录，以便系统不会私自改去个人空间或本地文件。
3. 作为未指定目录的用户，我想默认使用连接账号的个人空间，以便不必先寻找或粘贴文件夹链接。
4. 作为尚未启用、需要重授权或缺少目录 scope 的用户，我想看到准确的恢复动作，以便修复连接，而不是收到一个冒充完成的本地文件。
5. 作为只想先看草稿的用户，我想让 Partner 尊重“暂不创建”，以便草拟行为不会产生远端副作用。
6. 作为修改既有文档的用户，我想继续看到完整差异并确认一次，以便新建流程的简化不会放松已有资源的保护。
7. 作为遇到超时、partial 或 unknown 结果的用户，我想让系统停止自动重试并提示核对，以便避免重复创建文档。
8. 作为同时连接多个飞书账号的 Partner 用户，我想在创建前明确选择账号，以便文档不会被静默写到第一个或最近使用的账号。

#### P8 Testing Decisions

本功能使用用户已确认的一个端到端公共 seam：**Partner conversation / run-scoped connector contract**。主 tracer 从 `RealKodaXSession.send()` 和录制型模型 provider 进入，让真实 SDK 会话实际看见并调用 `partner_feishu_document_create`；中间使用真实 run runtime、`PartnerConnectorService` 和持久化 store，只在飞书 CLI 外部边界替换为确定性 fake。出口观察结构化 tool result、公开的 `PartnerRemoteRecords.documentTasks` / changed signal、Partner workspace 的审核 / Artifact / Delivery 公共投影和最终 browser target；不直接调用私有提交方法、不旁路读取 store 文件，也不以 mock service 直接返回 task 代替持久化门。期望值来自固定请求 / 回执样例，而不是用生产代码重算。

- 第一条 tracer 验证个人空间新建：每个被接纳的顶层用户输入在 runtime 构建前获得一个稳定 `turnExecutionId`，pre-admission retry / daemon reconnect 沿用它；interrupt / mid-turn 输入仍属于当前 admitted run，after-turn 队列只有在下一次 admission 时才取得新 ID。同一 ID 与相同解析后参数即使被模型重复调用，公开 records 也只返回同一 task、CLI 只派发一次；同一 run 内不同内容使用不同 key，若用户真要两份参数完全相同的文档则需下一轮明确提出。create 后必须 read-back 标题与正文，返回 persisted succeeded task，不产生 proposal / receipt / Artifact / Delivery，并通过 live signal 投影为一个匹配 canonical URL 的 browser target；初始历史加载不打开。
- 后续纵切覆盖已授权文件夹、0 / 1 / 多个可用账号、缺失 create 或 readonly scope、Plan、身份或 revision 变化、扩展禁用、工具列出后撤权、重复领取、`preparing` / `submitting` 进程恢复、invalid receipt、read-back 失败、标题 / URL / 正文不匹配、failed / partial / unknown，以及这些状态均不自动打开或重试。多账号无 host-owned choice 时 0 dispatch；选择后只能解析该账号，模型不能用另一 ID 覆盖。
- 0 账号测试同时观察 trusted eligibility context / UI reason 和实际 provider prompt，分别验证连接、启用、重授权、补 create/read scope 与升级宿主的恢复动作；工具隐藏本身不作为“恢复动作准确”的唯一证据。
- 兼容测试从公开 records / apply contract 验证旧 create proposal、receipt、Base task 仍可使用；模型专用 proposal schema 只含 append，持久化 / API schema 仍能解析和处理旧 create。v1 → v2 另允许一个窄 migration seam：仅用固定 v1 bytes 安排临时 store，通过 `PartnerConnectorStore` 公共 read/mutate 与重启观察迁移；注入原子替换失败后原 v1 仍可读。旧专家快照和 0.8.1 扩展记录分别走 session-runtime 与 extension-store 公共 seam，不冒充 connector-records 证明。
- capability matrix 不锁死精确版本：`hostApiVersion >= 4`、extension `requiredHostCapabilities`、host supported set 和 action enabled 才出现；任一能力条件缺失则不出现或安装失败。测试保留已发布 v3 validator/schema fixture，断言它原子拒绝 v4 升级并保留 0.8.1/enabled/bindings；v4 host + 0.8.1 可加载但无 direct action，只给草稿 / 明确不可用；v4 host 缺所需 capability 时原子拒绝 0.9.0；双方具备 capability 但 action disabled 时包可加载、工具隐藏且历史可读。
- Partner ownership 测试从 Partner runtime tool list、Partner service/IPC 和 Partner workspace 投影断言：工具只在 Partner run 临时物化、非 Partner context 被 Partner service 拒绝、SDK 全局 tool list 未被污染、新 opener 只在 `PartnerWorkspace` 挂载。该测试写在 Partner 测试文件中，不新增或修改 Coder fixture / test。
- profile、专家和 Skill 的测试验证路由语义与真实 tool availability，不只匹配 UI 文案。renderer 测试经共享 arbiter 验证 preparing/submitting 不打开、首次 succeeded 打开一次、重复 / 乱序 / A→B→A / signal 先于 records / 跨 scope / 历史 hydration 不重放；direct task 与不同 ID 的 legacy receipt 指向同 documentId 时也只有一个 open intent / tab。
- 自动测试不宣称第三方成功。最终人工验收使用用户明确授权的非敏感飞书测试账号 / 目录，核对真实创建位置、标题、内容、链接、右侧网页登录和重复创建情况；结果单独记录。

#### P8 Out of Scope

- 不增加删除、覆盖、移动、分享 / 权限、评论、Wiki、消息、邮件、日历、会议、审批或批量操作。
- 不把 Notion、Slack、Atlassian、Airtable、腾讯会议、Zoom 等其他连接器自动提升为可写；它们各自需要真实 action contract。
- 不新增通用 connector task framework、第二套 Agent runtime、SDK fork、共享 Skill resolver / installer，也不新增或修改任何 Coder 分支。
- 不自动登录飞书网页、不读取 browser cookie 作为连接器凭据、不把普通网页自动化当作 API 创建的替代方案。
- 本切片不承诺复杂富文本、附件、图片或任意飞书 XML。首版保持现有受控文档内容能力；需要扩展内容结构时另以真实 CLI 契约和验收追加。
- 不承诺在 store 已迁移为 v2 后把安装包降回只认识 v1 的旧二进制；受支持回退是关闭 direct action、保留新宿主读取历史，再以前向修复恢复能力。
