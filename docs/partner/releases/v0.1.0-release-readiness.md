# Partner 0.1.0 发布就绪记录

> 状态：**本地 Release Candidate（RC）**。版本与本地自动化证据由本文件所在提交冻结；尚未创建最终 Git tag、远端 push 或 GitHub Release。
>
> 候选分支：`integration/partner-upstream-20260904-v0.1.0`
>
> 整理起点：`ea7d61c046d06c44cb3fd60c5ba6e1c7063a605b`
>
> Partner 功能边界：`0d1667d4c4b48abb05962ceeef4e4154309d14a7`
>
> 对外发布：**Blocked**。在取得仓库 [`LICENSE`](../../../LICENSE) 所要求的适用书面授权前，不执行远端 push、远端 tag 或 GitHub Release。

## 1. 本轮结论

本轮把现有 Partner 产品线和 Partner 插件库统一整理为 `0.1.0` 本地候选，不修改 Space 根应用版本，也不把 Partner 的本地状态冒充为 Space 已集成状态。

- Partner 产品版本：`0.1.0`
- Partner 插件库版本：`0.1.0`
- Space 兼容基线：`0.1.46-alpha.5`
- KodaX 依赖基线：`0.7.96-beta.1`
- Partner Host API：`4`
- 必需宿主能力：`partnerNativeDocumentDeliveryV1`
- 预留发布标签：`partner-v0.1.0`

`partner-v0.1.0` 仅是预留名称，当前没有创建。不得使用 plain `v0.1.0`：`v*` 命名空间属于 Space 主应用版本，且现有 Space 发布工作流按根应用版本处理 `v*` tag。

## 2. 候选矩阵

| 对象             | 候选值                                        | 权威来源                                                                                 | 当前状态         |
| ---------------- | --------------------------------------------- | ---------------------------------------------------------------------------------------- | ---------------- |
| Partner 产品     | `0.1.0`                                       | [`FEATURE_LIST.md`](../FEATURE_LIST.md) 与 [`features/v0.1.0.md`](../features/v0.1.0.md) | Local RC         |
| Partner 插件库   | `0.1.0`                                       | `extensions/partner-library/manifest.json`                                               | Local RC         |
| Extension 产物   | `kodax.partner-library-0.1.0.space-extension` | `scripts/build-partner-extension.mjs`                                                    | Pass（本地构建） |
| Manifest 格式    | `1`                                           | Partner manifest                                                                         | Pass             |
| Partner Host API | `4`                                           | Partner manifest                                                                         | Pass             |
| 必需宿主能力     | `partnerNativeDocumentDeliveryV1`             | Partner manifest                                                                         | Pass             |
| Space 兼容基线   | `0.1.46-alpha.5`                              | 根目录与 Desktop package manifests                                                       | Pass（本地回归） |
| KodaX 依赖基线   | `0.7.96-beta.1`                               | 根目录、Desktop package manifests 与 lockfile                                            | Pass（本地回归） |
| Partner tag      | `partner-v0.1.0`                              | 本文档的 Partner tag 规则                                                                | 已预留，未创建   |

Partner 版本、插件库版本、Host API 和 Space 版本是四条独立维度。版本号相同不授予权限；宿主是否兼容只由 Host API 与 required capability 协商结果决定。

## 3. Feature 范围

| PF                                                                                                | 0.1.0 呈现方式    | 当前状态             | 范围边界                                                                 |
| ------------------------------------------------------------------------------------------------- | ----------------- | -------------------- | ------------------------------------------------------------------------ |
| [PF001](../features/v0.1.0.md#feature-pf001) Partner Extension & Expert Library                   | Core candidate    | `InProgress / Local` | Extension 生命周期、专家目录、用户专家、可选 Skill、会话绑定与不可变快照 |
| [PF002](../features/v0.1.0.md#feature-pf002) Governed Connector Previews & Feishu Platform Expert | Preview candidate | `InProgress / Local` | 可信宿主管理的连接、范围、读取和受审写入；真实服务能力必须逐项验收       |
| [PF003](../features/v0.1.0.md#feature-pf003) Receipt-First Delivery & Unified Workspace           | Core candidate    | `InProgress / Local` | 创建回执与内容校验分离、统一任务详情工作区、受控原生网页                 |

三项共同映射到 Space `F146`：`F146 <- PF001, PF002, PF003`。本记录不会把任何 PF 自动改为 `Completed`、`Ready`、`Proposed` 或 `Integrated`；状态只能在具有相应证据后按 Partner Feature 流程推进。

## 4. Core 与 Preview 边界

### Core candidate

- Partner Extension 可独立构建、安装、启用、停用和卸载；Extension UI 不作为可信 Renderer 模块静态编译。
- 内置专家、用户专家、可选单个 Skill、会话绑定和不可变专家快照形成稳定的 Partner 交互基础。
- 任务资料、协作、产物、专家、Skill、连接器和受控网页进入统一详情工作区，不改变 Coder 的产品逻辑或权限。
- 创建操作采用 receipt-first 语义：宿主先验证远端资源身份与 canonical URL，再分别报告资源创建状态和内容校验状态。

### Preview candidate

- 飞书：候选范围包括会话选定文档读取、受控新建文档与 Base，以及审核后追加既有文档；只有真实账号、真实资源和私有读回证据才能证明对应能力可用。
- 企业微信、钉钉、腾讯会议：当前只有适配器、受控 CLI 和 fixture/契约证据，不声明真实企业环境已通过。
- Notion、Airtable、Atlassian：当前只有 OAuth、远程 MCP 与本地契约证据，不声明真实租户端到端已通过。
- Slack、Zoom：缺少受审的产品级应用配置时必须保持 unavailable，不得发起授权或显示已连接。

任何 Preview 项都不得用自动化 fixture、Mock、目录展示或“连接按钮存在”替代真实服务验收。未实测的平台、账号、scope 或操作必须记录为 `Pending`、`Blocked` 或 `Unavailable`，不能记为 Pass。

## 5. 技术门槛

以下结果在 2026-09-04 的当前候选内容上执行。测试机为 macOS 26.6.2 arm64、Node 25.9.0、npm 11.12.1；仓库 `.nvmrc` 指定的 Node 22.23.1 与远端 CI 仍需在最终发布提交上补跑。

| 门槛                           | 命令或证据                                                  | 状态                                                                                                                                  |
| ------------------------------ | ----------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------- |
| Manifest 与归档一致            | `node --test scripts/test/build-partner-extension.test.mjs` | Pass：3/3                                                                                                                             |
| Partner Extension 构建         | `npm run build:partner-extension`                           | Pass：SHA-256 `8d08548d4a78458c345a8d85d05e9d2d9470a0ea56872e8735f001b4919cd631`                                                      |
| IPC schema 回归                | `npm test -w @kodax-space/space-ipc-schema`                 | Pass：350/350                                                                                                                         |
| Desktop / Partner / Coder 回归 | `npm test -w @kodax-space/desktop`                          | Pass：由全工作区测试执行                                                                                                              |
| 全工作区测试                   | `NODE_OPTIONS=--no-experimental-webstorage npm test`        | Pass                                                                                                                                  |
| 类型检查                       | `npm run typecheck`                                         | Pass                                                                                                                                  |
| Lint                           | `npm run lint`                                              | Pass                                                                                                                                  |
| 格式策略                       | `npm run format:check`                                      | Blocked：上游基线仍有 340 个文件不满足当前 Prettier；本轮 Partner/测试文件已定向通过，Space `docs/FEATURE_LIST.md` 保留既有全文件漂移 |
| Space smoke build              | `npm run build:smoke`                                       | Pass；Vite 仅报告既有 chunk-size warning                                                                                              |
| Diff 健康                      | `git diff --check`                                          | Pass                                                                                                                                  |
| 精确候选提交远端 CI            | CI URL 与 commit SHA                                        | Pending；当前未 push                                                                                                                  |

完整技术门槛还包括：

- 候选提交确实基于预定的 `upstream/main`，且版本、README、测试断言、Changelog、归档文件名互相一致。
- Partner connector 测试在修正两个测试时序门后连续执行五轮通过；修改只强化“创建回执不等待内容回读”和“测试结束前后台校验已收敛”的既有公共行为，没有改变生产实现。
- Extension 安装、默认禁用、显式启用、停用、同 ID 替换、卸载、重装和失败恢复均有证据。
- 所有 Partner 触及的共享 IPC、Shell、Session、Runtime 与 Electron main 接缝均有 Coder 非回归证据。
- 跟踪文件与构建产物通过凭据、私密内容、机器绝对路径、生成物和第三方许可证检查。
- 打包宿主能在声明支持的平台启动，并能安装、启用独立 `.space-extension` 产物。

## 6. 人工与真实服务门槛

按 [`FEATURE_F146_PARTNER_v0.1.0_TEST_GUIDE.md`](../test-guides/FEATURE_F146_PARTNER_v0.1.0_TEST_GUIDE.md) 执行并记录：

- Extension 生命周期、专家选择、多轮、重启、fork、切换、自定义专家、缺失 Skill 与跨会话隔离。
- 飞书授权、范围读取、文档/Base 创建、真实回执、私有读回、受审追加、断开和 scope 撤销。
- 其他连接器必须有逐服务真实账号结果，或继续以 Preview/Unavailable 显示且不产生虚假成功状态。
- 宽窄布局、明暗主题、统一详情工作区和隔离网页的桌面视觉与键盘操作。
- 从本地旧候选 `0.9.1` 到 `0.1.0` 的同 ID 替换、显式重新启用、数据保留和回滚。
- 每个未执行的操作系统或真实服务均明确标为未验证。

## 7. 候选证据

结果必须包含精确 commit、命令或用例、操作系统、日期、结果和任何 skipped/deferred 原因。本表只把已经执行的本地自动化标为通过；人工、真实服务、标准 Node/CI 和授权结论继续保持 Pending 或 Blocked。

| 证据组                            | 状态    | 证据                                            |
| --------------------------------- | ------- | ----------------------------------------------- |
| Partner 产品与插件库版本一致      | Pass    | 产品、manifest、构建断言与归档名均为 `0.1.0`    |
| Partner Extension 自动化与构建    | Pass    | 3/3；归档 SHA-256 见技术门槛                    |
| 全工作区与 Coder 回归             | Pass    | 本地全工作区测试、typecheck、lint、smoke 均通过 |
| 打包宿主与 Extension 生命周期     | Pending | 本地构建通过；打包宿主人工生命周期尚未执行      |
| 专家、会话与统一工作区            | Pending | 自动化通过；开发态/打包态人工验收尚未执行       |
| 飞书真实账号与资源                | Pending | 待真实服务执行                                  |
| 其他 Preview 连接器               | Pending | 待逐服务记录，未配置时保持 Preview/Unavailable  |
| `0.9.1 -> 0.1.0` 同 ID 迁移与回滚 | Pending | 待人工执行                                      |
| 精确候选提交远端 CI 与发布产物    | Pending | 尚无远端候选；标准 Node 22.23.1 也待 CI 补跑    |
| 书面分发授权                      | Blocked | 本仓库尚未记录适用书面授权                      |

## 8. 分发授权门槛

仓库 [`LICENSE`](../../../LICENSE) 对 Community Grant 之外的使用、再分发和 Partner distribution 设有额外条件。发布负责人必须在任何对外操作前，确认适用的书面授权明确覆盖接收者、产品身份、产物和使用场景。

在该证据未记录前：

- 不向远端 push 发布提交或发布分支；
- 不向远端 push `partner-v0.1.0`；
- 不上传 `.space-extension` 或安装包；
- 不创建 GitHub Release；
- 不把本地 RC 描述成已公开发布。

这是根据当前仓库许可证设置的项目发布门槛，不替代法律意见。

## 9. 回滚边界

首选产品级回滚是停用相关连接器能力，或停用/卸载 Partner Extension。该操作不得删除会话历史、用户专家、专家快照、任务记录或已经存在的远端资源。

`0.9.1 -> 0.1.0` 是一次版本编号重整下的同 ID 替换，不应假设 Extension Store 会进行 SemVer 升级判断。替换后应保持禁用，等待用户明确重新启用；数据保留与回滚必须按测试指南实测。Space 宿主降级是另一项操作，在数据格式兼容性得到验证前不得声称安全。

## 10. 晋级规则

只要任一必需技术、人工作业、真实服务或授权门槛处于 Pending、Failed 或 Blocked，本文件就保持 **Release Candidate**。

只有在同一个精确候选提交上完成全部证据，并取得适用书面授权后，才可以：

1. 创建并评审专用 Partner release commit；
2. 创建 `partner-v0.1.0`，不得创建 plain `v0.1.0`；
3. 精确 push release commit 和该单个 tag，不使用 `git push --tags`；
4. 创建与相同 commit、Changelog 和产物校验值一致的 GitHub Release。
